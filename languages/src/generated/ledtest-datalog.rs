// Generated Ascent Datalog for ledtest theory
// This file is generated by the theory! macro and is for inspection only.
// Do not edit manually - changes will be overwritten.

ascent_source! {
    ledtest_source:

    // Relations
relation num(Num);

#[ds(crate::eqrel)] relation eq_num(Num, Num);

relation rw_num(Num, Num);

relation pred(Pred);

#[ds(crate::eqrel)] relation eq_pred(Pred, Pred);

relation rw_pred(Pred, Pred);

relation expr(Expr);

#[ds(crate::eqrel)] relation eq_expr(Expr, Expr);

relation rw_expr(Expr, Expr);

relation step_term(Num);


    // Category rules
num(sub.clone()) <--
    num(t),
    for sub in { std::thread_local! { static POOL_NUM_NUM : std::cell::Cell < Vec < Num >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_NUM.with(| p | p.take()); buf.clear(); match t {
        Num::AddNum(f0, f1) => {
            buf.push(f0.as_ref().clone());
            buf.push(f1.as_ref().clone());
        },
        Num::MulNum(f0, f1) => {
            buf.push(f0.as_ref().clone());
            buf.push(f1.as_ref().clone());
        },
        Num::NegNum(f0) => {
            buf.push(f0.as_ref().clone());
        },
        Num::FactNum(f0) => {
            buf.push(f0.as_ref().clone());
        },
        Num::ApplyNum(lam, arg) => {
            buf.push(lam.as_ref().clone());
            buf.push(arg.as_ref().clone());
        },
        Num::MApplyNum(lam, args) => {
            buf.push(lam.as_ref().clone());
            buf.extend(args.iter().cloned());
        },
        Num::LamNum(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Num::MLamNum(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Num::ApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::MApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::LamPred(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Num::MLamPred(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Num::ApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::MApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::LamExpr(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Num::MLamExpr(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_NUM.with(| p | p.set(buf)); iter_buf }.into_iter();

pred(sub.clone()) <--
    num(t),
    for sub in { std::thread_local! { static POOL_NUM_PRED : std::cell::Cell < Vec < Pred >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_PRED.with(| p | p.take()); buf.clear(); match t {
        Num::ApplyPred(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        Num::MApplyPred(_, args) => {
            buf.extend(args.iter().cloned());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_PRED.with(| p | p.set(buf)); iter_buf }.into_iter();

expr(sub.clone()) <--
    num(t),
    for sub in { std::thread_local! { static POOL_NUM_EXPR : std::cell::Cell < Vec < Expr >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_EXPR.with(| p | p.take()); buf.clear(); match t {
        Num::ExprToNum(f0) => {
            buf.push(f0.as_ref().clone());
        },
        Num::ApplyExpr(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        Num::MApplyExpr(_, args) => {
            buf.extend(args.iter().cloned());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter();

num(sub.clone()) <--
    pred(t),
    for sub in { std::thread_local! { static POOL_PRED_NUM : std::cell::Cell < Vec < Num >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_NUM.with(| p | p.take()); buf.clear(); match t {
        Pred::EqNum(f0, f1) => {
            buf.push(f0.as_ref().clone());
            buf.push(f1.as_ref().clone());
        },
        Pred::NeNum(f0, f1) => {
            buf.push(f0.as_ref().clone());
            buf.push(f1.as_ref().clone());
        },
        Pred::ApplyNum(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        Pred::MApplyNum(_, args) => {
            buf.extend(args.iter().cloned());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_NUM.with(| p | p.set(buf)); iter_buf }.into_iter();

pred(sub.clone()) <--
    pred(t),
    for sub in { std::thread_local! { static POOL_PRED_PRED : std::cell::Cell < Vec < Pred >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_PRED.with(| p | p.take()); buf.clear(); match t {
        Pred::AndPred(f0, f1) => {
            buf.push(f0.as_ref().clone());
            buf.push(f1.as_ref().clone());
        },
        Pred::ApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::MApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::LamNum(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Pred::MLamNum(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Pred::ApplyPred(lam, arg) => {
            buf.push(lam.as_ref().clone());
            buf.push(arg.as_ref().clone());
        },
        Pred::MApplyPred(lam, args) => {
            buf.push(lam.as_ref().clone());
            buf.extend(args.iter().cloned());
        },
        Pred::LamPred(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Pred::MLamPred(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Pred::ApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::MApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::LamExpr(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Pred::MLamExpr(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_PRED.with(| p | p.set(buf)); iter_buf }.into_iter();

expr(sub.clone()) <--
    pred(t),
    for sub in { std::thread_local! { static POOL_PRED_EXPR : std::cell::Cell < Vec < Expr >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_EXPR.with(| p | p.take()); buf.clear(); match t {
        Pred::ApplyExpr(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        Pred::MApplyExpr(_, args) => {
            buf.extend(args.iter().cloned());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter();

num(sub.clone()) <--
    expr(t),
    for sub in { std::thread_local! { static POOL_EXPR_NUM : std::cell::Cell < Vec < Num >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_NUM.with(| p | p.take()); buf.clear(); match t {
        Expr::CastNum(f0) => {
            buf.push(f0.as_ref().clone());
        },
        Expr::ApplyNum(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        Expr::MApplyNum(_, args) => {
            buf.extend(args.iter().cloned());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_NUM.with(| p | p.set(buf)); iter_buf }.into_iter();

pred(sub.clone()) <--
    expr(t),
    for sub in { std::thread_local! { static POOL_EXPR_PRED : std::cell::Cell < Vec < Pred >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_PRED.with(| p | p.take()); buf.clear(); match t {
        Expr::CastPred(f0) => {
            buf.push(f0.as_ref().clone());
        },
        Expr::ApplyPred(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        Expr::MApplyPred(_, args) => {
            buf.extend(args.iter().cloned());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_PRED.with(| p | p.set(buf)); iter_buf }.into_iter();

expr(sub.clone()) <--
    expr(t),
    for sub in { std::thread_local! { static POOL_EXPR_EXPR : std::cell::Cell < Vec < Expr >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_EXPR.with(| p | p.take()); buf.clear(); match t {
        Expr::EPar(f0, f1) => {
            buf.push(f0.as_ref().clone());
            buf.push(f1.as_ref().clone());
        },
        Expr::ApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::MApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::LamNum(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Expr::MLamNum(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Expr::ApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::MApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::LamPred(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Expr::MLamPred(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Expr::ApplyExpr(lam, arg) => {
            buf.push(lam.as_ref().clone());
            buf.push(arg.as_ref().clone());
        },
        Expr::MApplyExpr(lam, args) => {
            buf.push(lam.as_ref().clone());
            buf.extend(args.iter().cloned());
        },
        Expr::LamExpr(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        Expr::MLamExpr(scope) => {
            buf.push(scope.inner().unsafe_body.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter();

num(c1.clone().normalize()) <--
    num(c0),
    rw_num(c0, c1);

rw_num(t.clone(), match t {
    Num::ApplyNum(_, arg) => Num::ApplyNum(Box::new(new_lam.clone()), arg.clone()),
    Num::MApplyNum(_, args) => Num::MApplyNum(Box::new(new_lam.clone()), args.clone()),
    Num::ApplyPred(_, arg) => Num::ApplyPred(Box::new(new_lam.clone()), arg.clone()),
    Num::MApplyPred(_, args) => Num::MApplyPred(Box::new(new_lam.clone()), args.clone()),
    Num::ApplyExpr(_, arg) => Num::ApplyExpr(Box::new(new_lam.clone()), arg.clone()),
    Num::MApplyExpr(_, args) => Num::MApplyExpr(Box::new(new_lam.clone()), args.clone()),
    _ => unreachable!(),
}) <--
    num(t),
    for lam in { std::thread_local! { static POOL_NUM_CONG_LAM : std::cell::Cell < Vec < Num >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_CONG_LAM.with(| p | p.take()); buf.clear(); match t {
        Num::ApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::MApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::ApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::MApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::ApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Num::MApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_CONG_LAM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_num(lam, new_lam);

rw_num(t.clone(), match t {
    Num::ApplyNum(lam, _) => Num::ApplyNum(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    num(t),
    for arg in { std::thread_local! { static POOL_NUM_CONG_ARG_NUM : std::cell::Cell < Vec < Num >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_CONG_ARG_NUM.with(| p | p.take()); buf.clear(); match t {
        Num::ApplyNum(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_CONG_ARG_NUM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_num(arg, new_arg);

rw_num(t.clone(), match t {
    Num::ApplyPred(lam, _) => Num::ApplyPred(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    num(t),
    for arg in { std::thread_local! { static POOL_NUM_CONG_ARG_PRED : std::cell::Cell < Vec < Pred >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_CONG_ARG_PRED.with(| p | p.take()); buf.clear(); match t {
        Num::ApplyPred(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_CONG_ARG_PRED.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_pred(arg, new_arg);

rw_num(t.clone(), match t {
    Num::ApplyExpr(lam, _) => Num::ApplyExpr(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    num(t),
    for arg in { std::thread_local! { static POOL_NUM_CONG_ARG_EXPR : std::cell::Cell < Vec < Expr >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_CONG_ARG_EXPR.with(| p | p.take()); buf.clear(); match t {
        Num::ApplyExpr(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_CONG_ARG_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_expr(arg, new_arg);

pred(c1.clone().normalize()) <--
    pred(c0),
    rw_pred(c0, c1);

rw_pred(t.clone(), match t {
    Pred::ApplyNum(_, arg) => Pred::ApplyNum(Box::new(new_lam.clone()), arg.clone()),
    Pred::MApplyNum(_, args) => Pred::MApplyNum(Box::new(new_lam.clone()), args.clone()),
    Pred::ApplyPred(_, arg) => Pred::ApplyPred(Box::new(new_lam.clone()), arg.clone()),
    Pred::MApplyPred(_, args) => Pred::MApplyPred(Box::new(new_lam.clone()), args.clone()),
    Pred::ApplyExpr(_, arg) => Pred::ApplyExpr(Box::new(new_lam.clone()), arg.clone()),
    Pred::MApplyExpr(_, args) => Pred::MApplyExpr(Box::new(new_lam.clone()), args.clone()),
    _ => unreachable!(),
}) <--
    pred(t),
    for lam in { std::thread_local! { static POOL_PRED_CONG_LAM : std::cell::Cell < Vec < Pred >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_CONG_LAM.with(| p | p.take()); buf.clear(); match t {
        Pred::ApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::MApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::ApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::MApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::ApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Pred::MApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_CONG_LAM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_pred(lam, new_lam);

rw_pred(t.clone(), match t {
    Pred::ApplyNum(lam, _) => Pred::ApplyNum(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    pred(t),
    for arg in { std::thread_local! { static POOL_PRED_CONG_ARG_NUM : std::cell::Cell < Vec < Num >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_CONG_ARG_NUM.with(| p | p.take()); buf.clear(); match t {
        Pred::ApplyNum(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_CONG_ARG_NUM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_num(arg, new_arg);

rw_pred(t.clone(), match t {
    Pred::ApplyPred(lam, _) => Pred::ApplyPred(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    pred(t),
    for arg in { std::thread_local! { static POOL_PRED_CONG_ARG_PRED : std::cell::Cell < Vec < Pred >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_CONG_ARG_PRED.with(| p | p.take()); buf.clear(); match t {
        Pred::ApplyPred(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_CONG_ARG_PRED.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_pred(arg, new_arg);

rw_pred(t.clone(), match t {
    Pred::ApplyExpr(lam, _) => Pred::ApplyExpr(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    pred(t),
    for arg in { std::thread_local! { static POOL_PRED_CONG_ARG_EXPR : std::cell::Cell < Vec < Expr >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_CONG_ARG_EXPR.with(| p | p.take()); buf.clear(); match t {
        Pred::ApplyExpr(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_CONG_ARG_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_expr(arg, new_arg);

expr(c1.clone().normalize()) <--
    expr(c0),
    rw_expr(c0, c1);

rw_expr(t.clone(), match t {
    Expr::ApplyNum(_, arg) => Expr::ApplyNum(Box::new(new_lam.clone()), arg.clone()),
    Expr::MApplyNum(_, args) => Expr::MApplyNum(Box::new(new_lam.clone()), args.clone()),
    Expr::ApplyPred(_, arg) => Expr::ApplyPred(Box::new(new_lam.clone()), arg.clone()),
    Expr::MApplyPred(_, args) => Expr::MApplyPred(Box::new(new_lam.clone()), args.clone()),
    Expr::ApplyExpr(_, arg) => Expr::ApplyExpr(Box::new(new_lam.clone()), arg.clone()),
    Expr::MApplyExpr(_, args) => Expr::MApplyExpr(Box::new(new_lam.clone()), args.clone()),
    _ => unreachable!(),
}) <--
    expr(t),
    for lam in { std::thread_local! { static POOL_EXPR_CONG_LAM : std::cell::Cell < Vec < Expr >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_CONG_LAM.with(| p | p.take()); buf.clear(); match t {
        Expr::ApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::MApplyNum(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::ApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::MApplyPred(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::ApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        Expr::MApplyExpr(lam, _) => {
            buf.push(lam.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_CONG_LAM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_expr(lam, new_lam);

rw_expr(t.clone(), match t {
    Expr::ApplyNum(lam, _) => Expr::ApplyNum(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    expr(t),
    for arg in { std::thread_local! { static POOL_EXPR_CONG_ARG_NUM : std::cell::Cell < Vec < Num >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_CONG_ARG_NUM.with(| p | p.take()); buf.clear(); match t {
        Expr::ApplyNum(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_CONG_ARG_NUM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_num(arg, new_arg);

rw_expr(t.clone(), match t {
    Expr::ApplyPred(lam, _) => Expr::ApplyPred(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    expr(t),
    for arg in { std::thread_local! { static POOL_EXPR_CONG_ARG_PRED : std::cell::Cell < Vec < Pred >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_CONG_ARG_PRED.with(| p | p.take()); buf.clear(); match t {
        Expr::ApplyPred(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_CONG_ARG_PRED.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_pred(arg, new_arg);

rw_expr(t.clone(), match t {
    Expr::ApplyExpr(lam, _) => Expr::ApplyExpr(lam.clone(), Box::new(new_arg.clone())),
    _ => unreachable!(),
}) <--
    expr(t),
    for arg in { std::thread_local! { static POOL_EXPR_CONG_ARG_EXPR : std::cell::Cell < Vec < Expr >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_CONG_ARG_EXPR.with(| p | p.take()); buf.clear(); match t {
        Expr::ApplyExpr(_, arg) => {
            buf.push(arg.as_ref().clone());
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_CONG_ARG_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_expr(arg, new_arg);


    // Equation rules
eq_num(t.clone(), t.clone()) <--
    num(t);

eq_pred(t.clone(), t.clone()) <--
    pred(t);

eq_expr(t.clone(), t.clone()) <--
    expr(t);

eq_expr(s.clone(), t.clone()) <--
    expr(s),
    expr(t),
    for (s_f0, s_f1, t_f0, t_f1) in { std::thread_local! { static POOL_EXPR_EQ_CONG_0 : std::cell::Cell < Vec < (Expr, Expr, Expr, Expr) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_EQ_CONG_0.with(| p | p.take()); buf.clear(); match (s, t) {
        (Expr::EPar(sf0, sf1), Expr::EPar(tf0, tf1)) => {
            buf.push((sf0.as_ref().clone(), sf1.as_ref().clone(), tf0.as_ref().clone(), tf1.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_EQ_CONG_0.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_expr(s_f0, t_f0),
    eq_expr(s_f1, t_f1);

eq_expr(s.clone(), t.clone()) <--
    expr(s),
    expr(t),
    for (s_f0, t_f0) in { std::thread_local! { static POOL_EXPR_EQ_CONG_1 : std::cell::Cell < Vec < (Num, Num) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_EQ_CONG_1.with(| p | p.take()); buf.clear(); match (s, t) {
        (Expr::CastNum(sf0), Expr::CastNum(tf0)) => {
            buf.push((sf0.as_ref().clone(), tf0.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_EQ_CONG_1.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_num(s_f0, t_f0);

eq_expr(s.clone(), t.clone()) <--
    expr(s),
    expr(t),
    for (s_f0, t_f0) in { std::thread_local! { static POOL_EXPR_EQ_CONG_2 : std::cell::Cell < Vec < (Pred, Pred) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_EQ_CONG_2.with(| p | p.take()); buf.clear(); match (s, t) {
        (Expr::CastPred(sf0), Expr::CastPred(tf0)) => {
            buf.push((sf0.as_ref().clone(), tf0.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_EQ_CONG_2.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_pred(s_f0, t_f0);

eq_num(s.clone(), t.clone()) <--
    num(s),
    num(t),
    for (s_f0, t_f0) in { std::thread_local! { static POOL_NUM_EQ_CONG_3 : std::cell::Cell < Vec < (Expr, Expr) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_EQ_CONG_3.with(| p | p.take()); buf.clear(); match (s, t) {
        (Num::ExprToNum(sf0), Num::ExprToNum(tf0)) => {
            buf.push((sf0.as_ref().clone(), tf0.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_EQ_CONG_3.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_expr(s_f0, t_f0);

eq_num(s.clone(), t.clone()) <--
    num(s),
    num(t),
    for (s_f0, t_f0) in { std::thread_local! { static POOL_NUM_EQ_CONG_4 : std::cell::Cell < Vec < (Num, Num) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_EQ_CONG_4.with(| p | p.take()); buf.clear(); match (s, t) {
        (Num::NegNum(sf0), Num::NegNum(tf0)) => {
            buf.push((sf0.as_ref().clone(), tf0.as_ref().clone()));
        },
        (Num::FactNum(sf0), Num::FactNum(tf0)) => {
            buf.push((sf0.as_ref().clone(), tf0.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_EQ_CONG_4.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_num(s_f0, t_f0);

eq_num(s.clone(), t.clone()) <--
    num(s),
    num(t),
    for (s_f0, s_f1, t_f0, t_f1) in { std::thread_local! { static POOL_NUM_EQ_CONG_5 : std::cell::Cell < Vec < (Num, Num, Num, Num) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_EQ_CONG_5.with(| p | p.take()); buf.clear(); match (s, t) {
        (Num::AddNum(sf0, sf1), Num::AddNum(tf0, tf1)) => {
            buf.push((sf0.as_ref().clone(), sf1.as_ref().clone(), tf0.as_ref().clone(), tf1.as_ref().clone()));
        },
        (Num::MulNum(sf0, sf1), Num::MulNum(tf0, tf1)) => {
            buf.push((sf0.as_ref().clone(), sf1.as_ref().clone(), tf0.as_ref().clone(), tf1.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_EQ_CONG_5.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_num(s_f0, t_f0),
    eq_num(s_f1, t_f1);

eq_pred(s.clone(), t.clone()) <--
    pred(s),
    pred(t),
    for (s_f0, s_f1, t_f0, t_f1) in { std::thread_local! { static POOL_PRED_EQ_CONG_6 : std::cell::Cell < Vec < (Num, Num, Num, Num) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_EQ_CONG_6.with(| p | p.take()); buf.clear(); match (s, t) {
        (Pred::EqNum(sf0, sf1), Pred::EqNum(tf0, tf1)) => {
            buf.push((sf0.as_ref().clone(), sf1.as_ref().clone(), tf0.as_ref().clone(), tf1.as_ref().clone()));
        },
        (Pred::NeNum(sf0, sf1), Pred::NeNum(tf0, tf1)) => {
            buf.push((sf0.as_ref().clone(), sf1.as_ref().clone(), tf0.as_ref().clone(), tf1.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_EQ_CONG_6.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_num(s_f0, t_f0),
    eq_num(s_f1, t_f1);

eq_pred(s.clone(), t.clone()) <--
    pred(s),
    pred(t),
    for (s_f0, s_f1, t_f0, t_f1) in { std::thread_local! { static POOL_PRED_EQ_CONG_7 : std::cell::Cell < Vec < (Pred, Pred, Pred, Pred) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_EQ_CONG_7.with(| p | p.take()); buf.clear(); match (s, t) {
        (Pred::AndPred(sf0, sf1), Pred::AndPred(tf0, tf1)) => {
            buf.push((sf0.as_ref().clone(), sf1.as_ref().clone(), tf0.as_ref().clone(), tf1.as_ref().clone()));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_EQ_CONG_7.with(| p | p.set(buf)); iter_buf }.into_iter(),
    eq_pred(s_f0, t_f0),
    eq_pred(s_f1, t_f1);


    // Rewrite rules
rw_pred(s.clone(), t) <--
    pred(s),
    if let Pred::EqNum(left, right) = s,
    if let Num::NumLit(a_ref) = left.as_ref(),
    if let Num::NumLit(b_ref) = right.as_ref(),
    let a = a_ref.clone(),
    let b = b_ref.clone(),
    let t = Pred::BoolLit((a == b));

rw_num(s.clone(), t) <--
    num(s),
    if let Num::AddNum(left, right) = s,
    if let Num::NumLit(a_ref) = left.as_ref(),
    if let Num::NumLit(b_ref) = right.as_ref(),
    let a = a_ref.clone(),
    let b = b_ref.clone(),
    let t = Num::NumLit((a + b));

rw_num(s.clone(), t) <--
    num(s),
    if let Num::MulNum(left, right) = s,
    if let Num::NumLit(a_ref) = left.as_ref(),
    if let Num::NumLit(b_ref) = right.as_ref(),
    let a = a_ref.clone(),
    let b = b_ref.clone(),
    let t = Num::NumLit((a * b));

rw_pred(s.clone(), t) <--
    pred(s),
    if let Pred::NeNum(left, right) = s,
    if let Num::NumLit(a_ref) = left.as_ref(),
    if let Num::NumLit(b_ref) = right.as_ref(),
    let a = a_ref.clone(),
    let b = b_ref.clone(),
    let t = Pred::BoolLit((a != b));

rw_pred(s.clone(), t) <--
    pred(s),
    if let Pred::AndPred(left, right) = s,
    if let Pred::BoolLit(a_ref) = left.as_ref(),
    if let Pred::BoolLit(b_ref) = right.as_ref(),
    let a = a_ref.clone(),
    let b = b_ref.clone(),
    let t = Pred::BoolLit((a && b));

rw_num(orig.clone(), t) <--
    num(orig),
    if let Num::NegNum(inner) = orig,
    if let Num::NumLit(s_ref) = inner.as_ref(),
    let a = s_ref.clone(),
    let t = Num::NumLit(((- a)));

rw_num(orig.clone(), t) <--
    num(orig),
    if let Num::FactNum(inner) = orig,
    if let Num::NumLit(s_ref) = inner.as_ref(),
    let a = s_ref.clone(),
    let t = Num::NumLit(({ (1 ..= a.max(0)).product::< i32 > () }));

rw_expr(lhs.clone(), match (lhs, vi) {
    (Expr::EPar(_, x1), 0usize) => Expr::EPar(Box::new(t.clone()), x1.clone()),
    (Expr::EPar(x0, _), 1usize) => Expr::EPar(x0.clone(), Box::new(t.clone())),
    _ => unreachable!(),
}) <--
    expr(lhs),
    for (field_val, vi) in { std::thread_local! { static POOL_EXPR_SCONG_EXPR : std::cell::Cell < Vec < (Expr, usize) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_SCONG_EXPR.with(| p | p.take()); buf.clear(); match lhs {
        Expr::EPar(x0, x1) => {
            buf.push(((** x0).clone(), 0usize));
            buf.push(((** x1).clone(), 1usize));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_SCONG_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_expr(field_val, t);

rw_expr(lhs.clone(), match (lhs, vi) {
    (Expr::CastNum(_), 0usize) => Expr::CastNum(Box::new(t.clone())),
    _ => unreachable!(),
}) <--
    expr(lhs),
    for (field_val, vi) in { std::thread_local! { static POOL_EXPR_SCONG_NUM : std::cell::Cell < Vec < (Num, usize) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_SCONG_NUM.with(| p | p.take()); buf.clear(); match lhs {
        Expr::CastNum(x0) => {
            buf.push(((** x0).clone(), 0usize));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_SCONG_NUM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_num(field_val, t);

rw_expr(lhs.clone(), match (lhs, vi) {
    (Expr::CastPred(_), 0usize) => Expr::CastPred(Box::new(t.clone())),
    _ => unreachable!(),
}) <--
    expr(lhs),
    for (field_val, vi) in { std::thread_local! { static POOL_EXPR_SCONG_PRED : std::cell::Cell < Vec < (Pred, usize) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_EXPR_SCONG_PRED.with(| p | p.take()); buf.clear(); match lhs {
        Expr::CastPred(x0) => {
            buf.push(((** x0).clone(), 0usize));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_EXPR_SCONG_PRED.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_pred(field_val, t);

rw_num(lhs.clone(), match (lhs, vi) {
    (Num::ExprToNum(_), 0usize) => Num::ExprToNum(Box::new(t.clone())),
    _ => unreachable!(),
}) <--
    num(lhs),
    for (field_val, vi) in { std::thread_local! { static POOL_NUM_SCONG_EXPR : std::cell::Cell < Vec < (Expr, usize) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_SCONG_EXPR.with(| p | p.take()); buf.clear(); match lhs {
        Num::ExprToNum(x0) => {
            buf.push(((** x0).clone(), 0usize));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_SCONG_EXPR.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_expr(field_val, t);

rw_num(lhs.clone(), match (lhs, vi) {
    (Num::AddNum(_, x1), 0usize) => Num::AddNum(Box::new(t.clone()), x1.clone()),
    (Num::AddNum(x0, _), 1usize) => Num::AddNum(x0.clone(), Box::new(t.clone())),
    (Num::MulNum(_, x1), 2usize) => Num::MulNum(Box::new(t.clone()), x1.clone()),
    (Num::MulNum(x0, _), 3usize) => Num::MulNum(x0.clone(), Box::new(t.clone())),
    (Num::NegNum(_), 4usize) => Num::NegNum(Box::new(t.clone())),
    (Num::FactNum(_), 5usize) => Num::FactNum(Box::new(t.clone())),
    _ => unreachable!(),
}) <--
    num(lhs),
    for (field_val, vi) in { std::thread_local! { static POOL_NUM_SCONG_NUM : std::cell::Cell < Vec < (Num, usize) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_NUM_SCONG_NUM.with(| p | p.take()); buf.clear(); match lhs {
        Num::AddNum(x0, x1) => {
            buf.push(((** x0).clone(), 0usize));
            buf.push(((** x1).clone(), 1usize));
        },
        Num::FactNum(x0) => {
            buf.push(((** x0).clone(), 5usize));
        },
        Num::MulNum(x0, x1) => {
            buf.push(((** x0).clone(), 2usize));
            buf.push(((** x1).clone(), 3usize));
        },
        Num::NegNum(x0) => {
            buf.push(((** x0).clone(), 4usize));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_NUM_SCONG_NUM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_num(field_val, t);

rw_pred(lhs.clone(), match (lhs, vi) {
    (Pred::EqNum(_, x1), 0usize) => Pred::EqNum(Box::new(t.clone()), x1.clone()),
    (Pred::EqNum(x0, _), 1usize) => Pred::EqNum(x0.clone(), Box::new(t.clone())),
    (Pred::NeNum(_, x1), 2usize) => Pred::NeNum(Box::new(t.clone()), x1.clone()),
    (Pred::NeNum(x0, _), 3usize) => Pred::NeNum(x0.clone(), Box::new(t.clone())),
    _ => unreachable!(),
}) <--
    pred(lhs),
    for (field_val, vi) in { std::thread_local! { static POOL_PRED_SCONG_NUM : std::cell::Cell < Vec < (Num, usize) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_SCONG_NUM.with(| p | p.take()); buf.clear(); match lhs {
        Pred::EqNum(x0, x1) => {
            buf.push(((** x0).clone(), 0usize));
            buf.push(((** x1).clone(), 1usize));
        },
        Pred::NeNum(x0, x1) => {
            buf.push(((** x0).clone(), 2usize));
            buf.push(((** x1).clone(), 3usize));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_SCONG_NUM.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_num(field_val, t);

rw_pred(lhs.clone(), match (lhs, vi) {
    (Pred::AndPred(_, x1), 0usize) => Pred::AndPred(Box::new(t.clone()), x1.clone()),
    (Pred::AndPred(x0, _), 1usize) => Pred::AndPred(x0.clone(), Box::new(t.clone())),
    _ => unreachable!(),
}) <--
    pred(lhs),
    for (field_val, vi) in { std::thread_local! { static POOL_PRED_SCONG_PRED : std::cell::Cell < Vec < (Pred, usize) >> = const { std::cell::Cell::new(Vec::new()) }; } let mut buf = POOL_PRED_SCONG_PRED.with(| p | p.take()); buf.clear(); match lhs {
        Pred::AndPred(x0, x1) => {
            buf.push(((** x0).clone(), 0usize));
            buf.push(((** x1).clone(), 1usize));
        },
        _ => {},
    } let iter_buf = std::mem::take(& mut buf); POOL_PRED_SCONG_PRED.with(| p | p.set(buf)); iter_buf }.into_iter(),
    rw_pred(field_val, t);

rw_num(a.clone(), c.clone()) <--
    eq_num(a, b),
    rw_num(b.clone(), c);

rw_pred(a.clone(), c.clone()) <--
    eq_pred(a, b),
    rw_pred(b.clone(), c);

rw_expr(a.clone(), c.clone()) <--
    eq_expr(a, b),
    rw_expr(b.clone(), c);

}
