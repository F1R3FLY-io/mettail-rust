// AUTO-GENERATED by language! macro for RhoCalc (rewrite) — do not edit
// Regenerated on each compilation of the language definition.
// Run with: cargo test -p mettail-languages

#![allow(unused_imports, dead_code)]

use mettail_languages::rhocalc::*;
use mettail_runtime::Language;
use mettail_runtime::BehavioralPred;

// ═══════════════════════════════════════════════════════════
// Equation tests (one per equation)
// ═══════════════════════════════════════════════════════════

#[test]
fn equation_rhocalc_quotedrop() {
    let _lang = RhoCalcLanguage;
    let meta = _lang.metadata();
    let equations = meta.equations();
    // Verify equation QuoteDrop (index 0) exists in metadata
    assert!(
        equations.len() > 0,
        "Expected at least 1 equations in metadata, found {}",
        equations.len()
    );
    let eq = &equations[0];
    assert!(!eq.lhs.is_empty(), "Equation QuoteDrop LHS should be non-empty");
    assert!(!eq.rhs.is_empty(), "Equation QuoteDrop RHS should be non-empty");
}

#[test]
fn equation_rhocalc_execeq() {
    let _lang = RhoCalcLanguage;
    let meta = _lang.metadata();
    let equations = meta.equations();
    // Verify equation ExecEq (index 1) exists in metadata
    assert!(
        equations.len() > 1,
        "Expected at least 2 equations in metadata, found {}",
        equations.len()
    );
    let eq = &equations[1];
    assert!(!eq.lhs.is_empty(), "Equation ExecEq LHS should be non-empty");
    assert!(!eq.rhs.is_empty(), "Equation ExecEq RHS should be non-empty");
}

// Equation Extrude has freshness/complex conditions
#[test]
fn equation_rhocalc_extrude() {
    let _lang = RhoCalcLanguage;
    // This equation requires freshness conditions or complex premises
    // that cannot be instantiated statically.
}

// ═══════════════════════════════════════════════════════════
// Rewrite tests (one per rewrite rule)
// ═══════════════════════════════════════════════════════════

#[test]
fn rewrite_rhocalc_comm() {
    let _lang = RhoCalcLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite Comm (index 0) exists in metadata
    assert!(
        rewrites.len() > 0,
        "Expected at least 1 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[0];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("Comm"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite Comm LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite Comm RHS should be non-empty");
}

#[test]
fn rewrite_rhocalc_exec() {
    let _lang = RhoCalcLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite Exec (index 1) exists in metadata
    assert!(
        rewrites.len() > 1,
        "Expected at least 2 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[1];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("Exec"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite Exec LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite Exec RHS should be non-empty");
}

// Concrete execution test: run "* ( 0 )" through SimulationRunner,
// assert it reaches NormalForm (exercises rewrite rule Exec).
#[test]
fn rewrite_rhocalc_exec_exec() {
    use mettail_simulation::runner::{SimulationConfig, SimulationRunner};
    use mettail_simulation::trace::TraceOutcome;
    let lang = RhoCalcLanguage;
    let lang_ref: &dyn mettail_runtime::Language = &lang;
    let config = SimulationConfig {
        max_steps: 100,
        track_morphology: false,
        ..SimulationConfig::default()
    };
    let runner = SimulationRunner::new(lang_ref, config);
    mettail_runtime::clear_var_cache();
    let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        runner.run_to_normal_form("* ( 0 )")
    }));
    if let Ok(Ok(trace)) = result {
        assert!(
            matches!(trace.outcome, TraceOutcome::NormalForm { .. }),
            "Rewrite rule Exec: input '* ( 0 )' did not reach NF: {:?}",
            trace.outcome,
        );
    }
    // Panics (e.g., division by zero in native eval) are tolerated.
}

// Rewrite test for ParCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_parcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NewCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_newcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_addcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_addcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_subcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_subcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_mulcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_mulcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_divcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_divcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ModCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_modcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ModCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_modcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NegIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_negintcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NegProcCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_negproccong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_bitandcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_bitandcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_bitorcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_bitorcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitNotCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_bitnotcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_eqcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_eqcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_necongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_necongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_gtcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_gtcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_ltcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_ltcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_gteqcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_gteqcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_lteqcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_lteqcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NotCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_notcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AndCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_andcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AndCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_andcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for OrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_orcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for OrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_orcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LenCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_lencong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ConcatListCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_concatlistcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ConcatListCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_concatlistcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ElemListCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_elemlistcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ElemListCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_elemlistcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DeleteListCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_deletelistcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DeleteListCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_deletelistcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for UnionBagCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_unionbagcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for UnionBagCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_unionbagcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for RemoveBagCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_removebagcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for RemoveBagCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_removebagcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DiffBagCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_diffbagcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DiffBagCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_diffbagcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CountBagCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_countbagcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CountBagCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_countbagcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GetMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_getmapcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GetMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_getmapcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PutMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_putmapcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PutMapCongKey skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_putmapcongkey() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PutMapCongVal skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_putmapcongval() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DeleteMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_deletemapcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DeleteMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_deletemapcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MergeMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_mergemapcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MergeMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_mergemapcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for HasMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_hasmapcongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for HasMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_hasmapcongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for KeysMapCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_keysmapcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ValuesMapCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_valuesmapcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CastMapCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_castmapcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CastIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_castintcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CastUInt32Cong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_castuint32cong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CastBigIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_castbigintcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CastBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_castbigratcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CastFixedCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_castfixedcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FractionProcCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_fractionproccongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FractionProcCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_fractionproccongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntBinProcCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_intbinproccongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntBinProcCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_intbinproccongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for UIntBinProcCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_uintbinproccongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for UIntBinProcCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_uintbinproccongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatBinProcCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_floatbinproccongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatBinProcCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_floatbinproccongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FixedBinProcCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_fixedbinproccongl() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FixedBinProcCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_fixedbinproccongr() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BigintCastProcCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_bigintcastproccong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BigratCastProcCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_bigratcastproccong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ToBoolCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_toboolcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ToStrCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_rhocalc_tostrcong() {
    let _lang = RhoCalcLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

