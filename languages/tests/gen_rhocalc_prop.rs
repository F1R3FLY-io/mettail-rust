// AUTO-GENERATED by language! macro for RhoCalc (prop) — do not edit
// Regenerated on each compilation of the language definition.
// Run with: cargo test -p mettail-languages

#![allow(unused_imports, dead_code)]

use mettail_languages::rhocalc::*;
use mettail_runtime::BehavioralPred;
use mettail_runtime::Language;

// ═══════════════════════════════════════════════════════════
// Proptest strategies + property tests (tape-based)
// ═══════════════════════════════════════════════════════════

use proptest::prelude::*;
use proptest::strategy::BoxedStrategy;

/// Heterogeneous term wrapper for the tape-based builder.
#[allow(dead_code)]
#[derive(Clone)]
enum AnyTerm {
    WrapProc(Proc),
    WrapName(Name),
    WrapInputBind(InputBind),
    WrapForRow(ForRow),
    WrapInt(Int),
    WrapUInt32(UInt32),
    WrapBigInt(BigInt),
    WrapBigRat(BigRat),
    WrapFixed(Fixed),
    WrapFloat(Float),
    WrapBool(Bool),
    WrapStr(Str),
    WrapBytes(Bytes),
    WrapList(List),
    WrapBag(Bag),
    WrapMap(Map),
    WrapSet(Set),
    WrapPathmap(Pathmap),
    WrapReadZipper(ReadZipper),
    WrapWriteZipper(WriteZipper),
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_proc(self) -> Proc {
        match self {
            AnyTerm::WrapProc(v) => v,
            _ => panic!("AnyTerm::unwrap_proc: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_name(self) -> Name {
        match self {
            AnyTerm::WrapName(v) => v,
            _ => panic!("AnyTerm::unwrap_name: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_inputbind(self) -> InputBind {
        match self {
            AnyTerm::WrapInputBind(v) => v,
            _ => panic!("AnyTerm::unwrap_inputbind: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_forrow(self) -> ForRow {
        match self {
            AnyTerm::WrapForRow(v) => v,
            _ => panic!("AnyTerm::unwrap_forrow: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_int(self) -> Int {
        match self {
            AnyTerm::WrapInt(v) => v,
            _ => panic!("AnyTerm::unwrap_int: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_uint32(self) -> UInt32 {
        match self {
            AnyTerm::WrapUInt32(v) => v,
            _ => panic!("AnyTerm::unwrap_uint32: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_bigint(self) -> BigInt {
        match self {
            AnyTerm::WrapBigInt(v) => v,
            _ => panic!("AnyTerm::unwrap_bigint: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_bigrat(self) -> BigRat {
        match self {
            AnyTerm::WrapBigRat(v) => v,
            _ => panic!("AnyTerm::unwrap_bigrat: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_fixed(self) -> Fixed {
        match self {
            AnyTerm::WrapFixed(v) => v,
            _ => panic!("AnyTerm::unwrap_fixed: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_float(self) -> Float {
        match self {
            AnyTerm::WrapFloat(v) => v,
            _ => panic!("AnyTerm::unwrap_float: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_bool(self) -> Bool {
        match self {
            AnyTerm::WrapBool(v) => v,
            _ => panic!("AnyTerm::unwrap_bool: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_str(self) -> Str {
        match self {
            AnyTerm::WrapStr(v) => v,
            _ => panic!("AnyTerm::unwrap_str: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_bytes(self) -> Bytes {
        match self {
            AnyTerm::WrapBytes(v) => v,
            _ => panic!("AnyTerm::unwrap_bytes: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_list(self) -> List {
        match self {
            AnyTerm::WrapList(v) => v,
            _ => panic!("AnyTerm::unwrap_list: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_bag(self) -> Bag {
        match self {
            AnyTerm::WrapBag(v) => v,
            _ => panic!("AnyTerm::unwrap_bag: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_map(self) -> Map {
        match self {
            AnyTerm::WrapMap(v) => v,
            _ => panic!("AnyTerm::unwrap_map: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_set(self) -> Set {
        match self {
            AnyTerm::WrapSet(v) => v,
            _ => panic!("AnyTerm::unwrap_set: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_pathmap(self) -> Pathmap {
        match self {
            AnyTerm::WrapPathmap(v) => v,
            _ => panic!("AnyTerm::unwrap_pathmap: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_readzipper(self) -> ReadZipper {
        match self {
            AnyTerm::WrapReadZipper(v) => v,
            _ => panic!("AnyTerm::unwrap_readzipper: wrong variant"),
        }
    }
}

impl AnyTerm {
    #[allow(dead_code)]
    fn unwrap_writezipper(self) -> WriteZipper {
        match self {
            AnyTerm::WrapWriteZipper(v) => v,
            _ => panic!("AnyTerm::unwrap_writezipper: wrong variant"),
        }
    }
}

/// Work item for the tape-based iterative term builder.
#[allow(dead_code)]
enum BuildTask {
    /// Build a Proc term at the given depth, storing result in the given slot.
    BuildProc { depth: u32, slot: usize },
    /// Build a Name term at the given depth, storing result in the given slot.
    BuildName { depth: u32, slot: usize },
    /// Build a InputBind term at the given depth, storing result in the given slot.
    BuildInputBind { depth: u32, slot: usize },
    /// Build a ForRow term at the given depth, storing result in the given slot.
    BuildForRow { depth: u32, slot: usize },
    /// Build a Int term at the given depth, storing result in the given slot.
    BuildInt { depth: u32, slot: usize },
    /// Build a UInt32 term at the given depth, storing result in the given slot.
    BuildUInt32 { depth: u32, slot: usize },
    /// Build a BigInt term at the given depth, storing result in the given slot.
    BuildBigInt { depth: u32, slot: usize },
    /// Build a BigRat term at the given depth, storing result in the given slot.
    BuildBigRat { depth: u32, slot: usize },
    /// Build a Fixed term at the given depth, storing result in the given slot.
    BuildFixed { depth: u32, slot: usize },
    /// Build a Float term at the given depth, storing result in the given slot.
    BuildFloat { depth: u32, slot: usize },
    /// Build a Bool term at the given depth, storing result in the given slot.
    BuildBool { depth: u32, slot: usize },
    /// Build a Str term at the given depth, storing result in the given slot.
    BuildStr { depth: u32, slot: usize },
    /// Build a Bytes term at the given depth, storing result in the given slot.
    BuildBytes { depth: u32, slot: usize },
    /// Build a List term at the given depth, storing result in the given slot.
    BuildList { depth: u32, slot: usize },
    /// Build a Bag term at the given depth, storing result in the given slot.
    BuildBag { depth: u32, slot: usize },
    /// Build a Map term at the given depth, storing result in the given slot.
    BuildMap { depth: u32, slot: usize },
    /// Build a Set term at the given depth, storing result in the given slot.
    BuildSet { depth: u32, slot: usize },
    /// Build a Pathmap term at the given depth, storing result in the given slot.
    BuildPathmap { depth: u32, slot: usize },
    /// Build a ReadZipper term at the given depth, storing result in the given slot.
    BuildReadZipper { depth: u32, slot: usize },
    /// Build a WriteZipper term at the given depth, storing result in the given slot.
    BuildWriteZipper { depth: u32, slot: usize },
}

/// Helper to consume bytes from the tape.
#[allow(dead_code)]
struct TapeReader<'a> {
    tape: &'a [u8],
    pos: usize,
}

#[allow(dead_code)]
impl<'a> TapeReader<'a> {
    fn new(tape: &'a [u8]) -> Self {
        TapeReader { tape, pos: 0 }
    }

    /// Read the next byte. On exhaustion return 0 — do NOT wrap. Byte 0 maps
    /// to constructor choice `0 % N == 0`, which is ALWAYS a leaf: every
    /// `build_*_from_tape` match emits its leaf arms before its recursive arms
    /// (see `classify_variants`), so choice 0 selects `leaves[0]` and the
    /// recursion bottoms out to the simplest term — the documented intent
    /// "shorter tapes = simpler terms". The old `pos % len` wrap RE-READ the
    /// same recursive-constructor byte at every level, so a 1-byte tape
    /// `[0x38]` built a COMPLETE binary tree down to max_depth (0x38 ->
    /// `MulBigRat` at all internal nodes -> `error*error*...`), which drove
    /// BigRat::parse into the exponential cross-category axis (~9s).
    fn next_byte(&mut self) -> u8 {
        if self.pos >= self.tape.len() {
            return 0;
        }
        let b = self.tape[self.pos];
        self.pos += 1;
        b
    }

    /// Read a u32 from 4 bytes (little-endian); reads 0 past end of tape.
    fn next_u32(&mut self) -> u32 {
        let b0 = self.next_byte() as u32;
        let b1 = self.next_byte() as u32;
        let b2 = self.next_byte() as u32;
        let b3 = self.next_byte() as u32;
        b0 | (b1 << 8) | (b2 << 16) | (b3 << 24)
    }

    /// Read an i32 from tape bytes.
    ///
    /// Returns the full i32 range. Overflow in `![a + b]` native eval paths is
    /// handled by the `SafeArith` trait and the `rust_code_rewrite` pass in the
    /// `language!` macro: arithmetic operators are rewritten to `safe_add`,
    /// `safe_mul`, etc., which return `Option<T>` on overflow rather than
    /// panicking. This removes the need for a restricted range here.
    fn next_i32(&mut self) -> i32 {
        self.next_u32() as i32
    }

    /// Read an i64 from tape bytes.
    fn next_i64(&mut self) -> i64 {
        let lo = self.next_u32() as i64;
        let hi = self.next_u32() as i64;
        lo | (hi << 32)
    }

    /// Read an f64 from tape bytes.
    fn next_f64(&mut self) -> f64 {
        let bits = self.next_i64() as u64;
        let val = f64::from_bits(bits);
        // Avoid NaN/Inf which cause issues with Eq/Ord
        if val.is_nan() || val.is_infinite() {
            0.0
        } else {
            val
        }
    }

    /// Read an f32 from tape bytes.
    fn next_f32(&mut self) -> f32 {
        let bits = self.next_u32();
        let val = f32::from_bits(bits);
        if val.is_nan() || val.is_infinite() {
            0.0f32
        } else {
            val
        }
    }

    /// Read a bool from tape.
    fn next_bool(&mut self) -> bool {
        self.next_byte() & 1 == 1
    }

    /// Read a short string from tape.
    fn next_string(&mut self) -> String {
        let len = (self.next_byte() % 8) as usize;
        (0..len)
            .map(|_| {
                let b = self.next_byte();
                // Map to printable ASCII range 'a'-'z'
                (b'a' + (b % 26)) as char
            })
            .collect()
    }
}

/// Build a `Proc` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_proc_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Proc {
    if depth == 0 {
        let choice = (reader.next_byte() as usize) % 7;
        let result = match choice {
            0 => AnyTerm::WrapProc(Proc::PZero),
            1 => AnyTerm::WrapProc(Proc::POutputNilEmpty),
            2 => AnyTerm::WrapProc(Proc::PPersistOutputNilEmpty),
            3 => AnyTerm::WrapProc(Proc::Err),
            4 => AnyTerm::WrapProc(Proc::MapEmpty),
            5 => AnyTerm::WrapProc(Proc::PathmapEmpty),
            _ => {
                let _ = reader.next_byte(); // consume tape byte for replay determinism
                AnyTerm::WrapProc(Proc::PVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
                    mettail_runtime::get_or_create_var("a"),
                ))))
            },
        };
        return result.unwrap_proc();
    }

    let choice = (reader.next_byte() as usize) % 115;
    let child_depth = depth - 1;
    match choice {
        0 => AnyTerm::WrapProc(Proc::PZero).unwrap_proc(),
        1 => AnyTerm::WrapProc(Proc::POutputNilEmpty).unwrap_proc(),
        2 => AnyTerm::WrapProc(Proc::PPersistOutputNilEmpty).unwrap_proc(),
        3 => AnyTerm::WrapProc(Proc::Err).unwrap_proc(),
        4 => AnyTerm::WrapProc(Proc::MapEmpty).unwrap_proc(),
        5 => AnyTerm::WrapProc(Proc::PathmapEmpty).unwrap_proc(),
        6 => {
            let _ = reader.next_byte(); // consume tape byte for replay determinism
            AnyTerm::WrapProc(Proc::PVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
                mettail_runtime::get_or_create_var("a"),
            ))))
        }
        .unwrap_proc(),
        7 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            Proc::PDrop(f0)
        },
        8 => {
            let num_elems = (reader.next_byte() % 4) as usize;
            let mut bag = mettail_runtime::HashBag::new();
            for _ in 0..num_elems {
                bag.insert(build_proc_from_tape(reader, child_depth));
            }
            Proc::PPar(bag)
        },
        9 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::POutput(f0, f1)
        },
        10 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PPersistOutput(f0, f1)
        },
        11 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            Proc::POutputEmpty(f0)
        },
        12 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            Proc::PPersistOutputEmpty(f0)
        },
        13 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let num_elems_2 = (reader.next_byte() % 4) as usize;
            let coll_2: Vec<_> = (0..num_elems_2)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            Proc::POutput2Plus(f0, f1, coll_2)
        },
        14 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let num_elems_2 = (reader.next_byte() % 4) as usize;
            let coll_2: Vec<_> = (0..num_elems_2)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            Proc::PPersistOutput2Plus(f0, f1, coll_2)
        },
        15 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::POutputNil(f0)
        },
        16 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PPersistOutputNil(f0)
        },
        17 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::POutputQuoted(f0, f1)
        },
        18 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::POutputShort(f0, f1)
        },
        19 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PPersistOutputShort(f0, f1)
        },
        20 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            Proc::POutputQuotedEmpty(f0)
        },
        21 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::POutputShortEmpty(f0)
        },
        22 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PPersistOutputShortEmpty(f0)
        },
        23 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let num_elems_1 = (reader.next_byte() % 4) as usize;
            let coll_1: Vec<_> = (0..num_elems_1)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            Proc::POutputNil2Plus(f0, coll_1)
        },
        24 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let num_elems_1 = (reader.next_byte() % 4) as usize;
            let coll_1: Vec<_> = (0..num_elems_1)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            Proc::PPersistOutputNil2Plus(f0, coll_1)
        },
        25 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let num_elems_2 = (reader.next_byte() % 4) as usize;
            let coll_2: Vec<_> = (0..num_elems_2)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            Proc::POutputQuoted2Plus(f0, f1, coll_2)
        },
        26 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let num_elems_2 = (reader.next_byte() % 4) as usize;
            let coll_2: Vec<_> = (0..num_elems_2)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            Proc::POutputShort2Plus(f0, f1, coll_2)
        },
        27 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let num_elems_2 = (reader.next_byte() % 4) as usize;
            let coll_2: Vec<_> = (0..num_elems_2)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            Proc::PPersistOutputShort2Plus(f0, f1, coll_2)
        },
        28 => {
            let num_elems_0 = (reader.next_byte() % 4) as usize;
            let coll_0: Vec<_> = (0..num_elems_0)
                .map(|_| build_forrow_from_tape(reader, child_depth))
                .collect();
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PForUser(coll_0, f1)
        },
        29 => {
            let num_binders = ((reader.next_byte() % 3) + 1) as usize;
            let binders: Vec<mettail_runtime::Binder<String>> = (0..num_binders)
                .map(|j| {
                    let name = format!("a{}", j);
                    mettail_runtime::Binder(mettail_runtime::get_or_create_var(&name))
                })
                .collect();
            let body = build_proc_from_tape(reader, child_depth);
            let scope = mettail_runtime::Scope::new(binders, std::sync::Arc::new(body));
            Proc::PNew(scope)
        },
        30 => {
            let f0 = std::sync::Arc::new(build_bigrat_from_tape(reader, child_depth));
            Proc::CastBigRat(f0)
        },
        31 => {
            let f0 = std::sync::Arc::new(build_fixed_from_tape(reader, child_depth));
            Proc::CastFixed(f0)
        },
        32 => {
            let f0 = std::sync::Arc::new(build_float_from_tape(reader, child_depth));
            Proc::CastFloat(f0)
        },
        33 => {
            let f0 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            Proc::CastInt(f0)
        },
        34 => {
            let f0 = std::sync::Arc::new(build_bigint_from_tape(reader, child_depth));
            Proc::CastBigInt(f0)
        },
        35 => {
            let f0 = std::sync::Arc::new(build_uint32_from_tape(reader, child_depth));
            Proc::CastUInt32(f0)
        },
        36 => {
            let f0 = std::sync::Arc::new(build_bool_from_tape(reader, child_depth));
            Proc::CastBool(f0)
        },
        37 => {
            let f0 = std::sync::Arc::new(build_str_from_tape(reader, child_depth));
            Proc::CastStr(f0)
        },
        38 => {
            let f0 = std::sync::Arc::new(build_bytes_from_tape(reader, child_depth));
            Proc::CastBytes(f0)
        },
        39 => {
            let f0 = std::sync::Arc::new(build_list_from_tape(reader, child_depth));
            Proc::CastList(f0)
        },
        40 => {
            let f0 = std::sync::Arc::new(build_bag_from_tape(reader, child_depth));
            Proc::CastBag(f0)
        },
        41 => {
            let f0 = std::sync::Arc::new(build_map_from_tape(reader, child_depth));
            Proc::CastMap(f0)
        },
        42 => {
            let f0 = std::sync::Arc::new(build_set_from_tape(reader, child_depth));
            Proc::CastSet(f0)
        },
        43 => {
            let f0 = std::sync::Arc::new(build_pathmap_from_tape(reader, child_depth));
            Proc::CastPathmap(f0)
        },
        44 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            Proc::IntBinProc(f0, f1)
        },
        45 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            Proc::UIntBinProc(f0, f1)
        },
        46 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            Proc::FloatBinProc(f0, f1)
        },
        47 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            Proc::FixedBinProc(f0, f1)
        },
        48 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BigintCastProc(f0)
        },
        49 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BigratCastProc(f0)
        },
        50 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::FractionProc(f0, f1)
        },
        51 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PParInfix(f0, f1)
        },
        52 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Implies(f0, f1)
        },
        53 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Or(f0, f1)
        },
        54 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::And(f0, f1)
        },
        55 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BitOr(f0, f1)
        },
        56 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BitAnd(f0, f1)
        },
        57 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BitNot(f0)
        },
        58 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Matches(f0, f1)
        },
        59 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::SpatialPPar(f0, f1)
        },
        60 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Eq(f0, f1)
        },
        61 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Ne(f0, f1)
        },
        62 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Gt(f0, f1)
        },
        63 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Lt(f0, f1)
        },
        64 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::GtEq(f0, f1)
        },
        65 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::LtEq(f0, f1)
        },
        66 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Add(f0, f1)
        },
        67 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Sub(f0, f1)
        },
        68 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Mul(f0, f1)
        },
        69 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Div(f0, f1)
        },
        70 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Mod(f0, f1)
        },
        71 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::NegProc(f0)
        },
        72 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MGet(f0, f1)
        },
        73 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f2 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MSet(f0, f1, f2)
        },
        74 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MContains(f0, f1)
        },
        75 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MDelete(f0, f1)
        },
        76 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MUnion(f0, f1)
        },
        77 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MSize(f0)
        },
        78 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MToByteArray(f0)
        },
        79 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MKeys(f0)
        },
        80 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::MValues(f0)
        },
        81 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::LLength(f0)
        },
        82 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::LNth(f0, f1)
        },
        83 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::LConcat(f0, f1)
        },
        84 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BCount(f0, f1)
        },
        85 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BDiff(f0, f1)
        },
        86 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::BRemove(f0, f1)
        },
        87 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PRestrict(f0, f1)
        },
        88 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PSubtract(f0, f1)
        },
        89 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PMeet(f0, f1)
        },
        90 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PGetSubtrie(f0)
        },
        91 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PGetSubtrieAt(f0, f1)
        },
        92 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PReadZipper(f0)
        },
        93 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PReadZipperAt(f0, f1)
        },
        94 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PWriteZipper(f0)
        },
        95 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::PWriteZipperAt(f0, f1)
        },
        96 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZGetLeaf(f0)
        },
        97 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZDescendTo(f0, f1)
        },
        98 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZChildCount(f0)
        },
        99 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZDescendFirst(f0)
        },
        100 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZToNextSibling(f0)
        },
        101 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZToPrevSibling(f0)
        },
        102 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZDescendIndexedBranch(f0, f1)
        },
        103 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZAscendOne(f0)
        },
        104 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::RZAscend(f0, f1)
        },
        105 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f2 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::WZSetLeaf(f0, f1, f2)
        },
        106 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::WZSetSubtrie(f0, f1)
        },
        107 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::WZRemoveLeaf(f0)
        },
        108 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::WZRemoveBranches(f0)
        },
        109 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::WZGraft(f0, f1)
        },
        110 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::WZJoinInto(f0, f1)
        },
        111 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::SAdd(f0, f1)
        },
        112 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::Not(f0)
        },
        113 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::ToBool(f0)
        },
        _ => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Proc::ToStr(f0)
        },
    }
}

/// Build a `Name` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_name_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Name {
    if depth == 0 {
        let choice = (reader.next_byte() as usize) % 2;
        let result = match choice {
            0 => AnyTerm::WrapName(Name::NQuoteNil),
            _ => {
                let _ = reader.next_byte(); // consume tape byte for replay determinism
                AnyTerm::WrapName(Name::NVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
                    mettail_runtime::get_or_create_var("a"),
                ))))
            },
        };
        return result.unwrap_name();
    }

    let choice = (reader.next_byte() as usize) % 5;
    let child_depth = depth - 1;
    match choice {
        0 => AnyTerm::WrapName(Name::NQuoteNil).unwrap_name(),
        1 => {
            let _ = reader.next_byte(); // consume tape byte for replay determinism
            AnyTerm::WrapName(Name::NVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
                mettail_runtime::get_or_create_var("a"),
            ))))
        }
        .unwrap_name(),
        2 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Name::NQuote(f0)
        },
        3 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            Name::NQuoteShort(f0)
        },
        _ => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            Name::NParen(f0)
        },
    }
}

/// Build a `InputBind` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_inputbind_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> InputBind {
    if depth == 0 {
        let result = {
            let _ = reader.next_byte(); // consume tape byte for replay determinism
            AnyTerm::WrapInputBind(InputBind::IVar(mettail_runtime::OrdVar(
                mettail_runtime::Var::Free(mettail_runtime::get_or_create_var("a")),
            )))
        };
        return result.unwrap_inputbind();
    }

    let choice = (reader.next_byte() as usize) % 12;
    let child_depth = depth - 1;
    match choice {
        0 => {
            let _ = reader.next_byte(); // consume tape byte for replay determinism
            AnyTerm::WrapInputBind(InputBind::IVar(mettail_runtime::OrdVar(
                mettail_runtime::Var::Free(mettail_runtime::get_or_create_var("a")),
            )))
        }
        .unwrap_inputbind(),
        1 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let num_elems_2 = (reader.next_byte() % 4) as usize;
            let coll_2: Vec<_> = (0..num_elems_2)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            InputBind::InputBindQuery(f0, f1, coll_2)
        },
        2 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let num_elems_1 = (reader.next_byte() % 4) as usize;
            let coll_1: Vec<_> = (0..num_elems_1)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            InputBind::InputBindEmptyQuery(f0, coll_1)
        },
        3 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let num_elems_2 = (reader.next_byte() % 4) as usize;
            let coll_2: Vec<_> = (0..num_elems_2)
                .map(|_| build_proc_from_tape(reader, child_depth))
                .collect();
            InputBind::InputBindQuotedQuery(f0, f1, coll_2)
        },
        4 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBindQuoted(f0, f1)
        },
        5 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let num_elems_1 = (reader.next_byte() % 4) as usize;
            let coll_1: Vec<_> = (0..num_elems_1)
                .map(|_| build_name_from_tape(reader, child_depth))
                .collect();
            let f2 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBindPolyadic(f0, coll_1, f2)
        },
        6 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let num_elems_1 = (reader.next_byte() % 4) as usize;
            let coll_1: Vec<_> = (0..num_elems_1)
                .map(|_| build_name_from_tape(reader, child_depth))
                .collect();
            let f2 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBindPersistentPolyadic(f0, coll_1, f2)
        },
        7 => {
            let f0 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBindQuotedPersistent(f0, f1)
        },
        8 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBind(f0, f1)
        },
        9 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBindPersistent(f0, f1)
        },
        10 => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBindEmpty(f0)
        },
        _ => {
            let f0 = std::sync::Arc::new(build_name_from_tape(reader, child_depth));
            InputBind::InputBindEmptyPersistent(f0)
        },
    }
}

/// Build a `ForRow` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_forrow_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> ForRow {
    if depth == 0 {
        let result = {
            let _ = reader.next_byte(); // consume tape byte for replay determinism
            AnyTerm::WrapForRow(ForRow::FVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
                mettail_runtime::get_or_create_var("a"),
            ))))
        };
        return result.unwrap_forrow();
    }

    let choice = (reader.next_byte() as usize) % 5;
    let child_depth = depth - 1;
    match choice {
        0 => {
            let _ = reader.next_byte(); // consume tape byte for replay determinism
            AnyTerm::WrapForRow(ForRow::FVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
                mettail_runtime::get_or_create_var("a"),
            ))))
        }
        .unwrap_forrow(),
        1 => {
            let f0 = std::sync::Arc::new(build_inputbind_from_tape(reader, child_depth));
            let num_elems_1 = (reader.next_byte() % 4) as usize;
            let coll_1: Vec<_> = (0..num_elems_1)
                .map(|_| build_inputbind_from_tape(reader, child_depth))
                .collect();
            let f2 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            ForRow::ForRowWhere(f0, coll_1, f2)
        },
        2 => {
            let f0 = std::sync::Arc::new(build_inputbind_from_tape(reader, child_depth));
            let num_elems_1 = (reader.next_byte() % 4) as usize;
            let coll_1: Vec<_> = (0..num_elems_1)
                .map(|_| build_inputbind_from_tape(reader, child_depth))
                .collect();
            ForRow::ForRowNoWhere(f0, coll_1)
        },
        3 => {
            let f0 = std::sync::Arc::new(build_inputbind_from_tape(reader, child_depth));
            let f1 = std::sync::Arc::new(build_proc_from_tape(reader, child_depth));
            ForRow::ForRowSingleWhere(f0, f1)
        },
        _ => {
            let f0 = std::sync::Arc::new(build_inputbind_from_tape(reader, child_depth));
            ForRow::ForRowSingleNoWhere(f0)
        },
    }
}

/// Build a `Int` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_int_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Int {
    if depth == 0 {
        let result =
            AnyTerm::WrapInt(Int::NumLit((reader.next_i64().unsigned_abs() as i64) & i64::MAX));
        return result.unwrap_int();
    }

    let choice = (reader.next_byte() as usize) % 4;
    let child_depth = depth - 1;
    match choice {
        0 => AnyTerm::WrapInt(Int::NumLit((reader.next_i64().unsigned_abs() as i64) & i64::MAX))
            .unwrap_int(),
        1 => {
            let f0 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            Int::NegInt(f0)
        },
        2 => {
            let f0 = std::sync::Arc::new(build_bool_from_tape(reader, child_depth));
            Int::BoolToInt(f0)
        },
        _ => {
            let f0 = std::sync::Arc::new(build_uint32_from_tape(reader, child_depth));
            Int::UInt32ToInt(f0)
        },
    }
}

/// Build a `UInt32` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_uint32_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> UInt32 {
    if depth == 0 {
        let result = AnyTerm::WrapUInt32(UInt32::NumLit(reader.next_u32()));
        return result.unwrap_uint32();
    }

    let choice = (reader.next_byte() as usize) % 2;
    let child_depth = depth - 1;
    match choice {
        0 => AnyTerm::WrapUInt32(UInt32::NumLit(reader.next_u32())).unwrap_uint32(),
        _ => {
            let f0 = std::sync::Arc::new(build_bool_from_tape(reader, child_depth));
            UInt32::BoolToUInt32(f0)
        },
    }
}

/// Build a `BigInt` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_bigint_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> BigInt {
    if depth == 0 {
        let result = AnyTerm::WrapBigInt(BigInt::NumLit(mettail_runtime::CanonicalBigInt::from(
            num_bigint::BigInt::from(reader.next_i32()),
        )));
        return result.unwrap_bigint();
    }

    let choice = (reader.next_byte() as usize) % 4;
    let child_depth = depth - 1;
    match choice {
        0 => AnyTerm::WrapBigInt(BigInt::NumLit(mettail_runtime::CanonicalBigInt::from(
            num_bigint::BigInt::from(reader.next_i32()),
        )))
        .unwrap_bigint(),
        1 => {
            let f0 = std::sync::Arc::new(build_bool_from_tape(reader, child_depth));
            BigInt::BoolToBigInt(f0)
        },
        2 => {
            let f0 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            BigInt::IntToBigInt(f0)
        },
        _ => {
            let f0 = std::sync::Arc::new(build_uint32_from_tape(reader, child_depth));
            BigInt::UInt32ToBigInt(f0)
        },
    }
}

/// Build a `BigRat` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_bigrat_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> BigRat {
    if depth == 0 {
        let result = AnyTerm::WrapBigRat(BigRat::RatLit(mettail_runtime::CanonicalBigRat::from(
            num_rational::Ratio::from_integer(num_bigint::BigInt::from(reader.next_i32())),
        )));
        return result.unwrap_bigrat();
    }

    let choice = (reader.next_byte() as usize) % 7;
    let child_depth = depth - 1;
    match choice {
        0 => AnyTerm::WrapBigRat(BigRat::RatLit(mettail_runtime::CanonicalBigRat::from(
            num_rational::Ratio::from_integer(num_bigint::BigInt::from(reader.next_i32())),
        )))
        .unwrap_bigrat(),
        1 => {
            let f0 = std::sync::Arc::new(build_bool_from_tape(reader, child_depth));
            BigRat::BoolToBigRat(f0)
        },
        2 => {
            let f0 = std::sync::Arc::new(build_int_from_tape(reader, child_depth));
            BigRat::IntToBigRat(f0)
        },
        3 => {
            let f0 = std::sync::Arc::new(build_uint32_from_tape(reader, child_depth));
            BigRat::UInt32ToBigRat(f0)
        },
        4 => {
            let f0 = std::sync::Arc::new(build_float_from_tape(reader, child_depth));
            BigRat::FloatToBigRat(f0)
        },
        5 => {
            let f0 = std::sync::Arc::new(build_bigint_from_tape(reader, child_depth));
            BigRat::BigIntToBigRat(f0)
        },
        _ => {
            let f0 = std::sync::Arc::new(build_fixed_from_tape(reader, child_depth));
            BigRat::FixedToBigRat(f0)
        },
    }
}

/// Build a `Fixed` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_fixed_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Fixed {
    if depth == 0 {
        let result =
            AnyTerm::WrapFixed(Fixed::FixedLit(mettail_runtime::CanonicalFixedPoint::new(
                num_bigint::BigInt::from(reader.next_i32()),
                0,
            )));
        return result.unwrap_fixed();
    }

    // No recursive constructors, fall back to leaf
    build_fixed_from_tape(reader, 0)
}

/// Build a `Float` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_float_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Float {
    if depth == 0 {
        let result = AnyTerm::WrapFloat(Float::FloatLit(mettail_runtime::CanonicalFloat64::from(
            reader.next_f64().abs(),
        )));
        return result.unwrap_float();
    }

    // No recursive constructors, fall back to leaf
    build_float_from_tape(reader, 0)
}

/// Build a `Bool` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_bool_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Bool {
    if depth == 0 {
        let result = AnyTerm::WrapBool(Bool::BoolLit(reader.next_bool()));
        return result.unwrap_bool();
    }

    // No recursive constructors, fall back to leaf
    build_bool_from_tape(reader, 0)
}

/// Build a `Str` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_str_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Str {
    if depth == 0 {
        let result = AnyTerm::WrapStr(Str::StringLit(reader.next_string()));
        return result.unwrap_str();
    }

    // No recursive constructors, fall back to leaf
    build_str_from_tape(reader, 0)
}

/// Build a `Bytes` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_bytes_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Bytes {
    if depth == 0 {
        let result = AnyTerm::WrapBytes(Bytes::StringLit(reader.next_string()));
        return result.unwrap_bytes();
    }

    // No recursive constructors, fall back to leaf
    build_bytes_from_tape(reader, 0)
}

/// Build a `List` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_list_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> List {
    if depth == 0 {
        let result = AnyTerm::WrapList(List::ListLit(Vec::new()));
        return result.unwrap_list();
    }

    // No recursive constructors, fall back to leaf
    build_list_from_tape(reader, 0)
}

/// Build a `Bag` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_bag_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Bag {
    if depth == 0 {
        let result = AnyTerm::WrapBag(Bag::BagLit(mettail_runtime::HashBag::new()));
        return result.unwrap_bag();
    }

    // No recursive constructors, fall back to leaf
    build_bag_from_tape(reader, 0)
}

/// Build a `Map` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_map_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Map {
    if depth == 0 {
        let result = AnyTerm::WrapMap(Map::MapLit(mettail_runtime::HashMapLit::new()));
        return result.unwrap_map();
    }

    // No recursive constructors, fall back to leaf
    build_map_from_tape(reader, 0)
}

/// Build a `Set` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_set_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Set {
    if depth == 0 {
        let result = AnyTerm::WrapSet(Set::SetLit(mettail_runtime::HashSetLit::new()));
        return result.unwrap_set();
    }

    // No recursive constructors, fall back to leaf
    build_set_from_tape(reader, 0)
}

/// Build a `Pathmap` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_pathmap_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> Pathmap {
    if depth == 0 {
        let result = AnyTerm::WrapPathmap(Pathmap::PathmapLit(mettail_runtime::PathMapLit::new()));
        return result.unwrap_pathmap();
    }

    // No recursive constructors, fall back to leaf
    build_pathmap_from_tape(reader, 0)
}

/// Build a `ReadZipper` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_readzipper_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> ReadZipper {
    if depth == 0 {
        let result = AnyTerm::WrapReadZipper(ReadZipper::Lit(::core::default::Default::default()));
        return result.unwrap_readzipper();
    }

    // No recursive constructors, fall back to leaf
    build_readzipper_from_tape(reader, 0)
}

/// Build a `WriteZipper` term from an instruction tape.
///
/// Consumes bytes from the tape to choose constructors.
/// At depth 0, only leaf constructors (nullary, literal, var) are chosen.
/// At depth > 0, recursive constructors are also available.
#[allow(dead_code, unused_variables, clippy::let_and_return)]
fn build_writezipper_from_tape(reader: &mut TapeReader<'_>, depth: u32) -> WriteZipper {
    if depth == 0 {
        let result =
            AnyTerm::WrapWriteZipper(WriteZipper::Lit(::core::default::Default::default()));
        return result.unwrap_writezipper();
    }

    // No recursive constructors, fall back to leaf
    build_writezipper_from_tape(reader, 0)
}

/// Generate an arbitrary `Proc` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_proc_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_proc(max_depth: u32) -> BoxedStrategy<Proc> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_proc_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Name` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_name_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_name(max_depth: u32) -> BoxedStrategy<Name> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_name_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `InputBind` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_inputbind_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_inputbind(max_depth: u32) -> BoxedStrategy<InputBind> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_inputbind_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `ForRow` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_forrow_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_forrow(max_depth: u32) -> BoxedStrategy<ForRow> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_forrow_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Int` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_int_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_int(max_depth: u32) -> BoxedStrategy<Int> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_int_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `UInt32` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_uint32_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_uint32(max_depth: u32) -> BoxedStrategy<UInt32> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_uint32_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `BigInt` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_bigint_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_bigint(max_depth: u32) -> BoxedStrategy<BigInt> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_bigint_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `BigRat` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_bigrat_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_bigrat(max_depth: u32) -> BoxedStrategy<BigRat> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_bigrat_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Fixed` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_fixed_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_fixed(max_depth: u32) -> BoxedStrategy<Fixed> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_fixed_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Float` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_float_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_float(max_depth: u32) -> BoxedStrategy<Float> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_float_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Bool` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_bool_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_bool(max_depth: u32) -> BoxedStrategy<Bool> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_bool_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Str` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_str_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_str(max_depth: u32) -> BoxedStrategy<Str> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_str_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Bytes` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_bytes_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_bytes(max_depth: u32) -> BoxedStrategy<Bytes> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_bytes_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `List` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_list_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_list(max_depth: u32) -> BoxedStrategy<List> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_list_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Bag` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_bag_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_bag(max_depth: u32) -> BoxedStrategy<Bag> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_bag_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Map` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_map_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_map(max_depth: u32) -> BoxedStrategy<Map> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_map_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Set` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_set_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_set(max_depth: u32) -> BoxedStrategy<Set> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_set_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `Pathmap` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_pathmap_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_pathmap(max_depth: u32) -> BoxedStrategy<Pathmap> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_pathmap_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `ReadZipper` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_readzipper_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_readzipper(max_depth: u32) -> BoxedStrategy<ReadZipper> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_readzipper_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

/// Generate an arbitrary `WriteZipper` term with bounded depth.
///
/// Uses a flat `Vec<u8>` tape interpreted by `build_writezipper_from_tape`.
/// Proptest shrinking produces shorter tapes = simpler terms.
#[allow(dead_code)]
fn arb_writezipper(max_depth: u32) -> BoxedStrategy<WriteZipper> {
    // Tape size scales with depth: deeper terms need more bytes
    let max_tape = (10 * (max_depth as usize + 1)).max(20);
    proptest::collection::vec(any::<u8>(), 1..max_tape)
        .prop_map(move |tape| {
            let mut reader = TapeReader::new(&tape);
            build_writezipper_from_tape(&mut reader, max_depth)
        })
        .boxed()
}

proptest! {
    #![proptest_config(ProptestConfig::with_cases(100))]

    #[test]
    fn proc_debug_does_not_panic(term in arb_proc(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn proc_display_does_not_panic(term in arb_proc(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn proc_clone_eq(term in arb_proc(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn proc_display_parse_roundtrip(term in arb_proc(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Proc::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_proc produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Proc::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn proc_strong_roundtrip_via_display(term in arb_proc(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Proc::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Proc::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip (display proxy): canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip (display proxy): canonical display not stable after double parse");
        }
    }

    #[test]
    fn proc_parse_determinism(term in arb_proc(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Proc::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Proc::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn name_debug_does_not_panic(term in arb_name(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn name_display_does_not_panic(term in arb_name(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn name_clone_eq(term in arb_name(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn name_display_parse_roundtrip(term in arb_name(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Name::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_name produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Name::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn name_strong_roundtrip(term in arb_name(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Name::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Name::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn name_parse_determinism(term in arb_name(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Name::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Name::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn inputbind_debug_does_not_panic(term in arb_inputbind(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn inputbind_display_does_not_panic(term in arb_inputbind(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn inputbind_clone_eq(term in arb_inputbind(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn inputbind_display_parse_roundtrip(term in arb_inputbind(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = InputBind::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_inputbind produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = InputBind::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn inputbind_strong_roundtrip(term in arb_inputbind(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = InputBind::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = InputBind::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn inputbind_parse_determinism(term in arb_inputbind(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = InputBind::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = InputBind::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn forrow_debug_does_not_panic(term in arb_forrow(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn forrow_display_does_not_panic(term in arb_forrow(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn forrow_clone_eq(term in arb_forrow(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn forrow_display_parse_roundtrip(term in arb_forrow(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = ForRow::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_forrow produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = ForRow::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn forrow_strong_roundtrip(term in arb_forrow(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = ForRow::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = ForRow::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn forrow_parse_determinism(term in arb_forrow(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = ForRow::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = ForRow::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn int_debug_does_not_panic(term in arb_int(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn int_display_does_not_panic(term in arb_int(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn int_clone_eq(term in arb_int(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn int_display_parse_roundtrip(term in arb_int(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Int::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_int produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Int::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn int_strong_roundtrip(term in arb_int(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Int::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Int::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn int_parse_determinism(term in arb_int(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Int::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Int::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn uint32_debug_does_not_panic(term in arb_uint32(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn uint32_display_does_not_panic(term in arb_uint32(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn uint32_clone_eq(term in arb_uint32(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn uint32_display_parse_roundtrip(term in arb_uint32(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = UInt32::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_uint32 produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = UInt32::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn uint32_strong_roundtrip(term in arb_uint32(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = UInt32::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = UInt32::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn uint32_parse_determinism(term in arb_uint32(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = UInt32::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = UInt32::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn bigint_debug_does_not_panic(term in arb_bigint(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn bigint_display_does_not_panic(term in arb_bigint(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn bigint_clone_eq(term in arb_bigint(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn bigint_display_parse_roundtrip(term in arb_bigint(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = BigInt::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_bigint produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = BigInt::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn bigint_strong_roundtrip(term in arb_bigint(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = BigInt::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = BigInt::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn bigint_parse_determinism(term in arb_bigint(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = BigInt::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = BigInt::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn bigrat_debug_does_not_panic(term in arb_bigrat(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn bigrat_display_does_not_panic(term in arb_bigrat(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn bigrat_clone_eq(term in arb_bigrat(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn bigrat_display_parse_roundtrip(term in arb_bigrat(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = BigRat::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_bigrat produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = BigRat::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn bigrat_strong_roundtrip(term in arb_bigrat(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = BigRat::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = BigRat::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn bigrat_parse_determinism(term in arb_bigrat(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = BigRat::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = BigRat::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn fixed_debug_does_not_panic(term in arb_fixed(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn fixed_display_does_not_panic(term in arb_fixed(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn fixed_clone_eq(term in arb_fixed(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn fixed_display_parse_roundtrip(term in arb_fixed(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Fixed::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_fixed produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Fixed::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn fixed_strong_roundtrip(term in arb_fixed(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Fixed::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Fixed::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn fixed_parse_determinism(term in arb_fixed(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Fixed::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Fixed::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn float_debug_does_not_panic(term in arb_float(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn float_display_does_not_panic(term in arb_float(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn float_clone_eq(term in arb_float(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn float_display_parse_roundtrip(term in arb_float(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Float::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_float produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Float::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn float_strong_roundtrip(term in arb_float(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Float::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Float::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn float_parse_determinism(term in arb_float(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Float::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Float::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn bool_debug_does_not_panic(term in arb_bool(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn bool_display_does_not_panic(term in arb_bool(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn bool_clone_eq(term in arb_bool(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn bool_display_parse_roundtrip(term in arb_bool(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Bool::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_bool produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Bool::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn bool_strong_roundtrip(term in arb_bool(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Bool::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Bool::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn bool_parse_determinism(term in arb_bool(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Bool::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Bool::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn str_debug_does_not_panic(term in arb_str(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn str_display_does_not_panic(term in arb_str(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn str_clone_eq(term in arb_str(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn str_display_parse_roundtrip(term in arb_str(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Str::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_str produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Str::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn str_strong_roundtrip(term in arb_str(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Str::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Str::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn str_parse_determinism(term in arb_str(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Str::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Str::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn bytes_debug_does_not_panic(term in arb_bytes(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn bytes_display_does_not_panic(term in arb_bytes(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn bytes_clone_eq(term in arb_bytes(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn bytes_display_parse_roundtrip(term in arb_bytes(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Bytes::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_bytes produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Bytes::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn bytes_strong_roundtrip(term in arb_bytes(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Bytes::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Bytes::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn bytes_parse_determinism(term in arb_bytes(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Bytes::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Bytes::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn list_debug_does_not_panic(term in arb_list(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn list_display_does_not_panic(term in arb_list(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn list_clone_eq(term in arb_list(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn list_display_parse_roundtrip(term in arb_list(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = List::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_list produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = List::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn list_strong_roundtrip(term in arb_list(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = List::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = List::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn list_parse_determinism(term in arb_list(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = List::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = List::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn bag_debug_does_not_panic(term in arb_bag(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn bag_display_does_not_panic(term in arb_bag(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn bag_clone_eq(term in arb_bag(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn bag_display_parse_roundtrip(term in arb_bag(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Bag::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_bag produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Bag::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn bag_strong_roundtrip(term in arb_bag(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Bag::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Bag::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn bag_parse_determinism(term in arb_bag(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Bag::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Bag::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn map_debug_does_not_panic(term in arb_map(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn map_display_does_not_panic(term in arb_map(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn map_clone_eq(term in arb_map(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn map_display_parse_roundtrip(term in arb_map(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Map::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_map produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Map::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn map_strong_roundtrip(term in arb_map(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Map::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Map::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn map_parse_determinism(term in arb_map(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Map::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Map::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn set_debug_does_not_panic(term in arb_set(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn set_display_does_not_panic(term in arb_set(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn set_clone_eq(term in arb_set(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn set_display_parse_roundtrip(term in arb_set(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Set::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_set produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Set::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn set_strong_roundtrip(term in arb_set(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Set::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Set::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn set_parse_determinism(term in arb_set(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Set::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Set::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn pathmap_debug_does_not_panic(term in arb_pathmap(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn pathmap_display_does_not_panic(term in arb_pathmap(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn pathmap_clone_eq(term in arb_pathmap(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn pathmap_display_parse_roundtrip(term in arb_pathmap(3)) {
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow).
        // NOTE: length is only a coarse backstop against degenerate
        // displays; cross-category-shared operator chains are parsed
        // via the walker's k-best extraction, so depth-3 terms are cheap.
        if displayed.len() > 500 {
            return Ok(());
        }
        // GRAMMAR-AWARE ROUNDTRIP CONTRACT (strengthened from
        // silent-skip): the literal-build codegen now projects
        // tape values onto the language's admitted literal
        // domain (see rust_code_rewrite + automaton_walk::classify).
        // Any parse failure here is a real regression — the
        // generator emitted something the grammar does not admit.
        // Canonical-form idempotence:
        //   Parse(Display(Parse(s))) ≡ Parse(s) for any s that
        //   the generator emits.
        let parsed = Pathmap::parse(&displayed)
            .unwrap_or_else(|e| panic!(
                "arb_pathmap produced unparseable surface term {:?}: {:?}",
                displayed, e));
        let canonical = format!("{}", parsed);
        if canonical.len() > 500 { return Ok(()); }
        let reparsed = Pathmap::parse(&canonical).unwrap_or_else(|e| panic!(
            "Parse(Display(Parse(s))) should succeed for canonical form {:?}: {:?}",
            canonical, e));
        let recanonical = format!("{}", reparsed);
        prop_assert_eq!(canonical, recanonical,
            "Display should be idempotent after canonicalization: \
             display(parse(display(parse(display(t))))) == display(parse(display(t)))");
    }

    #[test]
    fn pathmap_strong_roundtrip(term in arb_pathmap(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = Pathmap::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = Pathmap::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip: canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip: canonical display not stable after double parse");
        }
    }

    #[test]
    fn pathmap_parse_determinism(term in arb_pathmap(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = Pathmap::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = Pathmap::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn readzipper_debug_does_not_panic(term in arb_readzipper(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn readzipper_display_does_not_panic(term in arb_readzipper(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn readzipper_clone_eq(term in arb_readzipper(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn readzipper_strong_roundtrip_via_display(term in arb_readzipper(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = ReadZipper::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = ReadZipper::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip (display proxy): canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip (display proxy): canonical display not stable after double parse");
        }
    }

    #[test]
    fn readzipper_parse_determinism(term in arb_readzipper(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = ReadZipper::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = ReadZipper::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

    #[test]
    fn writezipper_debug_does_not_panic(term in arb_writezipper(4)) {
        let _ = format!("{:?}", term);
    }

    #[test]
    fn writezipper_display_does_not_panic(term in arb_writezipper(4)) {
        let _ = format!("{}", term);
    }

    #[test]
    fn writezipper_clone_eq(term in arb_writezipper(4)) {
        let cloned = term.clone();
        prop_assert_eq!(term, cloned);
    }

    #[test]
    fn writezipper_strong_roundtrip_via_display(term in arb_writezipper(1)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        if let Ok(parsed) = WriteZipper::parse(&displayed) {
            let canonical = format!("{}", parsed);
            if canonical.len() > 500 { return Ok(()); }
            mettail_runtime::clear_var_cache();
            let reparsed = WriteZipper::parse(&canonical).unwrap_or_else(|e| panic!(
                "Strong roundtrip (display proxy): canonical form {:?} did not parse: {:?}",
                canonical, e));
            let recanonical = format!("{}", reparsed);
            prop_assert_eq!(&canonical, &recanonical,
                "Strong roundtrip (display proxy): canonical display not stable after double parse");
        }
    }

    #[test]
    fn writezipper_parse_determinism(term in arb_writezipper(2)) {
        mettail_runtime::clear_var_cache();
        let displayed = format!("{}", term);
        // Skip terms whose display is too long (parser may overflow)
        if displayed.len() > 500 {
            return Ok(());
        }
        mettail_runtime::clear_var_cache();
        let p1 = WriteZipper::parse(&displayed);
        mettail_runtime::clear_var_cache();
        let p2 = WriteZipper::parse(&displayed);
        match (p1, p2) {
            (Ok(t1), Ok(t2)) => {
                let d1 = format!("{}", t1);
                let d2 = format!("{}", t2);
                prop_assert_eq!(d1, d2,
                    "Parse determinism failed: two parses of the same string differ");
            }
            (Err(_), Err(_)) => { /* Both failed — consistent */ }
            (Ok(_), Err(e)) | (Err(e), Ok(_)) => {
                prop_assert!(false,
                    "Parse determinism failed: one parse succeeded, other failed: {}", e);
            }
        }
    }

}

// ═══════════════════════════════════════════════════════════
// Simulation tests (runner integration)
// ═══════════════════════════════════════════════════════════

#[test]
fn sim_rhocalc_normal_form_reachability() {
    use mettail_simulation::runner::{SimulationConfig, SimulationRunner};
    use mettail_simulation::trace::TraceOutcome;

    let lang = RhoCalcLanguage;
    let lang_ref: &dyn mettail_runtime::Language = &lang;
    let config = SimulationConfig {
        max_steps: 100,
        track_morphology: false,
        ..SimulationConfig::default()
    };
    let runner = SimulationRunner::new(lang_ref, config);

    let test_inputs: Vec<&str> = vec![
        "0 ! ( )",
        "0 !! ( )",
        "@ Nil ! ( 0 )",
        "@ Nil !! ( 0 )",
        "@ 0 ! ( 0 )",
        "@ 0 !! ( 0 )",
        "@ Nil ! ( )",
        "@ Nil !! ( )",
        "@ 0 ! ( )",
        "@ 0 !! ( )",
        "__guard_then ( 0 , 0 )",
        "@ 0 <- 0",
        "1",
        "1.0",
        "true",
        "\"hello\"",
    ];

    let mut tested = 0usize;
    let mut reached_nf = 0usize;

    for input in &test_inputs {
        mettail_runtime::clear_var_cache();
        // Catch panics from native eval (e.g., division by zero in ![a / b]).
        let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            runner.run_to_normal_form(input)
        }));
        match result {
            Ok(Ok(trace)) => {
                tested += 1;
                if matches!(trace.outcome, TraceOutcome::NormalForm { .. }) {
                    reached_nf += 1;
                }
            },
            _ => {
                // Skip inputs that fail to parse, evaluate, or panic.
            },
        }
    }

    if tested > 0 {
        let pass_rate = reached_nf as f64 / tested as f64;
        assert!(
            pass_rate >= 0.80,
            "RhoCalc normal form reachability: only {:.1}% reached NF ({} / {})",
            pass_rate * 100.0,
            reached_nf,
            tested,
        );
    }
}

#[test]
fn sim_rhocalc_roundtrip_under_rewrite() {
    use mettail_simulation::runner::{SimulationConfig, SimulationRunner};
    use mettail_simulation::trace::TraceOutcome;

    let lang = RhoCalcLanguage;
    let lang_ref: &dyn mettail_runtime::Language = &lang;
    let config = SimulationConfig {
        max_steps: 100,
        seed: Some([99u8; 32]),
        track_morphology: false,
        ..SimulationConfig::default()
    };
    let runner = SimulationRunner::new(lang_ref, config);

    // Test a set of concrete expressions for rewrite roundtrip.
    let test_inputs: Vec<&str> = vec![
        "0 ! ( )",
        "0 !! ( )",
        "@ Nil ! ( 0 )",
        "@ Nil !! ( 0 )",
        "@ 0 ! ( 0 )",
        "@ 0 !! ( 0 )",
        "@ Nil ! ( )",
        "@ Nil !! ( )",
        "@ 0 ! ( )",
        "@ 0 !! ( )",
        "__guard_then ( 0 , 0 )",
        "@ 0 <- 0",
        "1",
        "1.0",
        "true",
        "\"hello\"",
    ];

    for input in &test_inputs {
        mettail_runtime::clear_var_cache();
        // Catch panics from native eval (e.g., division by zero).
        let result1 = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            runner.run_to_normal_form(input)
        }));
        let trace1 = match result1 {
            Ok(Ok(t)) => t,
            _ => continue, // Skip inputs that panic, fail to parse, or error.
        };
        let nf1 = match &trace1.outcome {
            TraceOutcome::NormalForm { term, .. } => term.clone(),
            _ => continue,
        };

        mettail_runtime::clear_var_cache();
        let result2 = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            runner.run_to_normal_form(&nf1)
        }));
        let trace2 = match result2 {
            Ok(Ok(t)) => t,
            _ => panic!(
                "RhoCalc roundtrip re-run panicked or errored for input {:?} \
                 (first NF: {:?})",
                input, nf1,
            ),
        };
        let nf2 = match &trace2.outcome {
            TraceOutcome::NormalForm { term, .. } => term.clone(),
            other => panic!(
                "RhoCalc roundtrip re-run did not reach NF for input {:?} \
                 (first NF: {:?}): {:?}",
                input, nf1, other,
            ),
        };

        if nf1 != nf2 {
            panic!(
                "RhoCalc roundtrip under rewrite FAILED for input {:?}:\n\
                 First NF:  {:?}\nSecond NF: {:?}",
                input, nf1, nf2,
            );
        }
    }
}

#[test]
fn sim_rhocalc_morphology_bounded() {
    use mettail_simulation::runner::{SimulationConfig, SimulationRunner};
    use mettail_simulation::trace::TraceOutcome;

    let lang = RhoCalcLanguage;
    let lang_ref: &dyn mettail_runtime::Language = &lang;
    let config = SimulationConfig {
        max_steps: 100,
        track_morphology: true,
        ..SimulationConfig::default()
    };
    let runner = SimulationRunner::new(lang_ref, config);

    let test_inputs: Vec<&str> = vec![
        "0 ! ( )",
        "0 !! ( )",
        "@ Nil ! ( 0 )",
        "@ Nil !! ( 0 )",
        "@ 0 ! ( 0 )",
        "@ 0 !! ( 0 )",
        "@ Nil ! ( )",
        "@ Nil !! ( )",
        "@ 0 ! ( )",
        "@ 0 !! ( )",
        "__guard_then ( 0 , 0 )",
        "@ 0 <- 0",
        "1",
        "1.0",
        "true",
        "\"hello\"",
    ];

    for input in &test_inputs {
        mettail_runtime::clear_var_cache();
        // Catch panics from native eval (e.g., division by zero).
        let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            runner.run_to_normal_form(input)
        }));
        if let Ok(Ok(trace)) = result {
            if let Some(ref morph) = trace.morphology {
                assert!(
                    morph.max_nodes <= 50000,
                    "RhoCalc morphology: max nodes {} exceeds bound 50000 for input {:?}",
                    morph.max_nodes,
                    input,
                );
                assert!(
                    morph.max_depth <= 100,
                    "RhoCalc morphology: max depth {} exceeds bound 100 for input {:?}",
                    morph.max_depth,
                    input,
                );
            }
        }
    }
}

#[test]
fn sim_rhocalc_eval_determinism() {
    use mettail_simulation::runner::{SimulationConfig, SimulationRunner};
    use mettail_simulation::trace::TraceOutcome;

    let lang = RhoCalcLanguage;
    let lang_ref: &dyn mettail_runtime::Language = &lang;

    let test_inputs: Vec<&str> = vec![
        "0 ! ( )",
        "0 !! ( )",
        "@ Nil ! ( 0 )",
        "@ Nil !! ( 0 )",
        "@ 0 ! ( 0 )",
        "@ 0 !! ( 0 )",
        "@ Nil ! ( )",
        "@ Nil !! ( )",
        "@ 0 ! ( )",
        "@ 0 !! ( )",
        "__guard_then ( 0 , 0 )",
        "@ 0 <- 0",
        "1",
        "1.0",
        "true",
        "\"hello\"",
    ];

    for input in &test_inputs {
        let config1 = SimulationConfig {
            max_steps: 100,
            track_morphology: false,
            ..SimulationConfig::default()
        };
        let runner1 = SimulationRunner::new(lang_ref, config1);

        mettail_runtime::clear_var_cache();
        // Catch panics from native eval (e.g., division by zero).
        let result1 = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            runner1.run_to_normal_form(input)
        }));
        let trace1 = match result1 {
            Ok(Ok(t)) => t,
            _ => continue, // Skip inputs that panic or fail.
        };
        let nf1 = match &trace1.outcome {
            TraceOutcome::NormalForm { term, .. } => term.clone(),
            _ => continue,
        };

        let config2 = SimulationConfig {
            max_steps: 100,
            track_morphology: false,
            ..SimulationConfig::default()
        };
        let runner2 = SimulationRunner::new(lang_ref, config2);

        mettail_runtime::clear_var_cache();
        let result2 = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            runner2.run_to_normal_form(input)
        }));
        let trace2 = match result2 {
            Ok(Ok(t)) => t,
            _ => continue, // If the second run panics but the first didn't, skip.
        };
        let nf2 = match &trace2.outcome {
            TraceOutcome::NormalForm { term, .. } => term.clone(),
            _ => continue,
        };

        assert_eq!(
            nf1, nf2,
            "RhoCalc eval determinism: different normal forms for input {:?}: {:?} vs {:?}",
            input, nf1, nf2,
        );
    }
}

proptest! {
    #![proptest_config(ProptestConfig::with_cases(50))]

    #[test]
    fn sim_rhocalc_proptest_campaign(term in arb_proc(3u32)) {
        use mettail_simulation::runner::{SimulationConfig, SimulationRunner};
        use mettail_simulation::trace::TraceOutcome;
        use mettail_simulation::invariant::{BoundedSize, BoundedDepth};

        let lang = RhoCalcLanguage;
        let lang_ref: &dyn mettail_runtime::Language = &lang;

        let displayed = format!("{}", term);
        // Skip very large terms to avoid OOM in the rewrite engine.
        if displayed.len() > 500 {
            return Ok(());
        }

        // `AlwaysParseable` is intentionally excluded: the roundtrip tests
        // (sim_*_roundtrip_under_rewrite) already check parseability of normal
        // forms, and AlwaysParseable's per-step `clear_var_cache()` is a
        // significant per-iteration cost in a proptest campaign.
        let config = SimulationConfig {
            max_steps: 50,
            track_morphology: false,
            invariants: vec![
                Box::new(BoundedSize { max_nodes: 10000 }),
                Box::new(BoundedDepth { max_depth: 50 }),
            ],
            ..SimulationConfig::default()
        };
        let runner = SimulationRunner::new(lang_ref, config);

        // `catch_unwind` is defensive only: the `SafeArith` + `rust_code_rewrite`
        // pass removes arithmetic overflow as a panic source. Any remaining panic
        // (e.g. parser bug, language invariant broken) is a real bug we'd want to
        // surface — but we catch it here so proptest shrinking doesn't hit the
        // macOS double-panic abort. `prop_assert!` fires only on
        // `InvariantViolation` (the bug signal for this test); other outcomes
        // are covered by Tests 1–4.
        let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
            mettail_runtime::clear_var_cache();
            runner.run_to_normal_form(&displayed)
        }));
        if let Ok(Ok(trace)) = result {
            if let TraceOutcome::InvariantViolation { ref invariant, ref message, .. } = trace.outcome {
                prop_assert!(
                    false,
                    "RhoCalc invariant '{}' violated on '{}': {}",
                    invariant, displayed, message,
                );
            }
        }
        // All other outcomes (SimulationFailure, panics, StepLimitReached, Error)
        // are tolerated — they are covered by dedicated non-proptest simulation tests.
    }
}
