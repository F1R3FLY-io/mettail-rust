// AUTO-GENERATED by language! macro for Calculator (rewrite) — do not edit
// Regenerated on each compilation of the language definition.
// Run with: cargo test -p mettail-languages

#![allow(unused_imports, dead_code)]

use mettail_languages::calculator::*;
use mettail_runtime::Language;
use mettail_runtime::BehavioralPred;

// ═══════════════════════════════════════════════════════════
// Rewrite tests (one per rewrite rule)
// ═══════════════════════════════════════════════════════════

// Rewrite test for EqIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqBoolCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqboolcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqBoolCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqboolcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqStrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqstrcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqStrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqstrcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtBoolCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtboolcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtBoolCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtboolcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtStrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtstrcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtStrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtstrcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtBoolCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltboolcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtBoolCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltboolcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtStrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltstrcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtStrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltstrcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqBoolCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqboolcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqBoolCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqboolcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqStrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqstrcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqStrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqstrcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqBoolCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqboolcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqBoolCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqboolcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqStrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqstrcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqStrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqstrcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_neintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_neintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_nefloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_nefloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeBoolCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_neboolcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeBoolCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_neboolcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeStrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_nestrcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeStrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_nestrcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for EqFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_eqfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gtfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ltfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LtEqFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lteqfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GtEqFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_gteqfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_nefixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NeFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_nefixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AndCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_andcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AndCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_andcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for OrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_orcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for OrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_orcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for XorCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_xorcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for XorCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_xorcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NotCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_notcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LenCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lencong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ConcatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_concatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ConcatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_concatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddStrCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addstrcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddStrCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addstrcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NegCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_negcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NegFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_negfloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ModIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_modintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ModIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_modintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PowIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_powintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FactCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_factcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitNotIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitnotintcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PowFloatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_powfloatcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PowFloatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_powfloatcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SinFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_sinfloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for CosFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_cosfloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ExpFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_expfloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LnFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lnfloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ModFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_modfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ModFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_modfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NegFixedCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_negfixedcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrFixedCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorfixedcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrFixedCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorfixedcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitNotFixedCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitnotfixedcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcToBoolCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_proctoboolcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcToStrCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_proctostrcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntBinCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_intbincongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntBinCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_intbincongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for UIntBinCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_uintbincongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for UIntBinCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_uintbincongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatBinCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_floatbincongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatBinCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_floatbincongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FixedBinCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_fixedbincongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FixedBinCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_fixedbincongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BigintCastCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bigintcastcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BigratCastCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bigratcastcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procintcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procfloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcBoolCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procboolcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcStrCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procstrcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcListCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_proclistcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcBagCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procbagcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcMapCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procmapcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for LenMapCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_lenmapcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GetMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_getmapcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for GetMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_getmapcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PutMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_putmapcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PutMapCongKey skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_putmapcongkey() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for PutMapCongVal skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_putmapcongval() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DeleteMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_deletemapcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DeleteMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_deletemapcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MergeMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mergemapcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MergeMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mergemapcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for HasMapCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_hasmapcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for HasMapCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_hasmapcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for KeysMapCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_keysmapcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ValuesMapCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_valuesmapcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for TernCongC skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_terncongc() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for TernCongT skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_terncongt() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for TernCongE skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_ternconge() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcUInt32Cong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procuint32cong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddUInt32CongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_adduint32congl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddUInt32CongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_adduint32congr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndUInt32CongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitanduint32congl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndUInt32CongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitanduint32congr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrUInt32CongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitoruint32congl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrUInt32CongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitoruint32congr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitNotUInt32Cong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitnotuint32cong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcBigIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procbigintcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddBigIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addbigintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddBigIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addbigintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubBigIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subbigintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for SubBigIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_subbigintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NegBigIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_negbigintcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndBigIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandbigintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndBigIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandbigintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrBigIntCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorbigintcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrBigIntCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorbigintcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitNotBigIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitnotbigintcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procbigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for ProcFixedCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_procfixedcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FractionCongN skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_fractioncongn() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FractionCongD skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_fractioncongd() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddBigRatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addbigratcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AddBigRatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_addbigratcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndBigRatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandbigratcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitAndBigRatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitandbigratcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrBigRatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorbigratcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitOrBigRatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitorbigratcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BitNotBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_bitnotbigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulBigRatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulbigratcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for MulBigRatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_mulbigratcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivBigRatCongL skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divbigratcongl() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for DivBigRatCongR skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_divbigratcongr() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NegBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_negbigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntToFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_inttofloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BoolToFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_booltofloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for StrToFloatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_strtofloatcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatToIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_floattointcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BoolToIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_booltointcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for StrToIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_strtointcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BoolToStrCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_booltostrcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntToStrCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_inttostrcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatToStrCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_floattostrcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntToBoolCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_inttoboolcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatToBoolCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_floattoboolcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for StrToBoolCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_strtoboolcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for IntIdCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_intidcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for FloatIdCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_floatidcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BoolIdCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_boolidcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for StrIdCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_stridcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for BoolToUInt32Cong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_booltouint32cong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastbooltouint32inproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastBoolToUInt32InProc (index 209) exists in metadata
    assert!(
        rewrites.len() > 209,
        "Expected at least 210 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[209];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastBoolToUInt32InProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastBoolToUInt32InProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastBoolToUInt32InProc RHS should be non-empty");
}

// Rewrite test for BoolToBigIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_booltobigintcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastbooltobigintinproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastBoolToBigIntInProc (index 211) exists in metadata
    assert!(
        rewrites.len() > 211,
        "Expected at least 212 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[211];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastBoolToBigIntInProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastBoolToBigIntInProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastBoolToBigIntInProc RHS should be non-empty");
}

// Rewrite test for BoolToBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_booltobigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastbooltobigratinproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastBoolToBigRatInProc (index 213) exists in metadata
    assert!(
        rewrites.len() > 213,
        "Expected at least 214 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[213];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastBoolToBigRatInProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastBoolToBigRatInProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastBoolToBigRatInProc RHS should be non-empty");
}

// Rewrite test for UInt32ToBigIntCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_uint32tobigintcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastuint32tobigintinproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastUInt32ToBigIntInProc (index 215) exists in metadata
    assert!(
        rewrites.len() > 215,
        "Expected at least 216 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[215];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastUInt32ToBigIntInProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastUInt32ToBigIntInProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastUInt32ToBigIntInProc RHS should be non-empty");
}

// Rewrite test for UInt32ToBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_uint32tobigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastuint32tobigratinproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastUInt32ToBigRatInProc (index 217) exists in metadata
    assert!(
        rewrites.len() > 217,
        "Expected at least 218 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[217];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastUInt32ToBigRatInProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastUInt32ToBigRatInProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastUInt32ToBigRatInProc RHS should be non-empty");
}

// Rewrite test for FloatToBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_floattobigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastfloattobigratinproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastFloatToBigRatInProc (index 219) exists in metadata
    assert!(
        rewrites.len() > 219,
        "Expected at least 220 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[219];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastFloatToBigRatInProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastFloatToBigRatInProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastFloatToBigRatInProc RHS should be non-empty");
}

// Rewrite test for BigIntToBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_biginttobigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastbiginttobigratinproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastBigIntToBigRatInProc (index 221) exists in metadata
    assert!(
        rewrites.len() > 221,
        "Expected at least 222 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[221];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastBigIntToBigRatInProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastBigIntToBigRatInProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastBigIntToBigRatInProc RHS should be non-empty");
}

// Rewrite test for FixedToBigRatCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_calculator_fixedtobigratcong() {
    let _lang = CalculatorLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

#[test]
fn rewrite_calculator_normcastfixedtobigratinproc() {
    let _lang = CalculatorLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite NormCastFixedToBigRatInProc (index 223) exists in metadata
    assert!(
        rewrites.len() > 223,
        "Expected at least 224 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[223];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("NormCastFixedToBigRatInProc"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite NormCastFixedToBigRatInProc LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite NormCastFixedToBigRatInProc RHS should be non-empty");
}

