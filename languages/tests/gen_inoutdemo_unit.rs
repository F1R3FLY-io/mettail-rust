// AUTO-GENERATED by language! macro for InOutDemo (unit) — do not edit
// Regenerated on each compilation of the language definition.
// Run with: cargo test -p mettail-languages

#![allow(unused_imports, dead_code)]

use mettail_languages::inoutdemo::*;
use mettail_runtime::BehavioralPred;
use mettail_runtime::Language;

// ═══════════════════════════════════════════════════════════
// Unit tests (one per constructor)
// ═══════════════════════════════════════════════════════════

#[test]
fn inoutdemo_language_instantiates() {
    let lang = InOutDemoLanguage;
    assert_eq!(lang.name(), "InOutDemo");
}

#[test]
fn inoutdemo_metadata_populated() {
    let lang = InOutDemoLanguage;
    let meta = lang.metadata();
    assert!(!meta.types().is_empty(), "language should have at least one type");
    assert!(!meta.terms().is_empty(), "language should have at least one term");
}

#[test]
fn unit_inoutdemo_proc_pzero() {
    let term = Proc::PZero;
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for PZero");
    if let Ok(parsed) = Proc::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for PZero: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_proc_pa() {
    let term = Proc::PA;
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for PA");
    if let Ok(parsed) = Proc::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for PA: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_proc_pb() {
    let term = Proc::PB;
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for PB");
    if let Ok(parsed) = Proc::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for PB: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_name_na() {
    let term = Name::Na;
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for Na");
    if let Ok(parsed) = Name::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for Na: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_name_nb() {
    let term = Name::Nb;
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for Nb");
    if let Ok(parsed) = Name::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for Nb: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_name_nc() {
    let term = Name::Nc;
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for Nc");
    if let Ok(parsed) = Name::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for Nc: {} != {}",
            displayed, re_displayed
        );
    }
}

// Skipped unit test for PPar (Proc) — constructor too complex to construct statically

#[test]
fn unit_inoutdemo_proc_pin() {
    mettail_runtime::clear_var_cache();
    let term = Proc::PIn(std::sync::Arc::new(Name::Na), std::sync::Arc::new(Proc::PZero));
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for PIn");
    if let Ok(parsed) = Proc::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for PIn: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_proc_pout() {
    mettail_runtime::clear_var_cache();
    let term = Proc::POut(std::sync::Arc::new(Name::Na), std::sync::Arc::new(Proc::PZero));
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for POut");
    if let Ok(parsed) = Proc::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for POut: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_proc_pamb() {
    mettail_runtime::clear_var_cache();
    let term = Proc::PAmb(std::sync::Arc::new(Name::Na), std::sync::Arc::new(Proc::PZero));
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for PAmb");
    if let Ok(parsed) = Proc::parse(&displayed) {
        let re_displayed = format!("{}", parsed);
        assert_eq!(
            displayed, re_displayed,
            "Roundtrip failed for PAmb: {} != {}",
            displayed, re_displayed
        );
    }
}

#[test]
fn unit_inoutdemo_auto_proc_pvar() {
    mettail_runtime::clear_var_cache();
    let term = Proc::PVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
        mettail_runtime::get_or_create_var("a"),
    )));
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for PVar");
}

#[test]
fn unit_inoutdemo_auto_name_nvar() {
    mettail_runtime::clear_var_cache();
    let term = Name::NVar(mettail_runtime::OrdVar(mettail_runtime::Var::Free(
        mettail_runtime::get_or_create_var("a"),
    )));
    let displayed = format!("{}", term);
    assert!(!displayed.is_empty(), "Display should produce non-empty output for NVar");
}
