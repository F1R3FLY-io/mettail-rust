// AUTO-GENERATED by language! macro for Ambient (rewrite) — do not edit
// Regenerated on each compilation of the language definition.
// Run with: cargo test -p mettail-languages

#![allow(unused_imports, dead_code)]

use mettail_languages::ambient::*;
use mettail_runtime::BehavioralPred;
use mettail_runtime::Language;

// ═══════════════════════════════════════════════════════════
// Equation tests (one per equation)
// ═══════════════════════════════════════════════════════════

#[test]
fn equation_ambient_newcomm() {
    let _lang = AmbientLanguage;
    let meta = _lang.metadata();
    let equations = meta.equations();
    // Verify equation NewComm (index 0) exists in metadata
    assert!(
        equations.len() > 0,
        "Expected at least 1 equations in metadata, found {}",
        equations.len()
    );
    let eq = &equations[0];
    assert!(!eq.lhs.is_empty(), "Equation NewComm LHS should be non-empty");
    assert!(!eq.rhs.is_empty(), "Equation NewComm RHS should be non-empty");
}

// Equation ScopeExtrusion has freshness/complex conditions
#[test]
fn equation_ambient_scopeextrusion() {
    let _lang = AmbientLanguage;
    // This equation requires freshness conditions or complex premises
    // that cannot be instantiated statically.
}

// Equation InNew has freshness/complex conditions
#[test]
fn equation_ambient_innew() {
    let _lang = AmbientLanguage;
    // This equation requires freshness conditions or complex premises
    // that cannot be instantiated statically.
}

// Equation OutNew has freshness/complex conditions
#[test]
fn equation_ambient_outnew() {
    let _lang = AmbientLanguage;
    // This equation requires freshness conditions or complex premises
    // that cannot be instantiated statically.
}

// Equation OpenNew has freshness/complex conditions
#[test]
fn equation_ambient_opennew() {
    let _lang = AmbientLanguage;
    // This equation requires freshness conditions or complex premises
    // that cannot be instantiated statically.
}

// Equation AmbNew has freshness/complex conditions
#[test]
fn equation_ambient_ambnew() {
    let _lang = AmbientLanguage;
    // This equation requires freshness conditions or complex premises
    // that cannot be instantiated statically.
}

// ═══════════════════════════════════════════════════════════
// Rewrite tests (one per rewrite rule)
// ═══════════════════════════════════════════════════════════

#[test]
fn rewrite_ambient_inrule() {
    let _lang = AmbientLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite InRule (index 0) exists in metadata
    assert!(
        rewrites.len() > 0,
        "Expected at least 1 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[0];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("InRule"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite InRule LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite InRule RHS should be non-empty");
}

#[test]
fn rewrite_ambient_outrule() {
    let _lang = AmbientLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite OutRule (index 1) exists in metadata
    assert!(
        rewrites.len() > 1,
        "Expected at least 2 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[1];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("OutRule"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite OutRule LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite OutRule RHS should be non-empty");
}

// Concrete execution test: run "0 [ 0 ]" through SimulationRunner,
// assert it reaches NormalForm (exercises rewrite rule OutRule).
#[test]
fn rewrite_ambient_outrule_exec() {
    use mettail_simulation::runner::{SimulationConfig, SimulationRunner};
    use mettail_simulation::trace::TraceOutcome;
    let lang = AmbientLanguage;
    let lang_ref: &dyn mettail_runtime::Language = &lang;
    let config = SimulationConfig {
        max_steps: 100,
        track_morphology: false,
        ..SimulationConfig::default()
    };
    let runner = SimulationRunner::new(lang_ref, config);
    mettail_runtime::clear_var_cache();
    let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        runner.run_to_normal_form("0 [ 0 ]")
    }));
    if let Ok(Ok(trace)) = result {
        assert!(
            matches!(trace.outcome, TraceOutcome::NormalForm { .. }),
            "Rewrite rule OutRule: input '0 [ 0 ]' did not reach NF: {:?}",
            trace.outcome,
        );
    }
    // Panics (e.g., division by zero in native eval) are tolerated.
}

#[test]
fn rewrite_ambient_openrule() {
    let _lang = AmbientLanguage;
    let meta = _lang.metadata();
    let rewrites = meta.rewrites();
    // Verify rewrite OpenRule (index 2) exists in metadata
    assert!(
        rewrites.len() > 2,
        "Expected at least 3 rewrites in metadata, found {}",
        rewrites.len()
    );
    let rw = &rewrites[2];
    // Verify rewrite rule name
    assert_eq!(rw.name, Some("OpenRule"), "Rewrite rule name mismatch");
    assert!(!rw.lhs.is_empty(), "Rewrite OpenRule LHS should be non-empty");
    assert!(!rw.rhs.is_empty(), "Rewrite OpenRule RHS should be non-empty");
}

// Rewrite test for ParCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_ambient_parcong() {
    let _lang = AmbientLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for NewCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_ambient_newcong() {
    let _lang = AmbientLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}

// Rewrite test for AmbCong skipped: congruence rule (needs triggering context)
#[test]
fn rewrite_ambient_ambcong() {
    let _lang = AmbientLanguage;
    // Congruence rules require a rewrite-triggering context to test.
    // They fire when their premise (S ~> T) is satisfied by another rewrite.
}
