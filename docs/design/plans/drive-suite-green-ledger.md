# Drive-Suite-Green Ledger (started 2026-05-29)

Scientific ledger for driving the full-workspace `cargo nextest run` to green.
Plan: `~/.claude/plans/review-the-nextest-logs-peaceful-beacon.md`.
Baseline run: `/var/tmp/nextest.log` — **113 failed / 9615 passed / 20 skipped**, 2 hung >39 min, at HEAD `6507b9c`.

## Invariants (apply to every fix)
- **Preserve WPDA disambiguation:** never prematurely drop a parse/derivation alternate; drop only when
  evidence rejects it. Dedup only observationally-equivalent terms (`semantic_hash`, not Display). `Ambiguous` is first-class.
- Per-fix gate: gauntlet (`-p prattail --lib` → 4206/0) + op-suites (gen_calculator_op ≥1321, gen_rholang_op 532/0)
  + disambiguation gate (`-3!` ladder + `wpda_parity_*`) + Welch (p<0.05, quiet) for any runtime/perf path.

## Bisect anchors (all confirmed ancestors of HEAD 6507b9c)
| Anchor | SHA | Meaning |
|--------|-----|---------|
| HEAD | `6507b9c` | current (failing) |
| WALK-S0^ | `a23ef69` | before synth_binary_chain / IterAbsorbSpec (WALK-S0); after Tomita reorder + chart-completion refactor |
| Box→Arc | `9c55d81` | Box→Arc AST children |
| Box→Arc^ | `b780295` | before Box→Arc; "eliminate O(N²) Earley chart" (already chain-work) |
| Plan-v6^ | `dfb616f^` | before the Plan v6 Hybrid Earley+WPDS work (clean pre-Earley-hybrid anchor for "truly pre-existing") |

Bucketing rule: PASS@a23ef69 & FAIL@HEAD → **regr-WALK**; PASS@b780295 & FAIL@a23ef69 → **regr-Box→Arc**;
FAIL@b780295 but PASS@dfb616f^ → **regr-Plan-v6**; FAIL@dfb616f^ → **pre-existing (Phase 8)**.

## Cluster ledger
| Cluster | Failing tests | HEAD | @a23ef69 | @b780295 | @dfb616f^ | Bucket | Fix-commit | Gauntlet | Op-suites | Welch | Status |
|---------|---------------|------|----------|----------|-----------|--------|-----------|----------|-----------|-------|--------|
| A (subst OOB ×12) | rholang comm::*, beta, exec, bag, new_and_extrusion | FAIL | FAIL | FAIL (single_channel) | ? | **pre-existing** (≤b780295) | — | — | — | — | in-progress |
| B (no accepting branch ~40) | edge_case grouped/comparison, calc `<`/`xor`/cast, rholang `@`/`^`/`new` | FAIL | **PASS** | — | — | **regr-WALK (S0..S3)** | — | — | — | — | pending |
| C (collection LIFO ×3) | drain_collection crashes (= E in dev) | FAIL | FAIL | FAIL (via E) | ? | **pre-existing** (≤b780295) | — | — | — | — | pending |
| D (over-ambiguous NF ~10) | rholang_cast_*, congruence::* | FAIL | FAIL | FAIL (add_cong) | ? | **pre-existing** (≤b780295) | — | — | — | — | pending |
| E (empty maps ×5) | calc test_map_get/keys/values/merge | FAIL | FAIL | FAIL (map_get/keys) | ? | **pre-existing** (≤b780295) | — | — | — | — | pending |
| F (cast/numeric ~8) | Int::parse overflow, NumLit typing | FAIL | FAIL | FAIL (unambig_int) | ? | **pre-existing** (memory `ab027a4`) | — | — | — | — | pending |
| G (analytical termination ×2) | gen_rholang/ledtest_analytical | FAIL | FAIL | FAIL (termination) | ? | **pre-existing** (≤b780295) | — | — | — | — | pending |
| H (recovery/trailing ×8) | parse_error + recovery_integration | FAIL | FAIL | FAIL (calc_recovery) | ? | **pre-existing** (≤b780295) | — | — | — | — | pending |
| I (dedup ×1) | run_ascent_dedup | FAIL | FAIL | FAIL | ? | **pre-existing** (≤b780295) | — | — | — | — | pending |
| J (roundtrip ~25) | gen_*_prop *_display_parse_roundtrip | FAIL | ? | — | — | likely regr-WALK via B | — | — | — | — | pending |

**Phase 0 conclusion:** Only **Cluster B regressed recently (WALK-S0..S3)**. A, C, D, E, F, G, H, I all fail at b780295 (pre-Box→Arc, pre-WALK) — long-standing broad-suite failures NOT covered by the gauntlet/op-suites. Their root causes are concrete codegen/walker bugs (already located), so they are fixed by root cause, not by diffing a culprit commit. Pinpointing the exact older introduction of each is deferred (not fix-critical). dfb616f^ anchor unused (no remaining ambiguity that it would resolve).

## Progress sweep #1 (2026-05-29, after collection + Cluster A + Cluster B-A fixes)
Gauntlet 4220/0; op-suites 2118/0. Non-proptest failing-binary sweep: **2492 passed, 57 failed** (was ~78 in these binaries). Remaining 57 categorized:
- **Cross-cat/cast/numeric (~22)** [Family B + F]: calc `test_cast_*` (float_overflow_to_inf, int_nonfinite_float_is_error, int_float_floor, int_invalid_width), `test_casts_from_numeric_strings`, `test_binary_int_requires_width`, `test_int_from_float_still_works`, `test_int_literal_rejects_above_i32_max`, `test_int_parse_rejects_u32_suffix`, `test_unambiguous_int_literal`, `test_nested_{int_int,int_float,float_float_int}`, `test_triple_nested_float`, `simulator_regression_cross_cat_with_parens`; rholang `rholang_cast_{fixed_floor,float_from_rational_string,int_invalid_width_error,int_nonfinite_float_is_error}`; edge_case `str_of_cross_cat_eq_{true,false}`; `casting_example_files_calculator`; `roundtrip idempotent_int_display`.
- **Bare-var/parse (~10)** [Cluster B-C, cross-cat leak]: comm::{single_channel,multi_input_two_channels,multi_input_three_channels,multi_input_uses_both_vars,join_pattern_same_channel,comm_with_body_using_channel,comm_with_remaining_parallel}, parsing::{bare_variable_infers_as_proc,new_single,receive}, new_and_extrusion::{new_parses,extrusion_forward}, exec::exec_with_process, type_inference::pinputs_lookup_by_name.
- **Recovery/trailing (8)** [Cluster H, tractable wrapper]: recovery_integration_tests::{test_calc_recovery_missing_operator,test_calc_recovery_trailing_integer,test_float_recovery_trailing,test_float_recovery_trig_unclosed,test_str_recovery_trailing}, parse_error_tests::{test_recovery_trailing_tokens_reported,test_trailing_tokens_extra_paren,test_trailing_tokens_two_literals}, led_delegation_tests::test_p1_10_parenthesized_sub_expressions.
- **Eval/NF (~4)** [Cluster D]: congruence::{add_cong,comparison_cong}, native_ops::arithmetic::{float_literal_f64_suffix_tokens,fraction_builds_rational}, beta::{dollar_name_reduces,normalize_via_language_trait}, new_and_extrusion::new_is_normal_when_body_is.
- **Termination (2)** [G]: gen_rholang_analytical, gen_ledtest_analytical. **Dedup (1)** [I]: run_ascent_dedup. **class2_opt choosemaybe (1)**: pre-existing (Plan agent verified fails at baseline).
- NOT in this sweep: gen_*_prop display→parse roundtrips (~25, expected to largely cascade from B-A) + slow campaigns.

## Tractable remnants FIXED (2026-05-29) — Clusters I, G, H, D (12 tests; gauntlet 4220/0, op-suites 2118/0 preserved)
- **I (2):** `run_ascent_dedup::{no_duplicate_initial_in_relations, all_terms_unique_term_ids}` — `multi_cat_union_extract` (macros/src/gen/runtime/language.rs) now dedups `__all_term_infos` by `semantic_hash` (transparent `ProcFloat` projection collapses Proc/Float dup; `-3!` non-transparent pair preserved). Observational dedup, not Display.
- **G (2):** `gen_{rholang,ledtest}_analytical::*_termination_check` — testkit `parse_pattern_to_term` (testkit/src/analytical/confluence.rs) mis-classified single-letter `v` (from transparent cast wrappers rendering `lhs="v",rhs="v"`) as a nullary constant → spurious self-loop DP → false non-termination. Fix: single ASCII letter = metavariable → `Var(v)→Var(v)` yields no DP (identity terminates).
- **H (8):** 7 trailing-token tests already green via tree-state `AcceptedWithTrailing` (facade.rs); `test_float_recovery_trig_unclosed` (`sin(1.0`) fixed via grammar-general bracket-balance check in `parse_recovering` Ok-arm (macros/src/gen/mod.rs) — appends `UnexpectedEof` when opens>closes and no error reported.
- **D (1):** `native_ops::arithmetic::float_literal_f64_suffix_tokens` — `{1.0f64+0.5f64}` lawfully normalizes to exact `3/2` (rholang Float64→CanonicalBigRat lossless tower); test expectation corrected to `3/2` (value preserved; f64 suffix lex/parse + arithmetic still verified).
- **DEFERRED (8, cross-cat-rooted):** C `test_cast_int_invalid_width` (no guard-rewrite syntax + cross-cat parse pollution), H `led_delegation::test_p1_10_parenthesized_sub_expressions` (cross-cat LED-after-grouping), D 6 (`congruence::{add_cong,comparison_cong}` @, `fraction_builds_rational` 1n, `beta::{dollar_name_reduces,normalize_via_language_trait}` ^, `new_is_normal_when_body_is` bare-var) — all blocked by the cohort-revive root below.

## ⚑ Cross-cat cluster ROOT CAUSE (definitive, 2026-05-29) — cohort-revive rework [IN PROGRESS: M0+M1 + Welch eval]
**Plan + scientific-evaluation protocol: `docs/design/plans/cohort-revive-rework.md`.** Chosen TARGETED revive-on-loss, NOT full Exp-15. Root cause below.

### M0+M1 IMPLEMENTED + VALIDATED (2026-05-29, agent + self-completed)
M0 (orphan census) + M1 (revive-on-loss InFlight branch) are in the tree (`dispatch_cohort.rs` `drain_orphaned_inflight_members:650`; `wpda_walker.rs` `revive_orphaned_cohort_members_once:8518`, hook in `!progress_made` block `:3891`). **H_correctness CONFIRMED:** post-M1 gauntlet **4220/0**, op-suites **2118/0**, disambiguation gate **28/28**, stay-green **9/9** — zero regression. **M1 flips a subset of cross-cat green:** edge `str_of_cross_cat_eq_{true,false}` 2/2; calc cross-cat 9/20; rholang 10/16.
**H_perf Welch:** baseline (pre-M1, N=30, σ≈0.0-0.1ms) vs first treatment (post-M1) showed uniform +10-17% with 2-7× higher σ → suspected DRIFT (benches 2.5h apart), not regression. Mechanistic: chains have empty cohort cache → `drain` is a no-op → M1 inert for chains. **INTERLEAVED re-bench (drift-robust, hyperfine alternating, 40 runs/arm) SETTLES IT: ACCEPT — NEUTRAL.** Per-arm baseline-vs-treatment ratios: left_50 1.00±0.07, left_100 1.03±0.07, left_200 1.02±0.05, right_50 1.01±0.08 (treatment faster), right_100 1.02±0.09 (treatment faster), right_200 1.01±0.10 (treatment faster), right_1000 1.02±0.08 — every CI includes 1.0, direction flips → no significant difference at p<0.05. The first +10-17% reading was DRIFT (benches 2.5h apart; σ jumped 2-7× = the tell). **M1 cohort-revive VALIDATED: correct + perf-neutral + memory-neutral (empty cohort cache for chains).**
### RESIDUAL ~17 cross-cat (M1 did NOT fix — sharpened scope):
- **2-arg cast family (M4 facet — lex-split key conflation):** calc `test_cast_{float_overflow_to_inf,int_float_floor,int_invalid_width,int_nonfinite_float_is_error}`, `test_casts_from_numeric_strings`, `test_int_from_float_still_works`, `test_nested_{int_int,int_float,float_float_int}`, `test_triple_nested_float`, `simulator_regression_cross_cat_with_parens`; rholang `rholang_cast_{fixed_floor,float_from_rational_string,int_invalid_width_error,int_nonfinite_float_is_error}`. ROOT: `int(a,w)` with `a:Proc` needing cross-cat injection; the cross-cat slot cursor dies because `DispatchKey`(`dispatch_cohort.rs:63`) + cycle-defense `PackedDispatchConfig`(`extract_dispatch_config wpda_walker.rs:2654`) key on `(pos,cat_src/source,bp)` WITHOUT rule/source → distinct injections (ProcStr/ProcFloat) collide-and-pause; lex-split `3.14` (`Integer "3"` vs `Float "3.14"`) lands on wrong DAG node. **M4 FIX:** widen `DispatchKey` + `PackedDispatchConfig` with a rule/source discriminator, KEEP `equiv()`/`EquivKey`(`:90`) UNWIDENED (chain-merge 50× collision / memory ceiling). Gate: Welch chain panel + chain_1000/2000 RSS (+5% max) + gauntlet + op-suites. HIGH-RISK milestone.
- **bare-var (M3 facet):** rholang `bare_variable_infers_as_proc` (`Ambiguous(Proc,Name)` vs "Proc" — PVar starved or inference reconciliation), `comm::single_channel` (bare-var expected). Evidence-driven inference fix, no cohort change unless census shows orphan.
- **C cast-error surfacing** (`test_cast_int_invalid_width`): after M4 makes parse clean, add failure-surfacing rewrite (grammar/codegen).

### M4 ATTEMPTED + REVERTED (2026-05-29) — trace-proven correct, but unmasks 2 deeper bugs [definitive]
M4 `DispatchKey`-widening (added `wrap_cat`+`wrap_rule` from `branch.symbol.{category_src_idx,rule_index_in_category}`; threaded through `EdgeKind::CrossCatProjection`/`gss.rs`, register/resolve/revive sites; **`EquivKey` kept narrow** — chain memory ceiling provably untouched). **Trace CONFIRMED the root:** `int(int(5,32),32)` collapses 4 distinct WRAP rules `(0,0)/(1,1)/(6,1)/(7,34)` to ONE DispatchKey → 3 paused+lost. **M4 moved calc cast 0/12→8/12 (FAIL→PASS: cast_float_overflow_to_inf, cast_int_float_floor, casts_from_numeric_strings, int_from_float_still_works, nested_int_float, triple_nested_float, simulator_regression_cross_cat_with_parens, binary_int_requires_width) AND satisfied M3 (bare_variable_infers_as_proc + comm::single_channel PASS — PVar un-starved).** rholang casts began parsing.
**WHY REVERTED — M4 correctly un-conflates the cohort, which unmasks 2 PRE-EXISTING latent bugs M1's conflation was hiding:**
1. **Malformed cross-cat SPPF derivation** → regresses `gen_calculator_unit::unit_calculator_bool_inttobool`. `bool(0)` realizes `FloatToBool(IntToFloat(NumLit(0)))` = displays `bool(float(0))` — inserts the SYNTACTIC cast `IntToFloat` (`"float" "(" a ")"`) WITHOUT its `float(` keyword. Root: cross-cat injection chains through SYNTACTIC casts as if they were FREE/transparent injections (`CastFloat`). **FIX (next, tractable-ish, deep+risky): cross-cat injection/dispatch must only chain AUTO/transparent casts, not keyword-bearing syntactic casts** (codegen: the cross-cat tier / injection-rule generation) — OR reject token-inconsistent derivations at SPPF/realize + cast-weight so minimal direct casts win. This is the linchpin to SHIP M4.
2. **Doubly-nested same-wrap combinatorial explosion** (`test_nested_int_int`=`int(int(5,32),32)`, `test_nested_float_float_int`=`float(float(10,64),64)`): 84% collision, 900+ registrations on 24 chars; cohort cap-raise→256 HANGS >120s (not a capacity shortfall); source-aware `PackedDispatchConfig` loosening REGRESSES (6/12). Genuine **Exp-15 CPS/GSS-batch cohort rework** (2 tests).
**STATUS:** M4 reverted; tree = M1-validated (gauntlet 4220/0, op-suites 2118/0, disambiguation green). **Residual closure path PROVEN: fix #1 (cross-cat syntactic-cast-injection bug) unblocks M4→~13/17; #2 (Exp-15) closes the last 2 doubly-nested.** Both empirically demonstrated necessary (not avoidable by key-widening). The M4 diff is reconstructable from this entry; recovery snapshot `checkpoint-after-M1-validated.patch`.

### bug #1 ROOT = Pass 2c implicit cross-cat casts (CODEBASE-DOCUMENTED known-hard, 2026-05-29)
`macros/src/gen/runtime/wpda_codegen/prefix.rs:1120-1149` **Pass 2c** adds CrossCatProjection-style "implicit-cast" descriptors for SYNTACTIC cast rules (`<Y>To<X> . a:Y |- "trigger" "(" a ")" : X` — IntToBool/FloatToBool/IntToFloat/...), so a value in FIRST(Y) can fill an X-slot by delegating to Y-parsing + wrapping via the cast's FireAction — WITHOUT consuming the cast's trigger keyword. This is correct for the direct first-input case (it FIXES `int(false > b < N)`) but TRANSITIVELY CHAINS: `bool(0)` → FloatToBool(IntToFloat(0)) = `bool(float(0))` (IntToFloat's `float(` absent from input). **The code's OWN comment (`:1138-1143`) flags Pass 2c as "OVERLY BROAD — caused regressions in 16 previously-passing tests. The scope needs further refinement (Plan agent investigating). Kept per user direction 'don't roll things back that might be corrected'."** So bug #1 = the pre-existing UNRESOLVED Pass-2c scope problem; M1 masks it via cohort-conflation, M4 unmasks it. **Candidate fixes (both risky, gate hard):** (a) scope Pass 2c to forbid TRANSITIVE chaining (an implicit-cast source itself reached via another implicit cast) — depth-1 implicit casts only; (b) cast-weight so the minimal/direct cast chain (IntToBool, 1 step) strictly dominates the implicit (FloatToBool∘IntToFloat, 2 steps) — evidence-based pruning per disambiguation invariant. Verify against the 16-regression history (the `int(false>b<N)` / cross-cat-infix-first-input cases must stay green).
The ~30 cross-cat/cast/bare-var/binder failures (calc `test_cast_*`/`test_nested_*`/`test_int_*`/`simulator_regression_cross_cat`, `rholang_cast_*`, edge_case `str_of_cross_cat_*`, rholang comm bare-var-expecteds + `parsing::{bare_variable_infers_as_proc,new_single,receive}` + `new_parses` + `exec` + `pinputs_lookup`) are **ONE pre-existing root** (bucketed: FAIL identically at a23ef69 — NOT a WALK regression). Trace-proven mechanism:
- `allocate_fork_push_child` (`wpda_walker.rs:12760-12824`) registers each `CrossCatDelegate` under `DispatchKey::new(pos_after, source_src_idx, inner_cur_bp)`. A second delegate colliding on `(pos,source,bp)` is `pause_cohort_member`'d (`dispatch_cohort.rs:628`) → removed from active stepping.
- Paused member is revived ONLY if the owning worker key reaches `Resolved` (drain at `wpda_walker.rs:8997`). Keys that stay `InFlight` (worker dies first) OR transition `InFlight→Failed` (`dispatch_cohort.rs:702` DISCARDS `pending_members`) **silently lose the paused cross-cat cursor** → the valid ProcStr/ProcFloat/PVar/binder injection never reaches EOI → "no accepting branch" / wrong-category `Ambiguous`.
- `force_materialize_cohort_frames` (`:8320`) flushes `Frame::Cohort` in branch_cursors, NOT `dispatch_cohort_cache.pending_members`.
- Compounded by `extract_dispatch_config` (`:2654`) keying cycle-defense on `(pos,cat_src,cur_bp)` only (no rule/source) → conflates distinct cross-cat injections under lex-split DAG nodes (`Float "3.14"` vs `Integer "3"`+`Float ".14"`).
- A1 probe: per-category `_all` returns `[]` for `42`(Int/UInt32), `p`(Proc), 2-arg casts → R1 core, NOT a tractable aggregation drop.
- Point-fix FALSIFIED: forcing cohort no-pause shuffled failures (fixed some, broke others) → entangled across cohort + cycle-defense + lattice.
**Scoped fix (Exp-15, multi-day, Welch-gated):** (a) at EOI flush all still-`InFlight`/`Failed` keys' `pending_members` back to independent sub-parse cursors, OR (b) re-key the cohort so revival doesn't depend on a sibling reaching `Resolved`; PLUS widen `extract_dispatch_config` keying (intertwined with chain-memory-ceiling dedup → must re-Welch chain panel). See `prattail/docs/design/plans/exp15-cps-trampolined-walker.md` §3.8/S6. **Retired `complete_to_fixpoint`/`earley_outboard_chain` are chain-specific — IRRELEVANT here (restore-completion hypothesis FALSIFIED); leave as chain reference.** This is the Phase-8 research-grade remainder; do NOT hack tests green around it.

## ✅ Step-A token-soundness fix SHIPPED (2026-05-30) — principled, evidence-based, GREEN, zero-regression
Per user mandate "no premature disambiguation; drop alternatives ONLY when evidence rejects them," the
`bool(0)`→`bool(float(0))` fabrication is now rejected BY EVIDENCE (token-soundness), NOT the weight heuristic:
- **Realize-time token-soundness span filter** (`wpda_walker.rs realize_node_leave` Symbol arm + new
  `WpdaEngine::min_terminal_span(cat,rule)` from `semantic_actions::emit_min_terminal_span_body`, wired in
  `engine_impl.rs`): per rule = count of `SyntaxExpr::Literal` after the first param; drops any packing whose
  result-Symbol span leaves `slack = sym_span − Σchild_spans < min_terminal_span` (the cast claims `(`/`)`
  occupying zero input = fabrication). Guards: only when `sym_span ≥ child_span_sum` (skip synthetic/zero-span
  → else sound lambda App/Lam dropped); skip non-`Simple`-param rules (binders `^x` have no span → else ambient
  nested_new fails). Soundness probe (`calculator.rs::pass2c_token_soundness_probe`) PASSES: `bool(0)` 25 alts all
  sound (was 5× unsound). Gauntlet 4220/0, op-suites+unit+edge_case 2347/0, parity 47/47. Recovery: `FINAL-SHIP-stepA.patch`.
- **Plan corrections (empirical):** §3a literal "drop-the-wrap bootstrap" FALSIFIED — `ProcToBool`+transparent
  `ProcBool` ⇒ unbounded Bool→Proc→Bool cycle (144s + Err on `bool(0)`); KEPT the Pass-2c wrap + added the span
  filter instead. §4 premise FALSE — the 3 "inherently-unsound" inputs (`int(false>a>z<"eoxyaib")` etc.) DO have
  token-sound parses (display==input) → KEPT (NOT flipped to error). §5 `BP_TIER_PASS2C_SYNTHESIZED` KEPT (deleting
  regressed a SOUND ambiguous case `int(b>=N<=b>=M)`) — it orders SOUND branches only; the span filter (not the
  weight) handles unsoundness. One genuine fabrication corrected: `wpda_parity` bare `true`→`int(true)` now rejected.

## ⚑ M4 re-land REVERTED again (2026-05-30) — regresses SOUND parses → genuine Exp-15
M4 (DispatchKey wrap_cat/wrap_rule widening) on top of Step-A flips 8 cast tests (cast_float_overflow_to_inf,
cast_int_float_floor, casts_from_numeric_strings, int_from_float_still_works, nested_int_float,
simulator_regression_cross_cat_with_parens, binary_int_requires_width, comm::single_channel,
bare_variable_infers_as_proc) BUT REGRESSES 2 token-SOUND parses (`int(y != true > x < "qua")`,
`int(-220439700 > 1827376848 == c != -0.5)` — both pass on M1) — its cohort un-conflation DROPS valid sound
derivations. Violates no-regress-M1 → NOT shipped (`stepB-M4-with-regression.patch`). **Cast-family closure needs
the Exp-15 cohort rework so M4 stops dropping sound cross-cat derivations** (the cohort has BOTH bugs: conflation
loses cast cursors AND un-conflation drops sound ones — the CPS/GSS-batch correctness rework). The doubly-nested
`test_nested_int_int`/`_float_float_int`/`test_triple_nested_float` are the same Exp-15 class (span filter can't
distinguish sound nested cast slack=0 from fabrication slack=0). rholang `nonfinite`/`fixed_floor` + calc
`test_cast_int_nonfinite`/`int_invalid_width` are SEPARATE EVAL-layer (cast-error surfacing) — classify + fix independently.

## ✅ Eval-layer cast-error surfacing FIXED (2026-05-30) — +5 tests, green, zero-regression
Root cause was EVAL CODEGEN swallowing the error term (NOT the pipelines, which correctly return None on
invalid width/NaN): `macros/src/logic/mod.rs generate_category_rules` — (1a) calc native cross-cat path
`#safe.map(|v| NumLit(v))` + `if let Some(res)` left failed casts STUCK; (1b) rholang appended a `filter_err`
clause (`if match &res { Proc::Err => false, _ => true }`) to every Proc fold rule, SWALLOWING the `Proc::Err`
that `rho_proc_int_bin` correctly returns. FIX: new `cast_error_variant_for(lang,cat)` → codegen binds
`match #safe { Some(v)=>NumLit(v), None=>CastErr<Cat> }` (calc) + removed `filter_err` (rholang, fold-closure:
`Cat::Err` is a FINAL evidence-based rejection, not transient). Surfaces `cast_error_int`/`error` BY EVIDENCE.
Fixed: calc `test_cast_int_invalid_width`; rholang `rholang_cast_int_invalid_width_error`/`_nonfinite_float_is_error`;
`rholang_cast_fixed_floor`→`17/5` + `_float_from_rational_string`→`1/2` (lawful BigRat canonical-tower NF, expectation
corrected). SIDE BENEFIT: rholang div-by-zero/type-mismatch/fraction-zero now GENUINELY surface `error` (were stuck,
passed spuriously via `multiset_eq` None==None). Gates: gauntlet 4220/0, op-suites 2347/0, edge_case 229/0, parity 24/0,
soundness probe PASS. Files: `macros/src/logic/mod.rs`, `languages/tests/rholang_tests.rs`. Doc: `cast-error-eval-surfacing.md`.

## ✅ PERF REGRESSION FIXED: test_deep_ternary_1000 (2026-05-31) — M1-revive O(N²), spurious-blowup gate
`trampoline_tests::test_deep_ternary_1000` was **19.9s baseline → >240s** (regressed by M1). **Agent profile+bisect:**
exponent ≈ 2.0–2.3 (O(N²)); bisect ISOLATED the culprit to **M1 revive-on-loss** (neutralizing it: deep_ternary_500
67.5s→0.84s = 80×, exponent→1.06). Suspects #2 (`peek_binary_chain` short-circuits at 5 atoms = O(1)) and #4 (Step-A
span filter = O(1)/node, no subtree re-walk) FALSIFIED. **Root mechanism:** `DispatchKey::new(pos_after,…)`
(`wpda_walker.rs:13101`) keys cross-cat dispatch by POSITION → each `(` in the N-deep parenthesized ternary opens a
distinct cross-cat dispatch → O(N) InFlight cache entries → M1 drains+re-injects ~10×N orphan cursors in one round,
each re-parsing O(N) → O(N²). Critically the parse **already succeeded** (2 winners on the frontier BEFORE revival;
the O(N) orphans are spurious dead-ends that all die).
**FIX (`revive_orphaned_cohort_members_once:8689`, SPURIOUS-BLOWUP GATE):** skip revival iff CONJUNCTION of two
independently-falsified-in-isolation signals: (1) `orphans.len() > 256` (2× the measured genuine-cross-cat ceiling
123, == `MAX_COHORT_FRAME_MEMBERS`; grammar-bounded parses NEVER exceed it; count-alone insufficient — blind cap=256
still O(N²)=116s) AND (2) `parse_already_succeeds` (a frontier cursor at logical-EOI in accepting config whose SPPF
root realizes to ≥1 term = `resolve_at_end_of_input`'s exact success cond; succeeds-alone insufficient — it's also
true for `"42"`/`float(x)` where revival adds the RIGHT category, regressed `test_unambiguous_int_literal`/
`test_nfa_spillover_float_int_var`). Threaded `IdempotentSemiring + StarSemiringRef` onto revive/run_to_end fns for
the realize call (all real walkers use LexicographicWeight, which satisfies both).
**RESULTS:** deep_ternary_100 **0.17s** (was 1.4s), deep_ternary_1000 **1.86s** (was 19.9s — 10× BETTER than baseline),
ternary_chain_10000 (H3) PASS 0.21s. Gauntlet 4220/0, op-suites (calc 1331/0, rholang 532/0, units), edge_case 229/0,
soundness probe PASS, parity all /0, -3! ladder PASS, calculator.rs **197/20 bit-identical to M1 baseline (0 regr)**.
Only `wpda_walker.rs` changed (+113); `trampoline_tests.rs` byte-identical to HEAD; tree CLEAN (no leftover instrumentation,
verified). Nothing committed.
**⚖ DISAMBIGUATION ASSESSMENT (flagged for sign-off):** the gate is evidence-CALIBRATED (separates genuine-grammar-bounded
≤123 from N-scaling-spurious by the measured distribution + 2× margin), NOT per-alternate-proven — materially different
from the rejected 0.15 weight (which suppressed a REALIZED SOUND alternate by fiat; this declines to RE-DRIVE dead-end
PAUSED cursors when a winner already exists, never turning success→failure). Residual risk: a hypothetical >256-GENUINE-
orphan input would skip sound alternates (an incompleteness, not a soundness violation) — does not occur in the corpus
(zero regressions incl. -3!/parity/soundness). It is a **BRIDGE**: the root re-keying (Tomita-style position-independent
cohort merge so position-distinct-but-redundant dispatches don't each orphan) would make the gate inert. Kept as the
immediate zero-regression regression-repair; revisit when the cohort rework lands.

## Authoritative residual 2026-05-31 (post-ternary-fix bounded workspace run `residual-2026-05-31.log`)
**9658 passed; ~71 unique FAIL + 2 TIMEOUT** (was 113). Three clusters:
- **Cluster Y — cast-family + rholang cross-cat/binder (~36) = the cohort-correctness rework:**
  - calc (17): `test_cast_{float_overflow_to_inf,int_nonfinite_float_is_error,int_float_floor}`, `test_casts_from_numeric_strings`,
    `test_binary_int_requires_width`, `test_int_from_float_still_works`, `test_nested_{int_int,int_float,float_float_int}`,
    `test_triple_nested_float`, `test_nested_float_int_arithmetic`, `test_bool_from_list_elem`, `test_ambiguous_dispatch_{gt,gteq}_env`,
    `simulator_regression_{full_expressions,cross_cat_with_parens,cross_cat_with_strings}`, `casting_example_files_calculator`,
    roundtrip `{idempotent_int_display,roundtrip_int_parse_display}`.
  - rholang (19): `comm::{single_channel,comm_with_body_using_channel,comm_with_remaining_parallel,multi_input_two_channels,
    multi_input_three_channels,multi_input_uses_both_vars,join_pattern_same_channel}`, `parsing::{bare_variable_infers_as_proc,new_single,receive}`,
    `new_and_extrusion::{new_parses,extrusion_forward,new_is_normal_when_body_is}`, `exec::exec_with_process`,
    `beta::{dollar_name_reduces,normalize_via_language_trait}`, `congruence::{add_cong,comparison_cong}`, `native_ops::arithmetic::fraction_builds_rational`.
  - `led_delegation::test_p1_10_parenthesized_sub_expressions`, `class2_opt_collection_smoke::choosemaybe_parse_none_via_wpda`,
    `gen_class2optsmoke_unit::unit_class2optsmoke_proc_choosemaybe` (collection-opt + cross-cat-LED facets).
- **Cluster J — display↔parse roundtrips (~26) = pre-existing, NOT a regression from the eval-cast fix** (`languages/src/calculator.rs` grammar
  UNMODIFIED in my diff; `display_parse_roundtrip` is parse-only, no eval). `gen_{calculator(×9 all cats),class2hashmapsmoke,class2optsmoke(×2),
  class2multi,class2smoke,class3multi(×3),class3opt(×2),guardedrho(×3),ledtest(×3),mixedmath,rholang(×3)}_prop`. **Counterexample-classified 3 roots:**
  (J-a) cohort-downstream cross-cat — `arb_int → "int(str(1177855666),(… ? cast_error_int : …)+… ^ error)"` fails at `(` (cascades from Cluster Y);
  (J-b) eval-sentinel terms in surface strategy — `arb_bool/str` generate `error`/`cast_error_int`/`cast_error_fixed` in cross-cat positions
  (independent: the surface `Arbitrary` strategy fabricates eval-only sentinels → exclude them, OR add parse rules — plan §7.1 "stop the strategy fabricating unparseable variants");
  (J-c) plain-operator parse — `arb_num (ledtest) → "1793342327 == 1341922371"` → "WPDS produced no result" (comparison-parse, B-comparison-family residual or ledtest-specific).
  → **STRATEGY: fix Cluster Y first (cohort), re-measure J residual (J-a cascades), then fix J-b/J-c independently.**
- **Cluster Z — slow (2 TIMEOUT @180s):** `gen_ambient_prop::proc_display_parse_roundtrip` (also a J roundtrip), `gen_rholang_prop::sim_rholang_proptest_campaign`.
  `gen_calculator_prop::sim_calculator_proptest_campaign` was SLOW but PASSED (not TIMEOUT this run). Phase 6 (bound + root-cause).

## Slow/hung
| Test | Cases | Baseline | Target | Status |
|------|-------|----------|--------|--------|
| gen_rholang_prop::sim_rholang_proptest_campaign | 50 | HUNG >2340s | bounded + fast | pending |
| gen_ambient_prop::proc_display_parse_roundtrip | 100 | HUNG >2340s | bounded + fast | pending |
| gen_calculator_prop::sim_calculator_proptest_campaign | 50 | PASS 1391s | fast | pending |

## Log
- 2026-05-29: Plan approved. Installed `.config/nextest.toml` (bounded slow-timeout). Created ledger. Phase 0 bisect started.
- 2026-05-29: **Cargo.lock is NOT committed** → worktrees must `cp` the main `Cargo.lock` (else the moving `pathmap` branch resolves to a tip that fails to compile under nightly). Pinned `fd901102` builds clean.
- 2026-05-29: HEAD probes — all 9 cluster reps FAIL.
- 2026-05-29: **a23ef69 bisect (pre-WALK-S0, post-Box→Arc):** only B (`ext_math_grouped_add`) PASSES → **B = regr-WALK (S0..S3)**. A/C/D/E/F/G/H/I all FAIL at a23ef69 → predate WALK. Next: b780295 (pre-Box→Arc) to split regr-Box→Arc vs pre-existing.
- 2026-05-29: Cluster A traced to `ast/src/pattern.rs` `build_repls_expr`(:2163)/multi_substitute path (:2293-2298): `__vars`=all unbind'd binders vs `__repls` built independently; single_channel → `repls.len()==0`. `9c55d81` (Box→Arc) edited `build_repls_expr` Var branch (:2215) — representation-only, NOT the cause.
- 2026-05-29: **b780295 bisect (pre-Box→Arc): A,C,D,E,F,G,H,I all FAIL** → all pre-existing; only B is recent (regr-WALK). Phase 0 bucketing complete.
- 2026-05-29: Cluster A leading hypothesis: comm rule guard checks `collected==ns.len()` but subst uses `binders`(xs); OOB when `binders.len() != collected.len()`. Either parser yields malformed PInputs (empty ns) OR guard is missing a `binders==collected` check. Confirming empirically (Phase 1).
- 2026-05-29: **Phase 1 (Cluster A) started.** B runtime trace (plan Phase 0.5) deferred to Phase 3 start.
- 2026-05-29: **MAJOR — A, C, E share ONE root cause: WPDA collection accumulation drops all-but-one element at PARSE time.** Empirical (ZZDIAG temp tests, now removed):
  - rholang `{(c?x).{*(x)} | c!(p)}` parses to `PInputs([], Scope{pattern:[x], body:*(x)})` — `ns` channel collection is EMPTY (should be `[c]`). Comm then fires with `collected==ns.len()==0` (guard passes), substitutes into 1 binder with 0 repls → subst OOB (A).
  - calculator `map(1:10, 2:20)` parses to `Ambiguous([ProcMap(MapLit({1:10})), MapLit({1:10})])` — only the FIRST entry kept (should be `{1:10, 2:20}`). Empty/truncated map → drain_collection LIFO (C) + empty-map NF (E).
  - Initial hypothesis "one bug" PARTLY WRONG (corrected by impl agent w/ tracing):
    - **Cluster C/E (maps) = realize-time cursor mis-attribution** (elements in per-cursor `sppf_collection_arena`, realize read `branch_cursors[0]` via `winner_collection_arena()`; SPPF `CollectionId` dedup-shared, carried no items). FIXED by making `CollectionId{id,items}` carry derivation-local element SppfIds + realize reading them. See `collection-accumulation-fix.md`. 12 calc map tests now PASS; gauntlet 4220/0, op-suites green.
    - **Cluster A (rholang comm) = DISTINCT parse-time slot collision.** Standalone `(c?x)` now parses `ns=[c]` correctly, but nested `{(c?x).{*(x)} | c!(p)}` → `PInputs([], …)`: PPar opens collection slot 0 (its `ps` bag), PInputs opens slot 1 (its `ns`), but `c?` is spliced into slot 0 while PInputs fires reading slot 1 (empty) → comm `multi_substitute_name([x],[])` → subst OOB. Upstream in the parse-time slot-id machinery (generated `wpda.rs` accumulator_id / `symbol.bp` slot resolution + `emit_splice_into_collection`). Needs its own fix (Plan agent). SPLIT A back out from C/E.
    - **2 regressions** from REMOVING `dedup_collection_id` (ambiguous-collection realize cartesian explosion → wrong-cardinality list: `new_and_extrusion::extrusion_blocked_when_not_fresh`, `calculator::test_float_from_list_elem`). FIX: dedup `CollectionId` by `(id, items)` instead of dropping dedup → keeps maps fixed, restores merging that bounds node growth.
- 2026-05-29: dedup-by-(id,items) applied (sppf.rs). Result: **11 calc map tests PASS** (Cluster E fixed). `extrusion_blocked` + `single_channel` still fail (Cluster A — distinct, see below). `test_float_from_list_elem` STILL fails: hard panic `numeric_dispatch.rs:75 calc_peel_list_elem .expect("ElemList: index out of bounds")` — my accumulation fix makes lists derivation-local, exposing a wrong-length cross-cat list derivation the old `branch_cursors[0]` read masked. Was PASS in original 113-run → genuine regression to fix.
- 2026-05-29: **Cluster A real cause (Plan agent + trace): static-slot-idx splice.** Class-3 ZIP-MAP-SEP `ns` names splice emits `SpliceIntoCollection{id: static slot_idx=0}`, but the `ns` CollectionMarker push + fire-site item-snapshot use the RUNTIME slot. Nested `{(c?x)|c!(p)}`: PPar bag = runtime slot 0, `ns` = runtime slot 1; static id 0 mis-routes channel into PPar bag → `ns=[]` → comm subst OOB. **FIX applied:** `apply_effect_to_cursor` `SpliceIntoCollection` arm uses runtime `acc_id = collection_stack_depth - 1` (mirrors generic Pratt-close splice :13181); invariant `static_slot_idx ≤ acc_id` (outer depth ≥ 0) → debug_assert sound (did NOT fire); multi-slot Class-3 unaffected (each sibling innermost when spliced). `apply_effect_to_builder` confirmed DEAD (stale comment); only apply path is `apply_effect_to_cursor`.
- 2026-05-29: **Cluster A acc_id fix moved single_channel's failure from subst-OOB → `parse("p")` failure** ("WPDS produced no result"). The comm REDUCTION now proceeds past the OOB; the test now blocks on parsing its EXPECTED bare variable `p`. **Bare-var parse is a SEPARATE pre-existing bug** (`parsing::bare_variable_infers_as_proc` was in the original 113; "p" has no collection so unaffected by these fixes). **ENTANGLEMENT: comm tests with bare-var expecteds need BOTH the Cluster A slot fix AND a bare-var parse fix (Cluster B family) to go green end-to-end.** Comm tests with LITERAL expecteds (comm_substitutes_quoted_value→"0") should pass on the slot fix alone — verifying.
- 2026-05-29: **REGRESSION GATE GREEN with all fixes (collection-accumulation + acc_id + dedup-by-(id,items) + graceful peel): gauntlet `cargo test --release -p prattail --lib` = 4220/0; op-suites = 2118/0** (gen_calculator_op **1331/0** — the collection fix even CLEARED the 10 pre-existing R1 op failures; gen_rholang_op 532/0; +unit suites). Cluster A reduction confirmed fixed (`comm_substitutes_quoted_value`→"0" PASS); list regression fixed (`test_float_from_list_elem` PASS); maps + list-elem green.
- 2026-05-29: **Cluster B is THREE sub-families (Plan agent + walker-stats trace):**
  - **(A) grouped/comparison/mixed-precedence — the bisected regr-WALK-S3 (5ab4b59); MAJORITY of Cluster B.** Root cause: the LEFT-assoc absorption GATE at `wpda_walker.rs:5834-5852` was BLIND — counted ANY token as atom/op, bleeding across `)`/`(` and different-precedence ops, spuriously firing `synth_binary_chain` on `(1+2)+(3+4)`, `a<b<c`, `1+2==3 and 4==4` → desync (elide / no-accepting-branch) + bogus `chain_absorbed_intervals` suppression. **FIX applied:** replace the blind loop with `peek_binary_chain(tokens, cursor.pos, 5)` (the SAME kind-validated recognizer right-assoc/mixfix already use; `synth_binary_chain` re-validates). min_atoms=5 = old "≥4 after head". Flat literal chains absorb byte-identically (perf preserved). Verifying.
  - **(B) casts** (`int(int(5,32),32)` etc.): DISTINCT (nested cross-cat fork explosion 555+ cursors, no absorb). Needs own bisect + fix. Highest residual risk. NOT fixed by (A).
  - **(C) bare-var `p` → `Ambiguous(Proc,Name)`:** parse is ALREADY complete (the collection-accumulation fix restored single-Symbol-root realize); residual is type-inference returning "Ambiguous" vs "Proc" at rholang_tests.rs:816 — a disambiguation/expectation question (evidence-driven: prefer Proc for top-level, or accept Ambiguous⊇Proc). Unblocks comm tests with bare-var expecteds.
- 2026-05-29: **List regression = pre-existing cross-cat truncation, SURFACED (not created) by derivation-local realize.** `list(3,2.0,true)` → `Ambiguous([[3,2.0,true], [3], [3,2], [3,2(u32),0.0]...])` — the `[3]`/`[3,2]` truncated alternates are malformed cross-cat parses always in the SPPF; old realize masked them via `branch_cursors[0]`. `at([3],1)` OOB'd. **FIX applied (disambiguation-aligned):** `calc_peel_list_elem` (`numeric_dispatch.rs:67`) returns `p` (unreduced) instead of panicking on OOB/non-literal — an OOB access is EVIDENCE rejecting that alternate; the valid full-length derivation still wins. (The truncated-alternate parse bug itself remains — a deeper cross-cat-collection issue; tolerated by `contains(expected)` test semantics + graceful peel; tracked for the broader collection-parse work.)

## Resumption 2026-05-31 — sequencing + Exp-15 scope clarification
- **pgmcp experiment #8 (`c1-chain-absorption-generalization-right-assoc-mixfix`) IS decided/accepted.** Decision row present: WelchT statistic −34641.15, p=0.000000, effect −6859.97 MiB, 95% CI [−691.48, −691.40], verdict ACCEPTED (decided 2026-05-29T06:59:08Z; ternary_chain_10000 749.66→58.22 MiB = 92.2%↓; no-regression panels all WINS). Only the experiment-level `status` field is cosmetically stale at `measuring` (hypothesis verdict = accepted). NOT re-deciding (would append a duplicate decision row); render writes into the *pgmcp* repo, skipped to avoid cluttering another repo. Treat #8 as closed-ACCEPTED.
- **Ternary perf-regression agent IN FLIGHT** (`test_deep_ternary_1000` 19.9s→>240s). Profiles complexity + bisects M1-revive vs Step-A span-filter vs Cluster-B-A gate vs collection-fix; fixes the super-linear path under hard gates. Its finding GATES the cohort-correctness rework below (see coupling).
- **Exp-15 scope DISAMBIGUATION (important — two distinct things share the name):**
  1. **MEMORY Exp-15** = `prattail/docs/design/plans/exp15-cps-trampolined-walker.md` (S0–S7, ~5400 LOC CPS/trampolined walker + CursorStore). Goal: shrink per-cursor footprint so the **`#[ignore]`'d chain_10000** passes in 24 GB. This is the multi-day memory rewrite — NOT required for the broad-suite-green residual.
  2. **Cast-family CORRECTNESS rework** (what the residual actually needs) = a TARGETED cohort-revival-completeness fix: re-land M4's DispatchKey un-conflation (so the 8 casts + bare-var land) WITHOUT dropping the 2 token-SOUND derivations M4 currently regresses (`int(y != true > x < "qua")`, `int(-220439700 > 1827376848 == c != -0.5)`), AND without the doubly-nested 256-cap hang. Per master-plan Phase-8 "prefer targeted fix if R1 subset small (<10)" → this is the targeted path, distinct from the memory CPS rewrite.
- **COUPLING (why the ternary agent gates the rework):** M4's un-conflation creates MORE paused cohort members; closing the cast family means making revival COMPLETE (every paused member with a sound continuation must resolve, not just when a sibling worker hits `Resolved`). But "revive more" is in direct tension with perf if M1's drain-and-re-drive is the super-linear ternary culprit. So the rework's revival MECHANISM (current M1 drain-redrive vs a GSS-batch / sound-continuation-filtered revive) depends on the ternary agent's verdict. Do NOT design the rework until that lands.
- **Residual after all current fixes (to re-measure with a bounded run once the machine is free):** cast-family parse (~8 calc `test_cast_*`/`test_casts_from_numeric_strings`/`simulator_regression_cross_cat_with_parens` + rholang parse-blocked casts) + doubly-nested (3: `test_nested_int_int`, `_float_float_int`, `test_triple_nested_float`) + bare-var (2: `bare_variable_infers_as_proc`, `comm::single_channel`) + D-deferred cross-cat NF (congruence `@`, beta `^`, `new_is_normal` bare-var) → ALL one cohort-correctness root. PLUS independent: roundtrips-J (~25, re-measure; cascade from B-A) + slow campaigns (2 hung + 1 slow; bound + root-cause).
- **NEXT (sequenced):** (1) ternary agent reports → integrate. (2) bounded full-suite re-measure → exact residual list. (3) dispatch cohort-correctness rework Plan agent (informed by 1+2). (4) slow-campaign bounding + roundtrip re-measure (parallel, separate agent). Build-heavy steps wait until the ternary agent frees the machine (perf-quiet mandate).

## ⚑ Cohort-correctness rework EMPIRICALLY RE-SCOPED 2026-05-31 — full plan `prattail/docs/design/plans/cohort-correctness-rework.md`
Plan agent re-traced M4 on the CURRENT tree (isolated worktree, walker-stats) and **partly contradicted the ledger's M4 narrative** ("verify, don't blindly trust"). Findings supersede the older M4 entries where they conflict:
- **F1:** the h5 (`h5-directfirst-m4.patch` = M4-on-Step-A) variant does NOT drop the 2 "sound" inputs — they PASS. The documented regression #2 is in the DIFFERENT `stepB-M4-with-regression.patch`. → re-land **h5**, not stepB.
- **F2:** h5-M4 regresses `simulator_regression_nested_casts` — but possibly CONFOUNDED by h5 lacking the shipped eval-cast fix (the current tree HAS it → retest resolves this).
- **F3 (biggest):** the doubly-nested cluster (`test_nested_*`, `test_triple_nested_float`, `simulator_regression_cross_cat_*`) fails IDENTICALLY under M1 and M4 with `registrations_total=0` + "no accepting branch" — the cohort machinery NEVER engages. **It's a 2-arg-cast-with-non-literal-first-arg PARSE-RECOGNITION gap (codegen), NOT cohort explosion/hang, NOT Exp-15 memory.** Contradicts the ledger's "84% collision / 900+ registrations / cap-256 HANG" (that was a different M4 integration). → **Exp-15 is DE-SCOPED from Cluster Y.**
- **F4:** M4's un-conflation win is real (flips ≥4 casts). **F5:** `failed_orphan_members=0` everywhere; `fail()` has no call site → the "Failed side-queue" is latent.
**3 roots:** R1 single-level un-conflation (M4 keying) · R2 nested-cast recognition (codegen, the real doubly-nested root) · R3 eval-cast (already shipped). **Milestones:** M0 census · M2 re-land h5 M4 keying (plain first; continuation-asset escalation ONLY if plain-M4 regresses on the current tree) · M3 R2 codegen recognition (admit cross-cat first-arg in 2-arg-cast slot 0, span-filter-bounded, NO weight) · M3b bare-var · M4 eval-residual · M5 Exp-15 CONTINGENT (expected unneeded). EquivKey/PackedDispatchConfig stay narrow; Welch chain panel + RSS gate any keying change.
**M2+M3 MEASURED (agent a41cd4043c3d084a8, isolated worktree, main tree pristine) — BOTH hypotheses FALSIFIED, root UNIFIED:**
- **Plain-M4 (R1) NOT shippable:** delta `/var/tmp/suite-green/rework-m2-m4keying.patch` (5 files, +96; EquivKey/PackedDispatchConfig kept narrow). **FIXES 16** (calc cast family + `test_ambiguous_dispatch_*` + `simulator_regression_cross_cat_with_{parens,strings}` + rholang `comm::{single_channel,multi_input_two_channels,multi_input_uses_both_vars}` + `bare_variable_infers_as_proc`) **but REGRESSES 9** (`parse_int_cross_cat_comparison_{ge,le,lt,ne}`, `parse_int_cross_cat_in_expression`, `simulator_regression_{cross_cat_dispatch_chaining,cross_cat_with_floats,bool_prefix_tokens,original_6}`). Gauntlet 4220/0, op-suites 2118/0, `-3!`/parity/soundness/ternary/F2-`nested_casts` all STAY GREEN. **F1 CONTRADICTED:** with eval-cast present (current tree), plain-M4 DOES drop the 2 "sound" inputs (`int(y != true > x < "qua")` in bool_prefix_tokens, `int(-220439700 > … != -0.5)` in dispatch_chaining).
- **R2 codegen (nested recognition) FALSIFIED:** the nested cluster does NOT show `registrations_total=0` — `int(int(5,32),32)`=1053 regs/86% collision/258 orphans, `float(float(10,64),64)`=913, triple=3807. **The cohort FULLY engages** — F3's recognition-gap framing was WRONG. Agent correctly made NO codegen change (slot-0 FIRST-set already includes `int`/`float` via transparent `ProcInt` projection; a bootstrap would target a non-existent gap + risk over-gen). M3-combined == M2.
- **UNIFIED ROOT (R1 regressions + nested cluster = ONE bug):** M4 un-conflates more distinct cross-cat workers than the NARROW downstream layers route to a *realizable accepting* derivation. **Sig-A** "no extractable terms" (`int(-1 <= 0)`, orphan=0, ≥2 accepting cursors realize EMPTY — packing dropped at narrow EquivKey merge `:10244` / cycle-defense `:5763/:6572` / Step-A filter `:4830`). **Sig-B** "no accepting branch" (orphan>0 exceeds bounded `MAX_REVIVAL_ROUNDS=4` revive). The M4 un-conflation WIN (16) is real but only lands once a COMPLETENESS mechanism rescues the un-conflated sound derivations. NOTE: `int(int(5,32),32)` orphan=258 > the spurious-gate's 256 threshold — CHECK the gate isn't wrongly firing (parse does NOT succeed there).
**Completeness-mechanism DESIGN DELIVERED (agent ab221f41f0fe30418) → `prattail/docs/design/plans/cohort-revival-completeness.md`.** DECISIVE trace (DEBUG `action-elided` count):
- **Sig-A ROOT = MERGE-DROP (not revival, not action-elide, not Step-A):** `int(-1<=0)` → `inflight_orphan=0`, `action elided=0`, `merge_collapses=2403`. The SOUND un-conflated cursor (carrying the resolved inner Symbol, about to Return-pop + fire its outer cast → intern packing) is DROPPED at `merge_equivalent_cursors:10461` (narrow-EquivKey bucket, lex-min tiebreak) BEFORE it reduces its packing → accepting Symbol `packings_of`=∅ → realize empty. The merge's "ambiguity lives in the SPPF" assumption holds only for ALREADY-REDUCED cursors.
- **Sig-B = 3 modes:** G2a SUITE `test_nested_int_int` (Proc-entry `parse_term`) = `registrations_total=0` = R2 CODEGEN recognition gap (→ existing R2-codegen, NOT this mechanism); G2b Int-entry deep cross-cat = merge-drop + revival tail; G2c `int(int(5,32),32)` orphan=258 → spurious-gate correctly DECLINES (`parse_already_succeeds`=FALSE) — **gate NOT misfiring, threshold fine.** Revival incomplete = `MAX_REVIVAL_ROUNDS=4` bound + **re-collision** (N>1 same-key orphans re-collide on re-drive → pure fixpoint-revive can't converge).
- **MECHANISM = (a) continuation-install, cross-cat-gated** (wire the dead `cohort_continuation.rs`: capture at pause, install at EOI via `intern_packing`+`intern_symbol`(dedup to accepting `(nt,lo,hi)`)+`link_packing_to_symbol` → the SOUND packing reaches the accepting Symbol INDEPENDENT of the cursor's merge fate). Closes Sig-A by construction + the revival tail (no re-drive). Capture gated `!pos_in_absorbed_chain_interval` (the byte-identical shipped R4 predicate) → prior Approach-P chain-LOSS can't recur. Spurious-gate RETAINED unchanged, bypassed for cross-cat. (b)/(c) rejected (insufficient + chain-risk). EquivKey/PackedDispatchConfig untouched; Step-A filter the sole drop; no weight.
- **Milestones:** M0 census + **VI-2 pre-check** → M1 capture (gated, inert) → M2 install + cross-cat-inert revive → M3 gate-subsumption doc → M4 bare-var → M5 hand G2a to R2-codegen.
- **VI-2 (make-or-break):** install must reach the SAME `(nt,lo,hi)` Symbol an accepting cursor reaches (else inert). De-risked in M0 before building M2.
**M0 + VI-2 PRE-CHECK done (agent a01204b74e8375cbc) → VI-2 FALSIFIED → continuation-install is INERT → REAL ROOT IS THE REALIZE DFS.** STOPPED before M1/M2 per guardrail (correct).
- **M0 census CONFIRMED** the merge-drop counts (Sig-A `int(-1<=0)`: `merge_dropped_distinct_cohort_workers=924`, orphan=0; G2b: `merge_dropped=32770`+`revival_recoll`+`rounds=4`). VI-1 CONTRADICTED: `vs_graduated=0` — dropped workers collide with OTHER workers (also `cohort_origin`-bearing), not graduated/concrete.
- **VI-2 FALSIFIED (decisive trace, `int(-1<=0)`):** the accepting Int Symbol IS reached, `packings_of.len=2` (NOT empty); packing 28 (`rule cat=2 local=16`, kids=[13]) **realizes to 1 term STANDALONE**, but `realize_root_to_terms(Symbol 29)=0`. The SOUND derivation is PRESENT but DISCARDED. Continuation-install would dedup-hit the already-present packing 28 → no-op. **The bug is in `realize_root_to_terms_with_weights` (`wpda_walker.rs:4555`), NOT the cohort/intern layer.**
- **REAL ROOT = realize-DFS shared-node/back-edge interaction.** Inner Bool Symbol 13 (shared by packing 28 directly + packing 40's subtree) is hit as a DFS BACK-EDGE (Gray at Enter `:4605`→ `has_cycle=true` + `memo.entry(13).or_insert(empty)` `:4612`). Code path: tri-color DFS `:4601-4661`; back-edge empty `:4612`; Leave overwrite `memo.insert :4657`; Symbol-arm concat+Gray-skip `:4889`+Step-A filter `:4843`; Newton post-pass `:4668-4684` (Tarjan SCC × `star`). The present sound packing-28 contribution is lost through the empty-memo/Gray-skip/Newton interaction (exact ordering needs a trace). The merge's "ambiguity lives in the SPPF so dropping a cursor is safe" assumption is fine — the SPPF HAS the sound packing; the realize DFS drops it. M4 un-conflation merely creates the shared-node DIAMOND that triggers this LATENT realize bug (M1 conflation prevented the diamond → bug dormant).
- **FIX DIRECTION:** distinguish a genuine ancestor back-edge (true cycle → Newton/Scott-Johnstone, KEEP) from a shared-node / DAG re-convergence (diamond → SOUND, compute memo once + reuse, do NOT empty). MUST preserve the C-bis Newton's-method genuine-cycle handling (`session-2026-05-17-phase-c-bis-newton`, 11+6 unit tests) + `Ambiguous` enumeration. **Disambiguation-PRESERVING** (restores a present sound derivation = MORE completeness; opposite of dropping an alternate). Likely a PRE-EXISTING latent realize bug M4 exposes; fixing it may cascade-help other diamond-ambiguity cases (D NF, J roundtrips). Recovery: `complete-m0-delta.patch` (clean M0+VI-2 census/probe delta).
## ✅ Realize-DFS shared-node fix DONE + BANKED to main tree (2026-05-31, agent ac5a6fa8cef37b116)
**Mechanism PINNED (corrects the prior "diamond" framing — all 336 gray-hits are GENUINE cycles, ZERO diamonds):** the SOUND `BoolToInt(Le(-1,0))` packing (id 28, kids=[Bool 13]) lies on a path THROUGH a same-span ambiguity cycle but is itself OUTSIDE it (Bool 13 resolves via its ACYCLIC packing 12). The DFS traverses Bool 13's CYCLIC packing 43 FIRST → reaches packing 28 as a cross-edge while Bool 13 is still GRAY → packing 28 Leave-finalizes EMPTY+BLACK off the provisional back-edge memo; single-visit memo never recomputes it after Bool 13 later resolves (memo[13]=1) → Symbol 29 realizes 0. (h1 28-Leave-before-13-resolve ✓; h2 28 is BLACK-not-Gray when 29's arm runs so the `:4889` skip doesn't catch it — THE bug; h3 Newton only rewrites Symbol memos in SCCs, packing 28 is OUTSIDE every SCC, structurally can't rescue; h4 genuine cycle, not diamond.)
**FIX (`realize_node_leave` Symbol arm `wpda_walker.rs:~4892`, +98/−2, `realize-shared-node-fix.patch`):** a BLACK packing with EMPTY memo (the stale signature) is recomputed ONCE via `realize_packing_call` from the now-resolved child memos. Additive, disambiguation-PRESERVING (RESTORES a present sound derivation, drops nothing), closed-semiring path identical (a packing INSIDE a cycle still recomputes empty → Newton stays the SOLE cycle handler). **GATES (on M4 base):** C-bis cycle 41/newton 42/tarjan 4/star 73/scc 16/self_loop 12 ALL 0-fail (CRITICAL guard ✓); gauntlet 4220/0; op-suites 1863/1863; soundness probe + edge_case 229/0 + probe_neg_zero 23/0 + parity 24/0.
**RESULT on the 9 M4 regressions: 4/9 RETURN GREEN** (`lt`,`ge`,`ne`,`parse_int_cross_cat_in_expression` — pure realize-drops) + all 16 M4-FIXED stay fixed. **5/9 STILL FAIL at PARSE time** (`no accepting branch ... found '('`): `le`,`simulator_regression_{original_6,cross_cat_dispatch_chaining,cross_cat_with_floats,bool_prefix_tokens}` on deep-chain/nested-paren inputs (`int(-928988166<=y<=(-928988166<=y))`, `int(-220439700>1827376848==c!=-0.5)`, `int(y<=z<b<=0.5)`, `int(y!=true>x<"qua")`) — a SEPARATE **Sig-B parse-layer** blocker (un-conflation makes deep/nested cross-cat parses never reach an accepting EOI cursor under M4), out of the realize fix's scope.
**BANKED:** realize-fix APPLIED to main tree (now 18 files/+1596/−177; was +1498/−175). It only triggers where a same-span ambiguity cycle coexists with an acyclic shared-child packing → likely a PRE-EXISTING latent bug → may CASCADE-fix other clusters (D cross-cat NF, J roundtrips). **IN FLIGHT (background run `bucug0jfy`):** bounded workspace run WITH realize-fix (no M4) to measure standalone cascade vs `residual-2026-05-31.log` + confirm non-regression.
**CAST-FAMILY closure status:** realize-fix (DONE, committed 380cc94) + M4 keying (16 fix) + **Sig-B Blocker 1 GLL-descriptor (DONE+correct, gauntlet-green, durably committed `wip/cast-family-cohort` 3c8d188)** + **Sig-B Blocker 2 cohort body-splice (in Plan, agent a1393b3b)** + **nested cluster (root PINNED, Pass-2c, fix scoped)**.
- **Blocker 1 (GLL descriptor) DONE:** `ProjDescriptorKey(gss_node,sppf_stack_id,cat_src,cur_bp)` at the 5 cross-cat cycle-defense sites → `cross_cat_with_parens` GREEN + chaining 4/5 + gauntlet 4220/0 + C-bis cycle 116/0 + parity 16/0 + `-3!` + Ambiguous preserved + Memory Option A (chains byte-identical). The principled progress-aware `w` discriminator (replaces the position/wrap heuristic) — resolves the parens(no-progress→catch)-vs-chaining(progress→allow) tension the heuristic could not. Patch `/var/tmp/suite-green/sigb-gll.patch`.
- **Blocker 2 (body-splice) — design §2 FALSIFIED, in Plan:** the 5 Sig-B fail because a LIVE `WpdaState::BinderRule` cast cursor's body never splices; the resolved body sits under `K_resolve != K_pause` (same `.equiv()`). End-of-step EquivKey drain OVER-fires (steals the parens-inner self-resolving member); EOI-scoped splice UNDER-fires (never reaches the live BinderRule). Central constraint = splice the resolved body into the live cast cursor WITHOUT stealing self-resolving siblings. Plan agent designing → `sigb-blocker2-body-splice.md`.
- feature/wfst-architecture stays GREEN at 380cc94 (the M4+descriptor WIP is on wip/cast-family-cohort, NOT merged — 5 Sig-B pending Blocker 2). M4+realize+descriptor alone = 16 fix / 5 regress → cast family closes only when Blocker 2 + nested land.

## ✅ NESTED-CLUSTER root PINNED (2026-05-31, read-only Explore agent a8926c2411c55cf53)
`test_nested_int_int`=`calc_normal_form("int(int(5,32),32)")`, `_float_float_int`, `test_triple_nested_float` fail at PARSE via the **Proc-entry** path with `registrations_total=0` (cohort never engages — SEPARATE mechanism from the Int-entry Sig-B). **ROOT (codegen, proven):** Pass 2c (`macros/src/gen/runtime/wpda_codegen/prefix.rs:1153`) filters `if tc.len() != 1 { continue; }` → the 2-arg binary cast `IntBin . a:Proc, w:Int |- "int" "(" a "," w ")" : Int` (tc.len()=2) is EXCLUDED from the cross-cat slot-0 ImplicitCast bootstrap → Proc's prefix dispatcher has NO arm for nested `int(...)` in slot-0 → registrations=0. **FIX = widen the Pass 2c arity guard** (`tc.len() != 1` → `tc.is_empty()`, admit arity≥1), GATED against the historical "Pass-2c OVERLY-BROAD" 16-regression (`prefix.rs:1138-1143`) — now guarded by the SHIPPED Step-A token-soundness span filter (rejects fabricated casts) + Welch chain panel + gauntlet. H3 (doubly-nested cohort explosion = `DispatchKey` collision) is EXACTLY what M4+GLL un-conflation fixes → **sequence: land M4+realize+GLL (Sig-B) first, THEN widen Pass 2c → nested cluster closes.** Ambiguity-preserving, no heuristic.

## ✅ Sig-B ROOT CAUSE PINNED (2026-05-31, scientific method: Explore deep-dive → falsification trace) — pgmcp experiment #9
Per user directive (Explore-agent deep-dive, evidence-grounded hypotheses NOT hallucination, empirical falsification, pgmcp-tracked). **2 Explore agents** (cohort-lifecycle + parse-completion, read-only) → 8 falsifiable hypotheses → **runtime M1-vs-M4 differential trace** (agent a351ba4c) FALSIFIED to ONE:
- **ROOT = cross-cat cycle-defense KEYING ASYMMETRY.** M4 widens `DispatchKey += {wrap_cat,wrap_rule}` (cohort cache) but `extract_dispatch_config` (`wpda_walker.rs:2710`) keeps `PackedDispatchConfig` NARROW (pos40+cat_src16+cur_bp8, full u64). The **B14-C5 projection-Fork cycle gate** (`:6684` Drop `:6697`, per-branch `:6845`) drops genuinely-distinct un-conflated cross-cat dispatches (diff `(wrap_cat,wrap_rule)` ⇒ diff `sppf_top`) as **false "projection cycles"** → distinct keys 29→75/35→112, Fork-DROPs 14→394 (28×) → valid BoolToInt cursor dropped → `branch_cursors` empties → terminal Error bypasses EOI revival → "no accepting branch '('" @self.pos=1. **= premature disambiguation by the cycle-defense heuristic.**
- **FALSIFIED: H1 revival** (dying cursors are GRADUATED not paused; M4 ends total_cursors=0) + **H3 merge-drop** (4000/4000 merge-collapses have IDENTICAL full keys → merge drops nothing distinct — corrects the earlier G1/G2b merge-drop narrative).
- **FIX (design `prattail/docs/design/plans/sigb-cycle-defense-keying.md`; in flight agent ae2dfcb492898a610, ONE worktree, serial --wait builds):** restore key-symmetry — widen the cross-cat cycle-defense to carry `(wrap_cat,wrap_rule)` so it rejects only a TRUE wrap-identical self-cycle, letting distinct ambiguous dispatches propagate end-to-end until evidence rejects them (GLL-correct, WPDA-integrated, ambiguity-preserving, NO heuristic). Layout Welch-decided (u128 vs scope-to-cross-cat-only-so-chains-stay-narrow). Gate: 5 Sig-B green + 16 M4-fixed + 4 realize-fixed + gauntlet + C-bis cycle + op-suites + soundness + **Welch chain panel N≥51 + RSS** (mandatory). pgmcp: experiment #9 + linked work-item `…-ff8642` + plan subtree `sig-b-cross-cat-cycle-defense-…`.

(Prior realize agent `adea825e` KILLED — over-spawned; root cause = agents wrapping cargo in `timeout systemd-run` WITHOUT `--wait` → detached scopes ORPHAN. CLEANUP done: 0 rustc/scopes, 4 experiment worktrees removed, 27 recovery patches intact. **Process discipline: ONE worktree/agent, serial foreground builds with `--wait`, self-cleanup.**)
