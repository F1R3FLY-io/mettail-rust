\* ROOT B+C unified Fix A — the BUG WITNESS harness. This configuration runs the
\* CURRENT cohort-broadcast model (BinderForkBroadcast scenario), where a Fork
\* projection survivor in a binder frame is reconciled against the WORKER's frame
\* and dropped. It MUST violate NoValidParseLost — the valid binder-frame parse
\* is lost — demonstrating the fix is necessary and non-vacuous. The matching
\* FIXED model (inline singleton pop) is the CollectionFork scenario, which holds
\* NoValidParseLost. FV: ForkSurvivorBinderPop.current_broadcast_drops_matching_rule.

INIT Init
NEXT Next

CONSTANTS
  Scenario = "BinderForkBroadcast"
  WrapObservable = FALSE

INVARIANT NoValidParseLost
