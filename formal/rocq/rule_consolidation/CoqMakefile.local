# Included by the generated CoqMakefile; keeps explicit
# `make -f CoqMakefile` invocations under the formal RSS cap policy.

FORMAL_CAPPED_TARGET_HINT ?= rocq-rule-consolidation
FORMAL_CLEAN_GOALS ?= clean cleanall archclean
include ../../capped.mk
