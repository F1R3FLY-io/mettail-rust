# Rocq project for the MeTTaIL <-> f1r3node-rust bridge (mettail-rho-*) obligations.
#
#   BridgeInertness — the STRICTLY ONE-WAY dependency invariant the bridge crates
#     embody and the f1r3node guard test `mettail_rust_is_not_a_cargo_dependency`
#     enforces: MeTTaIL may depend on f1r3node-rust, f1r3node NEVER on MeTTaIL,
#     and the dependency graph is acyclic. (M-RHO.0.0 inert milestone.)
#
# Zero-Admitted / zero-Axiom (the project gate).

-Q theories RhoBridge
# Stage 5 CAPSTONE (iii) thread — build-wiring OPTION (a): the cross-project load path
# so a RhoBridge theory (WholeGsltInRhoOpCorrespondenceOptimalViaSameClts) can
# `From AdvancedAutomata Require Import InRhoSameCLTSWeakBisim` and discharge the
# capstone's matching_locus_* hypotheses LITERALLY from the landed obligation (iii)
# same_clts_weak_bisim. advanced_automata is built before rho_bridge in the Makefile
# `rocq-support` prerequisite order (rocq-advanced-automata precedes rocq-rho-bridge),
# so its .vo are present. The main harness WholeGsltInRhoOpCorrespondence.v itself
# carries NO cross-project import (it threads (iii) as cited Section hypotheses).
-Q ../advanced_automata/theories AdvancedAutomata

theories/BridgeInertness.v
theories/HostRhoMachineReuse.v
theories/MettaGsltPresentation.v
theories/MettaFundingLawsConformance.v
theories/RhoLoweringTotalOrRejects.v
theories/RhoScalarOperatorTyping.v
theories/RhoRejectedCoverage.v
theories/RhoDefaultBackendAudit.v
theories/RhoHostObligationBoundary.v
theories/RhoParWellFormedness.v
theories/RhoArtifactBoundary.v
theories/RhoAstSendBoundary.v
theories/RhocalcAstLowering.v
theories/RuntimeBackendDispatch.v
theories/SimulationReportBoundary.v
theories/CommReductionCorrespondence.v
theories/RhoCommScheduleFamily.v
theories/LinearCommCorrespondence.v
theories/EndToEndCommCorrespondence.v
theories/RhoGroundingAndNames.v
theories/RhoObservationFingerprint.v
theories/AmbiguityWitnessEnumeration.v
theories/RhoCallByNeedObservation.v
theories/RhoCallByNeedBudget.v
theories/DeltaOneMinCostJoin.v
theories/DeltaOneMinCostMatching.v
theories/GuardedCommSoundness.v
theories/AmbiguitySetPreservation.v
theories/RhoCostAxisSeparation.v
theories/RhoEscrowSettlement.v
theories/RhoPurseDeterminism.v
theories/RhoBackendFlipGate.v
theories/RhoPlannedExecutionBoundary.v
theories/RhoObservationReportBoundary.v
theories/RhoRuntimeBackendReportBridge.v
theories/DovetailLanguageBackendWrapper.v
theories/RhoLanguageBackendWrapper.v
theories/DovetailRhoLanguageBackendWrapper.v
theories/GeneratedLanguageInstallation.v
theories/OracleQuotientEquivalence.v
theories/RhoGuardedCommSoundness.v
theories/HeldFoldContractSound.v
# FV (ix) — the Stage 3 in-Rho matching encoder is total-or-reject + persistence-preserving
# (InRhoEncoderTotalOrReject): the encoder partitions every rewrite into a matched-in-Rho
# automaton entry or a reasoned deferral (total/sound/disjoint/count), the capability gate
# admits in-Rho matching iff every fired rule is matchable, and appending the transient
# (network + spread) per-firing call preserves the installed σ-receivers' persistent-input
# count. Companion, at the in-Rho encoder, to RhoLoweringTotalOrRejects's install boundary.
theories/InRhoEncoderTotalOrReject.v
# FV (iv)+(vi) — Stage 2 in-Rho non-linear eq: consistency (an INSTANCE of GuardedCommSoundness's
# guarded_attempt): AtomicFiringNoPartialMatch (the guarded join is all-or-nothing, no partial
# consume; accept atomic after the verdict) + NonLinearEqConsistency (commit <-> all-k name-equal,
# reject-safe = merge_substs->None, no-fabrication, oracle-agreement).
theories/AtomicFiringNoPartialMatch.v
theories/NonLinearEqConsistency.v
# FV (AC-nl) — Stage AC non-linear AC matching: a repeated var's occurrences are the AC selection
# values at its slot positions; the consistency guard is all_equal over them = vi COMPOSED WITH the
# slot-gather. Inherits vi's commit<->name-equality + reject-safety; two-slot x=x' characterization.
theories/AcNonLinearConsistency.v
# Stage 3b — the canonical single-receive Rholang COMMUNICATION rule fires as ONE non-linear AC COMM
# (CommRuleFiring): the AC 2-pick selects the PFor receive + POutput send, the Receive.condition
# `EEq(N_recv, N_send)` = ac_nl_guard [0;1] commits IFF the shared channel N is name-equal
# (reject-safe otherwise), the host reduct cont[Q/y] is forwarded (no fabrication), and the fired
# bag is {reduct, ...rest}. Specializes AcNonLinearConsistency (itself an instance of
# NonLinearEqConsistency / GuardedCommSoundness) to the two-element Comm selection.
theories/CommRuleFiring.v
# Stage 3d — the Ambient-calculus OpenRule fires as ONE STRUCTURAL non-linear AC COMM
# (AmbientOpenFiring): the AC 2-pick selects the POpen capability + PAmb ambient, the
# Receive.condition `EEq(N_open, N_amb)` = ac_nl_guard [0;1] commits IFF the shared ambient name N
# is name-equal (reject-safe otherwise), and — UNLIKE Comm — the reducts [P;Q] are STRUCTURAL
# projections of the selected elements (LHS-element args the σ carries, not a host substitution),
# atomically spliced with `...rest` into {P, Q, ...rest} (`insert_all`, the m-reduct generalization
# of Comm's single `insert_exact`). Specializes AcNonLinearConsistency's guard to the two-element
# OpenRule selection; generalizes CommRuleFiring's single-reduct emit to m structural reducts.
theories/AmbientOpenFiring.v
# D10 — the COMMUNICATION rule fires for a reduct of ARITY m ≥ 1 (SynchronousCommFiring), so the GSLT
# omnibus's SYNCHRONOUS π `Comm` `{(PIn n ^x.p), (POut n m q), ...rest} ~> {(eval ^x.p m), q, ...rest}`
# — whose output carries a CONTINUATION, hence a two-element reduct — is realized, not only the
# asynchronous one CommRuleFiring models. COMPOSES two landed pieces and adds no primitive: the guard
# is CommRuleFiring's VERBATIM `comm_guard` (the reduct arity never touches the non-linear channel
# gate) and the emit is AmbientOpenFiring's ATOMIC m-reduct `struct_attempt`/`insert_all`. What is new
# is the SPLIT of the reduct list into the ONE HOST-COMPUTED substitution (recovered from the firing's
# contractum) and the m-1 σ-DELIVERED elements — the distinction `RhoNetCommInjectionSite::reduct_slots`
# makes — with no-fabrication for both kinds, and CONSERVATIVITY: at m = 1 the fired bag is EXACTLY
# CommRuleFiring's `insert_exact` bag (mirroring the byte-identical emitted receiver/injection).
theories/SynchronousCommFiring.v
# Stage 4 S-binder SLICE 3 — the Ambient In/Out rules fire as ONE DEPTH-2 NESTED structural non-linear
# AC COMM (AmbientInOutFiring): the DEPTH-2 analogue of AmbientOpenFiring. REUSES AmbientOpenFiring's
# atomic m-reduct struct_attempt at the OUTER bag level, gates it by the CROSS-LEVEL guard
# `EEq(M_outer, M_inner)` = ac_nl_guard [0;1] over two `M` slots at DIFFERENT nesting levels
# (AcNonLinearConsistency.ac_nl_cross_level_commits/_reject_safe, inherited verbatim), and splices the
# nested reduct `⟦R⟧σ` — rest1 INLINE at the inner bag, rest2 INLINE at the outer bag
# (InRhoAcMatchMultiset.inrule/outrule_reduct_ids_two_level) — from the SUBJECT
# (nested_structural_ac_spread_is_report_faithful, reusing the outer/inner_located_is_spread pair-
# Hypothesis). Assembles the landed depth-2 In/Out FV pieces into an OPERATIONAL firing lemma +
# source<->in-Rho step-correspondence (inout_step_complete/sound/lower_preserves_barbs), putting the
# Stage-5 capstone's FAcNested (In/Out) arm on EQUAL FOOTING with FAcStructural (OpenRule).
theories/AmbientInOutFiring.v
# FV (vii) — Stage 3a contextual rewrite as an ATOMIC POLYADIC JOIN (INV-6) + PLUGGING-STABILITY
# (INV-2): generalizes LinearCommCorrespondence's SameChannelJoin 2->n. consume_list agrees with
# consume_two at n=2; the n-ary contextual join is sound+complete with matching barbs; atomic
# all-or-nothing (no partial consume, one reduced context emitted); plugging is total+injective with
# an outer head invariant to the holes (O2); the unary Wrap(S)~>Wrap(T) join reconstructs ⟦Wrap(T)⟧.
theories/ContextualAtomicJoinPlugging.v
# Stage 3c — the binder/β-substitution codegen (App(Lam(^x. b), a) ~> subst(b, x := a)) is
# well-defined + injective (BinderReflectionTotalOrReject): the De Bruijn LHS σ-variable
# extraction excludes the binder and preserves the body's free variables (free_lam_char), and the
# RHS Lambda-node reflection is collision-free (the reserved ^lambda/^bound tags never collide with
# an Apply node or a σ-slot) + injective under a NoDup σ-order (reflect_inj). Beta is a BASE
# rewrite, so it inherits LinearCommCorrespondence + the RhoLoweringTotalOrRejects install boundary.
# Stage-4 S-binder slice 1 adds MatchSideIndexedPeanoReflection; slice 2b adds the ^subst/^shift
# 5-shape injectivity (SubstShiftReflectionInjective).
theories/BinderReflectionTotalOrReject.v
# Stage 4 S-binder SLICE 2b — the in-Rho de-Bruijn substitution/shift TRS (the five persistent
# reserved receivers of rholang-codegen/src/rho_net_subst_trs.rs) is STRONGLY NORMALIZING
# (subst_trs_terminating, via the val(k)-weighted 3^k / 4^(mu t) measure — the naive <#nodes,size>
# is non-monotone since shiftk(S k) spawns a shift), CONFLUENT (subst_trs_confluent, by the
# normalizing interpretation `norm`: step preserves norm + every term reduces to it, an
# orthogonality-free Church-Rosser), and its seed subst(0,a,b) normalizes to the capture-avoiding
# de-Bruijn beta reduct b[a/0] (subst_normal_form_is_debruijn_beta); SN+CR give the UNIQUE NF
# (subst_trs_unique_nf) the bisimulation consumes. The numeral dispatch ^cmp/^pred/^sb/^shb is
# abstracted to nat conditionals (documented in the header).
theories/DeBruijnSubstTRS.v
# Stage 4 S-binder SLICE 2b — the in-Rho β cascade is weakly bisimilar to abstract β
# (InRhoBetaCascadeWeakBisim): object-β is the single VISIBLE label; each ^subst/^shift/^shiftk COMM
# is τ; the σ-receiver's one visible seed COMM followed by the τ-cascade collapses (via
# DeBruijnSubstTRS confluence+termination) to the UNIQUE NF = b[a/0]. Modeled on
# CommReductionCorrespondence (a genuine REDUCTION bisimulation, NOT the vacuous τ-erasure of
# advanced_automata/InRhoSameCLTSWeakBisim): R o c := norm c = o, proved a weak bisimulation.
theories/InRhoBetaCascadeWeakBisim.v
# A-S5.2 — the in-Rho QUIESCENCE DRIVER (the generated `^drive` receiver family, Lambda core,
# PS value carrier) as a big-step LTS over the reflected object fragment
# (InRhoQuiescenceDriver): drive_steps_sound (each DDone drive is an iterated genuine object
# beta reduction), quiescence_sound (PER-TRACE: every DDone rests a beta-NF — the structural
# induction whose post-join RE-CHECK case carries the children-normal hypothesis),
# fuel_exhaustion_never_wrong (the typed `^drive-fuel` datum is the stuck redex, never
# mistakable for an NF), the ITERATED beta weak bisimulation drive_weak_bisim (aiter/citer
# chains lifting InRhoBetaCascadeWeakBisim's single-fire clauses — cascade legs SN+CR-unique),
# and the two-firing non-vacuity witness.
theories/InRhoQuiescenceDriver.v
# E-1 — the DEMAND-DRIVEN SLOT-SCION for the driver's FiringEmission seam (ScionBundle,
# rho_net_drive.rs) as a big-step LTS over the SAME reflected `Obj` fragment, carrying the
# FIRED MULTISET in the result (InRhoScionGraft): ADDITIVE (no InRhoQuiescenceDriver edit);
# scion_graft_sound (SKIP-mark soundness — an unmarked position can never match under ANY
# slot instantiation, the Lemma-6.2.3 analogue), sdrives soundness / per-trace quiescence /
# typed fuel exhaustion, sdrives_gdrives_agree (SCION graft = CONTRACTUM redrive: same NF AND
# Permutation of fired multisets, under the graft_safe Fold-1 guard — SM-10), the reused
# weak-bisimulation kit instantiated for the cascade-FREE structural fire (cheaper than beta),
# the no-slot-redescent efficiency counter, the ladder non-vacuity witness, and the NEGATIVE
# TEST pinning the Fold-1 guard's necessity (without it, scion under-reduces).
theories/InRhoScionGraft.v
# E-2 MECHANISM D — the SOUNDNESS kernel for the hereditary-GROUND subst/shift short-circuit at
# the SHARED de-Bruijn subst-cascade entry (rho_net_subst_trs.rs `^subst`/`^shift` receiver head
# guard) (InRhoCreeperTrace): ADDITIVE over DeBruijnSubstTRS's Obj + osubst/oshift/oshiftk. Defines
# the decidable `oground` (hereditary-ground = no oBound leaf anywhere = the host-side reflected
# marker) and proves oground_subst_id (`oground t` ⟹ `osubst j a t = t` under ANY depth j — the
# `^subst`-entry guard is sound + DEPTH-INDEPENDENT, no `^cmp`), oground_shift_id (the `^shift`-entry
# guard), and oground_shiftk_id (the iterated `^shiftk` is identity on ground — the `^shift` guard's
# transitive `^shiftk` coverage), plus non-vacuity witnesses (a closed constant FIRES, the identity
# combinator body `oLam(oBound 0)` DECLINES). Grounds the E-2-D production tax: NF results stay
# byte-identical (the guard replaces identity subst/shift chains with one `ret!(t)`).
theories/InRhoCreeperTrace.v
# Stage 3e — the native-system-process dispatch (NativeProc(a..) ~> <native value>) is
# TOTAL-OR-REJECT + the emitted payload is EXACTLY the trusted handler's value
# (NativeSystemProcessBoundary): lower_native_system_process either materializes the flat one-slot
# dispatch receiver (iff `fold` — a host-computed contractum — AND the channel resolves) or fails
# closed with a precise reason (NotFold / ChannelUnresolved), never silently; and the receiver
# forwards ONLY the injected slot, which the native σ-injection fills with the REFLECTED contractum
# (the host's trusted-handler value) — so emitted = reflect(handler_value) (delegation, no
# fabrication), tracking the handler value injectively. Inherits LinearCommCorrespondence + the
# RhoLoweringTotalOrRejects install boundary + the RhoHostObligationBoundary trusted-handler seam.
theories/NativeSystemProcessBoundary.v

# Stage 3f — the fold-vs-equation criterion (D3, INV-9) is SOUND (FoldMotionVsCongruence): a
# COMPUTING native scalar fold `AddInt(a,b) ~> a+b` changes a CLTS barb (the unreduced redex is
# observably not the value), so it is directed motion firing as a COMM (`lower_native_fold` →
# `NativeFold` dispatch receiver); a LOSSLESS ISO COERCION `NormCast (Cast<Src> v) ~> (Cast<Tgt>
# (SrcToTgt v))` — marked by its `Premise::SyntheticInjGuard`, `is_lossless_cast_congruence` —
# preserves all barbs (the representation tag is not observable), so it is compile-time congruence,
# no COMM (`CongruenceClosure`). The discriminator (keyed on the inj-guard) is proved CONSISTENT
# with the barb behavior: it fires exactly the barb-changing rules and treats exactly the
# barb-preserving rules as congruence (fires-as-COMM ⟺ directed motion). Companion to
# NativeSystemProcessBoundary.v (the firing side) + the RhoHostObligationBoundary trusted-handler seam.
theories/FoldMotionVsCongruence.v
# Stage 5 CAPSTONE — the whole-⟦G⟧ operational correspondence for FINITE executions with O1-optimal
# in-Rho matching (WholeGsltInRhoOpCorrespondence, INV-13 / the knotted-topoi paper's `ob:opcorr`).
# A COMPOSITION HARNESS, not a new reduction proof: it (a) instantiates the assumption-free finite-trace
# lift EndToEndCommCorrespondence with the whole-⟦G⟧ in-Rho configuration LTS, (b) assembles the lift's
# three step obligations by a `family_of` case split whose SIX arms are already-landed per-step theorems
# (FBase<-LinearCommCorrespondence, FContextualJoin<-ContextualAtomicJoinPlugging, FAcLinear<-CommRuleFiring,
# FAcStructural<-AmbientOpenFiring, FBinderBeta<-InRhoBetaCascadeWeakBisim, FNative<-NativeSystemProcessBoundary)
# plus the slotted In/Out FAcNested (InRhoAcMatchMultiset depth-2), each a Section Hypothesis (a universally
# quantified premise on Section close, NOT an Axiom — so every theorem stays Closed), and (c) `apply`s the
# lift to obtain the whole-⟦G⟧ finite-trace opcorr, then threads obligation (iii) so the result holds over
# the O1-OPTIMAL matching (the `rem:nonopt` discharge). The matching layer (i/ii/iv/vi/viii/x) and the AC
# match-logic are NOT arms — they enter as gstep well-formedness + premises of (iii). Semantic predicates
# (INV-14) are excluded by construction (no Family constructor + the `semantic_predicates_emit_no_comm`
# fence); all-G universality is conditional on `g_install_gate_admits` (ix). The SwapDemo base rewrite is
# the non-vacuity witness (all §4.1 hypotheses jointly satisfiable, base arms discharged from the landed
# comm_step_complete/sound), simultaneously a concrete Closed finite-trace opcorr through the capstone.
theories/WholeGsltInRhoOpCorrespondence.v
# Stage 5 CAPSTONE (iii) thread — build-wiring OPTION (a): imports the landed obligation
# (iii) advanced_automata/InRhoSameCLTSWeakBisim.same_clts_weak_bisim and proves it
# discharges the capstone's matching_locus_fwd / matching_locus_bwd hypotheses' EXACT
# shapes (the two weak-bisimulation clauses), witnessing that the option-(b) Section
# hypotheses in WholeGsltInRhoOpCorrespondence.v are backed by the real (iii) theorem.
# The barb hypothesis is discharged downstream of (iii) (InRhoSameCLTSWeakBisim is
# schedule-level, defines no Par/RSpace barb). Requires the -Q AdvancedAutomata edge above.
theories/WholeGsltInRhoOpCorrespondenceOptimalViaSameClts.v
# Stage 5 CAPSTONE — the In/Out (FAcNested) NON-VACUITY WITNESS: the depth-2 mirror of the capstone's
# base-only SwapDemo witness. Instantiates the SAME abstract `whole_gslt_in_rho_opcorrespondence` with an
# InOutDemo common-carrier sum (GSrcIO/GRhoIO over AmbientInOutFiring's InOut states) whose step is the
# In/Out reduction / lowered In/Out COMM, and discharges the `FAcNested` arm NON-VACUOUSLY from
# AmbientInOutFiring.inout_step_complete/inout_step_sound (the other six families vacuous) — the concrete
# proof the In/Out arm is dischargeable, putting it on EQUAL FOOTING with FAcStructural (OpenRule). Yields
# a concrete Closed In/Out finite-trace opcorr through the capstone (+ the O1-optimal parity corollary).
theories/WholeGsltInRhoOpCorrespondenceInOutViaFiring.v
# A-S5.7 — the ITERATED-DRIVING DISCHARGE WITNESS: the capstone's §5 upgraded FBinderBeta burst
# premises are backed LITERALLY by the landed InRhoQuiescenceDriver theorems (plan v2 §7.4
# "discharged by (1)"). Instantiates the §5 iterated harness with the beta common-carrier sum
# DCbeta (DAbs Obj | DConc Tm, the InRhoBetaCascadeWeakBisim carriers): (a) the FBinderBeta BURST
# arms discharged non-vacuously from forward_/backward_simulation via the §5.0 pointwise lift
# (betadrive_iterated_opcorr); (b) the CHAIN face consumed from drive_weak_bisim proj1/proj2 —
# harness bursts project to aiter/citer and back, the matching chain is DELIVERED by the driver
# theorem (beta_burst_discharged_by_drive_weak_bisim + backward dual), non-vacuous via the
# two-firing witness chain; (c) the A-S5.5 bag-model REST grounding consumed literally
# (bag_quiescence_sound + driver_flatten_agrees_with_add_flattened_bag + bdrives_deterministic:
# every bag drive rests FLAT and host-flatten-agreeing — PER-TRACE, decision (3), no unique-NF
# claim for the non-confluent Ambient).
theories/WholeGsltInRhoOpCorrespondenceIteratedViaDriver.v
# A-S5.4a — the UNCONDITIONAL unbind-first binder float (binder_congruence.rs, gates dropped)
# canonicalizes Ambient-shaped terms: freshening totality (the gensym supply never exhausts, so the
# float never stalls — the F1 stall is an artifact of the retired gates); AM-2 bag flatness at the
# extrusion seam (the insert_into_<label> work-stack peel modeled at ALL depths);
# float_nf_exposes_redexes_{in,open} — over the C-G subset (Res Par + Res Amb + Res Res +
# representational Par Comm/Assoc), a bag/nested redex exists modulo == IFF it is syntactically
# present in the float NF (depth-2 fragment = exactly what the rewrite patterns inspect; Out is
# A-S5.4b scope, re-proved in AmbientInOutFiring.v post-redeclaration per AM-1); NewComm-permutation
# redex invariance (the <=6 canonical-run cap is display-only). Theory note: ma_theory_alignment.md.
theories/BinderFloatCanonicalization.v

# A-S5.8 — the in-Rho ^float receiver family (rho_net_float.rs) modeled over the NAMELESS
# (de Bruijn) reflected ABI in the RUN-LENGTH configuration representation
# (InRhoFloatCanonicalization): hoist_side_condition_by_shift_image + fstep_side_condition (the
# C-G freshness side condition of every extrusion instance held by the SHIFT-IMAGE argument — no
# gensym in-Rho); float_step_sound = fstep_side_condition + float_reachable (the float function is
# an fstep chain of documented C-G composites); float_functional_up_to_NewComm (F8-AM-4 first
# half: ANY peel order floats to the same run mass with bodies related by a run-permutation
# renaming carrying an explicit two-sided inverse — schedule confluence, variance exactly the
# NewComm class, the Q-NC record) + float_identity_on_canonical (second half: EXACT identity on
# canonical configs); redex_invariant_under_run_permutation (F8-AM-3 — the NEW de Bruijn lemma:
# redex predicates closed under injective index renamings; BinderFloatCanonicalization:381-397
# cover BAG permutation only); float_preserves_bag_flatness (the merge base's three-case dispatch,
# AM-2 in-Rho); float_exposes_redexes_{in,open,out} (over the extrusion + bag-permutation
# equivalence, a redex exists modulo == IFF syntactically present in the float NF).
theories/InRhoFloatCanonicalization.v
# A-S5.8 — the DRIVER FLOAT PHASE (InRhoDriveWithFloat): the bval fragment + a BINDER (fval =
# bval + FNu), the value-level float ffloat (canonical + idempotent), the float-routed driver
# fdrives whose d_redex_fire analogue tests/fires on the FLOATED value (decisions Q-AB=A /
# Q-SEED=S2), THE PREMISE DISCHARGE drive_with_float_on_raw_eq_drive_on_canonical (driving raw =
# driving canonical, for EVERY firing relation — the boundary-float premise discharged
# constructively), the nu-hidden-contractum witness (Seal-shaped: raw contractum matches NO rule,
# the per-firing float exposes and fires — with the load-bearing contrast lemma), and
# float_phase_conservative (ffloat . embed = embed . bdrive — every landed bag theorem transfers
# as a corollary; conservative extension, InRhoQuiescenceDriver untouched).
theories/InRhoDriveWithFloat.v
