[package]
name = "query"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Runtime query interpreter for MeTTaIL: parse and execute Datalog-style rules over runtime backend reports"
publish = false

[lib]
name = "mettail_query"

[dependencies]
ascent_syntax_export = { path = "../ascent_syntax_export" }
mettail-runtime = { package = "runtime", path = "../runtime" }

syn = { workspace = true }
