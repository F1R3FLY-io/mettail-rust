# Float Cast-Family — DEFINITIVE Root: forward-path Proc-projection (6-angle flip-grounded, 2026-06-02)

Authoritative trace artifacts: `/var/tmp/suite-green/realize-rootcause-VERDICT.txt` (252 lines) + 23 `realize-rootcause-*` traces (agent `adcd526d`, worktree `/var/tmp/wt-realize` @ `d2d9a3b`). This doc is the in-tree ledger of the PROVEN root; it supersedes the partial localizations in `float-trigger-fork-disambiguation.md §⚠`, `general-rule-set-left-factoring.md §RESULTS`, and `float-crosscat-projection.md`.

## The 2 targets
`float(float(10,64),64)` and `float(int(5,32),64)` ERR ("no accepting branch reached end of input"); controls `int(int(5,32),32)`, `float(10,64)`, `float(float(10.5))`, the Bool win, `int(str(3))` all GREEN. **Discriminator:** failure ⟺ the OUTER is a `float`-FOLD whose first operand `a:Proc` is itself a 2-arg cast fold; INT-outer-fold works.

## The root is TWO coupled layers (both flip-grounded)

### Layer 1 — PREFIX `rule_idx` pre-commit (necessary, NOT sufficient)
The inner `float(` is a 5-way Fork (4 unary casts + `FloatBin` fold) where lex-min collapses to `rule_idx`, pre-committing to a unary reading before the `,`/`)` evidence (`float-root-PROVEN.md`). The general prefix-trie (`general-rule-set-left-factoring.md`, tag `genfactor-STOP-GATE1`) FIXES this layer (evidence-driven, fully general, recovers standalone unaries) — but the targets STILL fail, proving Layer 1 is necessary-not-sufficient.

### Layer 2 — FORWARD-PATH Proc-projection asymmetry (the SUFFICIENT root)
**BEDROCK SITE:** `WpdaWalker::cursor_gss_pop_via_edge` (`prattail/src/wpda_walker.rs:15012-15080`). A Resolved dispatch-cohort body — the mechanism that delivers an inner sub-parse result to the paused outer cohort member (drained per-step at `:10219`) — is registered **ONLY when the popped GSS edge carries `EdgeKind::CrossCatProjection`** (the `if let Some(CrossCatProjection{..})` gate at `:15013`). The inner keyword-cast FOLD result pops through `CategoryEntryRoot` (the same-cat home-Fork path), so the resolve block is skipped → **no pos-2 Resolved body → the outer `a:Proc` slot is starved → at EOI `accepting_indices.len()==0` (`:4304`)**.

**THE ASYMMETRY (why INT passes, FLOAT fails) — lossless-lattice transparent-projection multiplicity:**
- Transparent (trigger-less `x:C |- x : D`) projections = hand Proc-injections (`calculator.rs:100-110`) + auto lossless promotions (`ast/src/language.rs:1048` `lossless_targets`).
- `Int32 → [Int64, Int128, BigInt, BigRat]` (RICH — multiple non-Proc transparent readings) → the inner INT fold pops as **11 distinct CrossCatProjection edges at `dispatch_pos=2`** (span `[4,8]`) → each registers a Resolved cohort body keyed `pos_at_dispatch=2` → forward drain splices the outer member → PASS.
- `Float64 → [CanonicalBigRat]` only (SPARSE — `{ProcFloat, FloatToBigRat}`) → Float's sole `ProcFloat` reading is consumed by the same-cat `FloatBin` Fork (`CategoryEntryRoot` pop) and **never pops as a CrossCatProjection at `dispatch_pos=2`** (trace: ZERO CrossCatProjection pops at pos 2 for the float target). Float's other casts (FloatToInt/Str/Bool) are trigger-prefixed, so they don't apply to a bare inner-fold operand.
- Lattice sites: `ast/src/language.rs:1110-1111` (Float, sparse) vs `:1066` (Int, rich); `calculator.rs:100-110`.

## FLIP-GROUNDING — 6 angles converge on "forward in-line parse is the sufficient layer"
1. **genfactor `FLIP_NOUNARY` (prefix layer):** delete the unary branches → inner collapses to a single `FloatBin` → parsed **IN-LINE** as the outer operand → projects to Proc → **both targets PASS**. ⇒ the forward in-line parse is the SUFFICIENT transform (but deleting unaries breaks standalone unary casts).
2. **realize cohort-delivery flip v1** (deliver raw `[4,8]` Proc body): Proc `[4,8]` Symbol appeared, body registered (resolved_total 21→37) — **targets STILL ERR**; outer `[2,10]` never forms.
3. **realize cohort-delivery flip v2** (deliver extended-span Proc `[2,8]` — the exact span the standalone case produces): `cat=0 Proc [2,8]` created, `R{pos:2,src:5,wrap:(0,1)} span=[2,8]` registered — **targets STILL ERR**; outer `[2,10]` STILL never forms. ⇒ the cohort/revival layer is **NECESSARY-BUT-INSUFFICIENT**: `revive_cohort_member_with_snapshot` (`:14480`) rebuilds the outer cursor from `member.return_frame` + the inner snapshot, but the rebuilt cursor **cannot drive the outer fold's remaining `, w )` to acceptance**.
4–6. **The 4 prior falsifications** (A2-merge, M8.1-Symbol-`lo_pos`, M9-cohort-projection, prefix-trie) ALL operated at the cohort/merge/projection/prefix layer; NONE reproduced the forward in-line parse.

⇒ **The SUFFICIENT layer is the FORWARD in-line parse / forward-path Proc-projection — NOT cohort/revival/merge/span-anchored.** The cohort layer fundamentally cannot reconstruct the outer fold's continuation post-hoc; the inner must be parsed in-line as the outer's Proc operand ON THE FORWARD PATH (which is exactly what the INT lattice's CrossCatProjection pops achieve, and what Float's sparse lattice denies).

## Bool-vs-Float (why span-anchored reconstruction closed Bool but not Float)
Span-anchored reconstruction (M7.1) pairs `K_sib` with body `R` iff `R.span_lo == K_sib.pos`.
- **Bool** `int(y != true > x < "qua")`: cast operand is a comparison chain starting at the bare var `y` at pos 2 (NO inner trigger) → body `span_lo=2 == dispatch pos 2` → span-anchoring MATCHES → SUCCEEDS. *(Single-operand cast over a trigger-free body.)*
- **Float** `float(float(10,64),64)`: cast operand is itself a keyword-cast FOLD; its result Symbol is `[4,8]` → `span_lo=4 ≠ dispatch pos 2`. The interval `[2,4]` is the inner `float(` trigger (consumed as TriggerTerminals OUTSIDE the inner Symbol, shifting `lo_pos` by 2). Span-anchoring structurally CANNOT match, and no Proc Symbol can span `[2,8]` (the `[2,4]` trigger belongs to the inner rule). *(2-arg fold whose first operand is itself trigger-prefixed.)* Confirmed by the PASSING standalone `float(10,64)`: operand `10` → Proc `[2,3]`, `span_lo=2 == dispatch pos 2`, and the pos-2 ProcFloat projection DOES pop.

⇒ The general fix is **NOT** an extension of span-anchored revival (the span anchor is the wrong key for a trigger-shifted operand).

## What the GENERAL fix must do (no hardcoded assumptions — per the standing mandate)
Make EVERY cast-result category project to Proc via a **CrossCatProjection pop on the FORWARD path** at a cross-cat operand position — **independent of lossless-lattice multiplicity** — so Float/Bool/Str behave like Int (whose rich lattice incidentally already does). I.e. close the asymmetry at its source: a cast-keyword result used as a cross-cat operand must emit a forward Proc-projection (CrossCatProjection) regardless of how many transparent lossless readings its result category happens to have. Candidate surfaces (to be Plan-confirmed, NOT yet chosen): (i) the transparent-projection emission so every cast result has a Proc CrossCatProjection edge available at the operand dispatch (generalize `calculator.rs:100-110` / the lossless lattice to guarantee a Proc projection per cast result cat); (ii) the `CrossCatProjection`-only resolve gate (`:15013`) + the forward `CrossCatDelegate` dispatch (`prefix.rs`) so a same-cat home-Fork pop of a cast-fold result ALSO registers the cohort body when the operand context demands a Proc projection. Almost certainly PAIRS with the prefix-trie (Layer 1: keep unaries alive + factor the prefix). Must be GENERAL across the whole cast class, evidence-driven, `Ambiguous`-preserving, and must not perturb the Bool win / the 3 M3.1 sentinels / the 5 A2-regressed cross-cat tests / chains / op-suites / gauntlet 4220/0.

**Critical sites:** `prattail/src/wpda_walker.rs:15012-15080` (`cursor_gss_pop_via_edge` — the CrossCatProjection-only resolve gate `:15013`), `:14480` (`revive_cohort_member_with_snapshot` — the cohort layer that CANNOT reconstruct the outer continuation; the fix must NOT live here), `:10219` (the per-step cohort drain), `:4304` (the `accepting_indices==0` arm); `macros/src/gen/runtime/wpda_codegen/prefix.rs` (the forward `CrossCatDelegate` cross-cat dispatch); `languages/src/calculator.rs:100-110` + `ast/src/language.rs:1048`/`:1066`/`:1110-1111` (the transparent-projection / lossless-lattice asymmetry). UNTOUCHED authorities: `min_terminal_span` (`:257`/`:4843-4884`), `Ambiguous` (`:4376`), lex-min (`lex_weight.rs`).
