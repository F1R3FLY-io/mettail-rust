# Sig-B Blocker-3 — OUTER-CAST-CURSOR LIVENESS / splice-timing (cast family's last residual, 2026-06-01)

**pgmcp experiment #9.** THIRD redesign. The prior TWO were FALSIFIED at their diagnostic gates because they designed against an UNVERIFIED static-read root cause: `sigb-blocker3-transitive-crosswrap.md` §2 (in-block single-body-pop drive — FALSIFIED at M3.1, `cursor_gss_pop_via_edge` pops the TOP inner wrap and self-enqueues, regressed 3 sentinels) and `sigb-blocker3-fanout-fixpoint.md` (fanout-level drain-fixpoint — FALSIFIED at M4.0: the residual does NOT exit via `!progress_made`, and `has_pending_crosswrap_climb()` is FALSE at every boundary). This redesign is grounded in the M4.0 death-moment diagnostic PLUS my own VERIFIED mechanism (§1). Builds ON Blocker-1 GLL descriptor + Blocker-2 single-level cross-wrap splice + §3d backstop + C-bis Newton + M4 keying — KEEP ALL. Build state ≡ `6507b9c` + `/var/tmp/suite-green/sigb-cast-family-COMPLETE.patch` (branch `wip/cast-family-cohort` @ `3d5d94e`) + `cp Cargo.lock`. Design-only; evidence- + code-confirmed against the Blocker-2 branch source.

## 0. Residual (measured)
`simulator_regression_bool_prefix_tokens` on EXACTLY `int(y != true > x < "qua")` (`calculator.rs:2188`, inside the test at `:2176`; tokens `0=int 1=( 2=y 3=!= 4=z/true 5=> 6=x 7=< 8="qua" 9=) 10=Eof`, `tokens.len()=11`, `eof_node=10`). Same structural class: `test_nested_float_float_int` (`calculator.rs:1054`, `float(float(10,64),64)`), `test_triple_nested_float` (`:1059`, `float(float(float(10,64),64),64)`). Requires ALL of: (1) var-first LHS (bare-var adds one coercion level), (2) ≥4-op chain, (3) Int→Str cross at the TAIL (`< "qua"` adds another coercion level). Ladder: boollit-first `int(true != y > x < "qua")` OK; 3-op `int(y!=z>x)` OK; ends-in-var/int OK. Baseline (Blocker-2) calc 210/7 → B3 success 213/4.

## 1. Root cause — OUTER-CAST-CURSOR LIVENESS / splice-timing (VERIFIED mechanism; M5.0 must re-confirm)

This section is the M5.0 CONFIRM-gate specification. Every claim below is grounded in a named trace artifact + a named code site on the Blocker-2 branch. The implementation agent's M5.0 diagnostic MUST reproduce the bracketed `[CONFIRM-n]` predicates BEFORE any behavioral change; if any is falsified, the milestone STOPS.

### 1.1 The cohort dispatch architecture (code-confirmed)
At a cast's `(` dispatch site, `allocate_fork_push_child` (`wpda_walker.rs:13610`) constructs `DispatchKey::new(pos_after, source, bp, wrap_cat, wrap_rule)` and calls `dispatch_cohort_cache.register(key)` (`:13628`). M4 widened `DispatchKey` to include `(wrap_cat, wrap_rule)` (`dispatch_cohort.rs:73-95`), so distinct wrap-rule injections at the same `(pos, source, bp)` get DISTINCT cache entries. `register` returns (`:13631`):
- `WorkerInserted` → the cursor falls through to "allocate normally" (`:13768`) and becomes a **live `branch_cursor`** that sub-parses the body.
- `InflightCollision` → the cursor's continuation is captured as `CohortMember{ return_frame: parent.clone(), … }` and `pause_cohort_member`'d (`:13670`, `dispatch_cohort.rs:1171`) — stored in the cache as a `pending_member` shell+state, NOT a live cursor.
- `ResolvedHit` → revived immediately + a synthetic future member paused (`:13700-13758`).

**Workers and the cast's parked continuation share the SAME `branch_cursors` set** (single-walker Tomita-GLR). The OUTER `int(` cast frame — the one that pushed `(` and must eventually consume `)` then fire the Int cast — exists ONLY as a paused `CohortMember.return_frame` (or as a transiently-live worker that re-pauses). It is revived only when its body resolves and the end-of-step drain fires (`step_fanout:9752-9895`).

`[CONFIRM-1]`: at the `(` dispatch for the residual, ≥2 wrap-rule injections register at the dispatch site; ≥1 returns `InflightCollision` (the outer cast continuation is paused as a `pending_member`, not retained as a persistent live `branch_cursor`). Trace via a `SIGB_CROSSWRAP` print of `RegisterOutcome` at `:13631`.

### 1.2 The cross-sibling cascade DOES reach full span — and the outer wrap DOES resolve
The Blocker-2 cross-wrap drain (`take_pending_for_drain_crosswrap`, `dispatch_cohort.rs:791`; walker call `:9876`) splices each resolved body into InFlight siblings at the same dispatch pos. Verified climb (artifact `b3-varfirst-b2drain.log`, `M3.1-DRAIN fstep` markers): span `[2,3]` (fstep 9) → `[4,5]` (13-17) → `[6,7]` (18-24) → **`[8,9]` (fstep 22,25,28,30,31)**. The full body span (pos 2→9) IS reached and drained.

Decisively (artifact `b3v2-m40-trace4.log:25672-26131`, residual `Int::parse` section): the OUTERMOST cast wrap appears as a **Resolved sibling** `R=resolved_key{wrap:(0,2)} R.symbol_id=103 R.hi_pos=9 R.pos_at_dispatch=8 equiv=(7,0)`, and the drain finds ELIGIBLE InFlight siblings `K_sib={pos:8, wrap:(1,11)/(2,16)/(5,12)/(6,9)}` to splice the full-span body into. So the splice **revives ARE produced** — the outer body IS available and IS attached to live pos:8 siblings (`revive_cohort_member_with_snapshot:13865` pushes them as `Frame::Concrete` into `new_cursors`, `:9890`). The ELIGIBLE `R.pos_at_dispatch` histogram is `pos=2(hi3)→4(hi5)→6(hi7)→8(hi9,×17)` (M4.0 GATE-FAILED txt) — the drain reaches outer full-span.

`[CONFIRM-2]`: in the residual trace, a Resolved sibling with `R.pos_at_dispatch == 8 ∧ R.hi_pos == 9` exists (the outer cast body resolved full-span), AND `take_pending_for_drain_crosswrap` emits ≥1 job for it (the splice fires; revives are produced). This FALSIFIES any "the cascade never reaches the outer resolution" hypothesis (which prior design 2 §1 wrongly assumed).

### 1.3 …but NO surviving revive reconstructs the genuine outer `int(` BinderRule frame → frontier collapses to Error
The surviving cross-wrap revives are all "inner-shaped": `revive_cohort_member_with_snapshot` (`:13865`) sets `cursor.pos = hi_pos` (=9, the `)`), re-pushes a fresh `CategoryEntry(source)` edge with the resolved wrap (`:13917`, `cursor_gss_push_with_kind`), and sets `cursor.inner_state = snap.worker_inner_state` (the BODY worker's pop state, `:13941`). This reconstructs each InFlight sibling's OWN-wrap continuation — but NONE of them is the genuine outer `int(` cast frame whose GSS holds the `(`-pushed BinderRule frame that expects to consume `)` at pos:9 and then fire the Int cast (`emit_fire_action` over a `RuleAt`/`Return` pop, `apply_pop_body_to_cursor:14064`). Each spliced revive's next step fails to reach `is_accepting_config` (`:5620`: requires `is_cursor_accepting_terminal` ∧ inner_state ∈ {Accepted, InfixLoop, Unwinding-at-entry}) → `CursorOutcome::Drop` (`:9467`, discarded).

By fstep ~31 the inner-body workers have all terminated (their spans self-resolved, becoming `Resolved(>=hi)` which clause 4 EXCLUDES — the parens-inner-steal guard, `dispatch_cohort.rs:846`). At the moment of death (`b3v2-m40-trace4.log:26129`): `ALL_DROPPED at pos=1: strict_climb=false relaxed_climb=false resolved_with_live=87 clause123_pairs=279 clause123_undrained=76 ... drained_pairs=103` — all 76 undrained clause-1-3 pairs FAIL clause 4 (`sib_hi=9` full-span, or empty members). The drain has nothing eligible left to revive, and the last live revives Drop → `branch_cursors` empties → `step_fanout` sets `WpdaState::Error{"all fork branches dropped"}` (`:9954-9958`). The walker's `self.pos` reads 1 because the LIVE FORK frontier (the worker/outer line in `branch_cursors`) never advanced past the `(`; the entire body pos 2→9 was consumed inside cohort WORKER cursors whose pos is tracked per-cursor, and the cohort-cache resolution is invisible to `self.pos`. Death message: `1:4: ... found '('`.

`[CONFIRM-3]` (the LIVENESS/TIMING signature — the central confirm): the residual exits via `step_fanout` → `WpdaState::Error("all fork branches dropped")` (`:9954`) with `branch_cursors == 0`, caught at the NEXT `run_to_end_of_input` iteration's top-of-loop `is_terminal()` (`:3981`). It does **NOT** reach the `!progress_made` block (`:4082`). Confirmed in the ACTUAL test (`b3v2-m40-bptest.log:29015`): residual exits `EXIT=terminal Error branch_cursors=0`. This re-confirms prior design 2's `!progress_made` hook is a no-op for the residual.

### 1.4 The structural flip (why `int(int(5,32),32)` lives but the deep var-first chain dies)
The PASSING cases reach `run_to_end_of_input`'s `!progress_made` parked boundary with a **LIVE** outer frontier (`b3v2-m40-bptest.log`: the 7 passing `bool_prefix_tokens` inputs exit `fanout_!progress_made` with `branch_cursors = 13/13/13/31/31/26`; `b3v2-m40-trace4.log:26769`: `nest_int_int` exits `fanout_!progress_made branch_cursors=3`). For these the inner cross-sibling cascade resolves the full body span FAST ENOUGH — within the window where the outer cast continuation is still reconstructable from a still-live sibling — so the outer revive lands on a live frame, fires the cast, consumes `)`, and parks accepting. For the ≥4-op var-first Int→Str-tail chain the bare-var LHS (+1 coercion level) and the `< "qua"` Int→Str tail (+1 level) DELAY the inner cascade past the point where any live sibling carries the outer `int(` BinderRule continuation. By the time the outer body resolves (fstep ≥22), every sibling that could host the outer frame has already self-resolved at full span (clause-4-EXCLUDED) → the splice attaches to nothing → frontier empties → Error.

`[CONFIRM-4]`: contrast traces — for `int(int(5,32),32)` the `EXIT=fanout_!progress_made branch_cursors>0` boundary IS reached (outer survives); for the residual it is NEVER reached (terminal Error first). The delta is the number of fanout steps from `(`-dispatch to outer-body-full-span-resolution vs the step at which the last outer-frame-bearing sibling self-resolves.

### 1.5 Drop-cause classification (the (a)/(b)/(c)/(d) question)
The mechanism is **(a) ∧ (c)**: the outer cast continuation is held ONLY as a paused `pending_member`/`return_frame` (c — never a persistent live branch cursor), AND when its body resolves the splice produces revives that are pruned by reaching no accepting branch — i.e. they `CursorOutcome::Drop` (a — the "all fork branches dropped" path empties the frontier once the inner-shaped revives are the last live cursors). It is NOT (b): no progress/visited/liveness check kills a waiting parked frame mid-flight — `pause_cohort_member` retains the member in the cache across the whole parse (it is only consumed by `take_pending_for_drain*` or `drain_orphaned_inflight_members`); the loss is that the member's revive is never the genuine outer frame and the genuine outer frame's own live worker line dead-ends. The fix therefore targets reconstructing/keeping-alive the genuine outer `int(` frame so it survives to splice the resolved body, consume `)`, and fire the cast.

This is squarely a PREMATURE-DROP-BY-NON-EVIDENCE under the WPDA invariant: a sound derivation exists (`int(IntToBool-chain)` is the correct parse — the boollit-first sibling proves the body resolves and the cast can fire), but the outer cast cursor is dropped before EOI evidence rejects it, purely because of cross-sibling splice timing. STOPPING the drop until evidence rejects it is the principled fix.

## 2. Mechanism — (A) RECOMMENDED: retain the outer cast member as a REVIVABLE parked frame, revived at the EOI fixpoint from any Resolved full-span sibling. (Reject (B) reconstruct-from-SPPF-at-drain.)

### 2.0 Why (B) reconstruct-the-outer-frame-from-the-resolved-SPPF-at-drain is REJECTED
(B) = at the cross-wrap drain (`:9876`), when a Resolved sibling `R` has `R.pos_at_dispatch == K_sib.pos ∧ R.hi_pos == full body span`, synthesize a fresh outer `int(` BinderRule cursor from `R.symbol_id`'s SPPF subtree + the cast rule, splice the body, consume `)`, fire. REJECTED because: (i) it must fabricate a GSS frame (the `(`-push + BinderRule state) that no cursor actually walked — there is no faithful source for the cast's pre-`(` `recovery_deltas`/builder/`incoming_edge_stack`; `revive_cohort_member_with_snapshot` works precisely because `member.return_frame` IS a real walked cursor. (ii) It re-derives the same defect prior design 1 §2b hit: an in-drain multi-step body-walk that bypasses `merge_equivalent_cursors`/SPPF-dedup (`:9621`), which regressed `cross_cat_dispatch_chaining`/`cross_cat_with_floats`/`nested_int_int`. (iii) `realize_root_to_terms` (`:4693`) yields TERMS not cursors — it cannot resume parsing to consume `)`. No (B) variant produces a sound, mergeable, `)`-consuming outer cursor without the falsified in-drain walk.

### 2.1 (A) — the principled fix, grounded in §1.3/§1.5
The genuine outer `int(` cast member IS already retained in the cache (as a paused `pending_member` on its DispatchKey entry — §1.1), and its body DOES resolve full-span (§1.2). The ONLY defect is **timing**: by the time the outer body resolves, the live `branch_cursors` line carrying the outer continuation has dead-ended, and the per-step cross-wrap drain's clause 4 EXCLUDES the now-`Resolved(>=hi)` outer sibling from being re-spliced (correctly — clause 4 must stay to keep `cross_cat_with_parens` green). So the resolved full-span body never reaches the still-parked outer cast member.

Fix: at the EOI fixpoint (the `!progress_made` boundary AND — critically, because the residual exits via Error first — also via a new pre-Error retention), revive the outer cast member from the Resolved full-span sibling, **without relaxing clause 4** (which gates the per-step forward drain), via a NEW dedicated EOI-time splice that uses a DISTINCT eligibility: a paused member whose body resolved at the member's FULL required span but which never received that body because its own live line died.

Concretely, two cooperating pieces:

**2.1a — Prevent the premature frontier-collapse-to-Error while an outer cast splice is still pending (`wpda_walker.rs`, `step_fanout` drop site `:9954`).** When `self.branch_cursors.is_empty()` after the per-step loop + drain + merge, BEFORE declaring `Error("all fork branches dropped")`, attempt ONE EOI-style outer-cast revival (§2.1b). If it injects ≥1 cursor, set `branch_cursors` to those revives and return `AmbiguityFanout{…}` (re-enter the loop) instead of `Error`. Guarded by `!b2_crosswrap_disabled() && !b3_disabled() && !pending`-empty bypass: the retention only fires when the cohort cache holds an eligible outer-cast splice (§2.1b predicate). On cast-free chains the predicate is `false` (entries empty) → byte-identical Error path → Welch-neutral. This is the SOLE site that changes the residual's control flow (the M4.0-proven Error exit, NOT the no-op `!progress_made` site).

**2.1b — NEW `dispatch_cohort.rs::DispatchCohortCache::take_outer_cast_revival(&mut self) -> Vec<CrossWrapSpliceJob<W>>` + walker `revive_outer_cast_members(&mut self) -> usize`.** A take-once EOI-time drain DISTINCT from the per-step `take_pending_for_drain_crosswrap`:
- Scan every Resolved `R` with ≥1 non-terminal snapshot.
- For each paused-member-bearing sibling `K_sib` with `K_sib.equiv() == R.equiv()` ∧ `K_sib != R` ∧ `K_sib.pos == R.pos_at_dispatch` ∧ `(K_sib, R.symbol_id) ∉ crosswrap_drained`:
  - **EOI eligibility (NOT clause 4):** `K_sib` is `Resolved` with `sib_hi == R.hi_pos` (the member's OWN wrap resolved at the SAME full span as `R` — i.e. the member's body is complete and equals `R`'s span) AND `K_sib` still carries non-empty `pending_members`. This is the EXACT complement of the per-step clause 4's `sib_hi < R.hi_pos`: it captures the outer cast member whose own wrap self-resolved at full span (the `Resolved(>=hi)` state §1.3) but whose paused continuation never fired. **It does NOT relax clause 4** — clause 4 stays verbatim in `take_pending_for_drain_crosswrap` (forward, per-step). This is a SEPARATE EOI-only drain with a SEPARATE eligibility, reached only when the per-step path has exhausted (frontier collapse / parked fixpoint).
  - Materialize the member (`materialize_branch_cursor`, REUSE) and emit one `CrossWrapSpliceJob` per `(member × non-terminal R snapshot)`. Mark `(K_sib, R.symbol_id)` in `crosswrap_drained` (shared take-once set — §3).
- Walker `revive_outer_cast_members` calls `revive_cohort_member_with_snapshot` (`:13865`, REUSE) on each job, pushing `Frame::Concrete` cursors into `branch_cursors`; returns the count.

**Why this is principled, not premature-disambiguation.** It ADDS the resolved body to a member whose body provably resolved (the Resolved `R` at full span IS the evidence the body parses) and whose continuation was dropped purely by splice timing. It removes NO alternate and applies NO weight/threshold. `Ambiguous` stays first-class (revives flow through `merge_equivalent_cursors`/SPPF-dedup at `:9621` on the re-entered step). The parens-inner steal stays excluded because §2.1b requires the member to have a paused continuation AND `R` to be a DISTINCT-wrap full-span resolution — the parens-inner case self-resolves its own span as the SOLE wrap with no distinct outer wrap awaiting it (verified: `cross_cat_with_parens` members are `Resolved(>=hi)` with the SAME wrap as `R`, so `K_sib == R` clause-1-fails, or no distinct outer member exists).

**2.1c — §3d backstop + per-step drain UNCHANGED.** `crosswrap_backstop_for_pausing_member` (`dispatch_cohort.rs:1244`) and `take_pending_for_drain_crosswrap` (`:791`) bodies are byte-identical. The fix is purely ADDITIVE: a new EOI-time drain + a new pre-Error retention site. Both behind `!b3_disabled()`.

## 3. TERMINATION (no new cap, no heuristic — the existing take-once set IS the certificate)
`take_outer_cast_revival` inserts each spliced `(K_sib, R.symbol_id)` into the SAME monotone non-shrinking `crosswrap_drained: FxHashSet<(DispatchKey, SppfId)>` (`dispatch_cohort.rs:458`; insert mirrors `:978`; cleared ONLY at parse boundary `:506`). It SKIPS any pair already present. So each `(K_sib, R-body-symbol)` is outer-revived AT MOST ONCE across the whole parse — and it shares the set with the per-step drain + §3d backstop, so a pair drained by ANY path is never re-spliced by ANY other. Each pre-Error retention (§2.1a) that injects ≥1 cursor strictly grows `crosswrap_drained` (it fired ≥1 job ⇒ ≥1 new pair inserted; an EOI revival that finds no undrained eligible pair returns 0 → the Error path proceeds normally). Therefore the number of retention-fires ≤ `|crosswrap_drained|` ≤ #distinct `(DispatchKey, SPPF-symbol)` ≤ #DispatchKeys × #SPPF-symbols — grammar/input-bounded (#levels × #wraps × #members, members ≤ `MAX_PENDING_COHORT_PER_KEY = 16`, `dispatch_cohort.rs:1181`). Between retentions, the re-entered fanout either makes structural progress (normal parse) or its revives Drop and re-trigger retention — but each retention consumes ≥1 fresh pair, so the loop reaches `Error` (no eligible pair left) or `Accepted`/parked in finitely many steps. `max_steps` (`run_to_end_of_input:3974`, `Err(WpdaMaxStepsExceeded)`) is the hard backstop. Disjoint from `MAX_REVIVAL_ROUNDS = 4` / `revive_orphaned_cohort_members_once` (`:8953`, EOI orphan path, only REMOVES `InFlight` entries; runs FIRST at `!progress_made`; no shared counter). M5.3 asserts retention-fires ≤ `crosswrap_drained.len()` AND `crosswrap_splices_total` ≤ the static product.

## 4. Invariants preserved
- **WPDA end-to-end disambiguation.** Only ADDS sound cursors (the member's body provably resolved = the Resolved `R` at full span); `K_sib` is NEVER removed; no weight/cost/threshold picks a winner; `Ambiguous` first-class (revives merge via `:9621` + SPPF-dedup). The fix STOPS a premature drop-by-non-evidence (§1.5).
- **Clause-4 own-wrap-gate UNCHANGED.** `take_pending_for_drain_crosswrap`'s clause 4 (`sib_hi < R.hi_pos`, `dispatch_cohort.rs:846`) is byte-identical → `cross_cat_with_parens` (`calculator.rs:2159`) STAYS GREEN (parens-inner self-Resolved-at-equal-hi steal excluded at every per-step level). §2.1b's EOI eligibility (`sib_hi == R.hi_pos` + distinct-wrap + paused-member) is a SEPARATE predicate on a SEPARATE EOI-only drain — it does not touch the forward path.
- **EquivKey narrow, cache full DispatchKey.** §2.1b reads `K_sib.equiv()` (READ-only, `:131`); cache + `crosswrap_drained` + `cohort_origin` stay full `DispatchKey` → M4 un-conflation 67>23 holds.
- **Preserve the closed set.** Blocker-1 descriptor + Blocker-2 single-level splice + §3d backstop + M4 keying + C-bis Newton (`realize` walk `:4680`, disjoint) intact. 16 M4-fixes + 8/9 M4-regressions + `test_nested_int_int` bonus unaffected (the per-step path is byte-identical; the fix only fires when the per-step path collapses).
- **Welch-neutral on cast-free chains.** §2.1a's retention is reached only at `branch_cursors.is_empty()` AND guarded by the §2.1b predicate (`false` on chains — `entries` empty); §2.1b is O(entries) = O(1) on chains. The per-step hot path (`:9752` non-empty-`pending` guard) is untouched → byte-identical → NEUTRAL/WIN.

## 5. Milestones (ONE worktree `/var/tmp/wt-b3-liveness`; serial FOREGROUND `cargo`; `systemd-run --scope` runs sync, do NOT pass `--wait`; self-clean + verify `pgrep -x rustc`==0; main tree NEVER modified). Env gates: `SIGB_CROSSWRAP`/`SIGB_TRACE`/`B2_DISABLE` (reuse) + NEW `B3_DISABLE` (skips ONLY §2.1a retention + §2.1b → falls back to EXACTLY Blocker-2's 8/9, for A/B).

- **M5.0 — DIAGNOSTIC-CONFIRM gate FIRST (INERT; like M3.0/M4.0).** Reuse the M4.0 read-only machinery (`b3v2-m40-ONLY-delta.diff`: `crosswrap_climb_diagnostic`, the `ELIGIBLE`/`EXCLUDED`/`BACKSTOP-PAUSE`/`ALL_DROPPED` `SIGB_CROSSWRAP` prints, `b3_climb_trace.rs` harness). Add a `SIGB_CROSSWRAP` print of `RegisterOutcome` at `:13631` and, at the `step_fanout` drop site (`:9954`), a print of the would-be `take_outer_cast_revival` candidate count (READ-ONLY: scan only, no insert/materialize). **GATE — all four `[CONFIRM-n]` of §1 reproduced:** (1) ≥1 `InflightCollision` at the residual's `(`-dispatch; (2) a Resolved sibling `R.pos_at_dispatch==8 ∧ R.hi_pos==9` exists AND the per-step drain emits ≥1 job for it; (3) the residual exits `step_fanout`→`Error("all fork branches dropped")` with `branch_cursors==0` (NOT `!progress_made`); at the drop site, `take_outer_cast_revival` candidate count ≥1 (an outer cast member with `sib_hi==R.hi_pos==9` + paused members + undrained pair EXISTS); (4) `int(int(5,32),32)` reaches `EXIT=fanout_!progress_made branch_cursors>0` (outer survives) while the residual does not. **Mechanical:** gauntlet 4220/0; calc == Blocker-2 210/7; chain Welch NEUTRAL (all additions inert). **If CONFIRM-3's candidate count is 0 at the drop boundary (no eligible outer-cast member with a paused continuation + full-span Resolved sibling) → the mechanism is FALSIFIED → STOP+report; do NOT wire §2.1a/§2.1b, do NOT relax clause 4.**

- **M5.1 — wire §2.1b (`take_outer_cast_revival` + `revive_outer_cast_members`) + §2.1a (pre-Error retention), gated `!b3_disabled()`.** **GATE:** ALL 9 M4-regressions GREEN (5 Sig-B incl. `bool_prefix_tokens` + 4 realize) + `cross_cat_with_parens` + 16 M4-fixed + `parse_int_cross_cat_comparison_{ge,ne,lt,le}` + `_in_expression` + nested `test_nested_{int_int,float_float_int}` / `test_triple_nested_float` GREEN + gauntlet 4220/0. **The 3 M3.1-regression sentinels (`cross_cat_dispatch_chaining` `:2119`, `cross_cat_with_floats` `:2272`, `test_nested_int_int` `:1064`) MUST stay GREEN.** With `B3_DISABLE=1` ⇒ EXACTLY Blocker-2's 8/9 (residual returns) — proves the liveness retention is the SOLE new lever. Calc 213/4. If any sentinel regresses OR a target is unmet → STOP+report.

- **M5.2 — full sweep.** gauntlet 4220/0; C-bis cycle/newton/tarjan/star/scc/self_loop 0-fail; op-suites gen_calculator_op ≥1331 / gen_rholang_op 532; soundness + `-3!` (edge_case 229/0 + probe_neg_zero 23/0) + parity 16/0 + cross_cat 2/0; rholang pre-existing-fail ≤8.

- **M5.3 — TERMINATION + Welch + ambiguity.** TERMINATION test (`int(y != true > x < "qua")` + synthetic 5-op + 6-op chains all parse AND RETURN; instrument + assert retention-fires ≤ `crosswrap_drained.len()` AND `crosswrap_splices_total` ≤ the static product) + **interleaved Welch chain panel N≥51 + chain_1000/2000 RSS +5% max** (control `B2_DISABLE=1`; predict NEUTRAL/WIN — retention never fires on cast-free chains) + ambiguity-preservation probe. Save `git diff > /var/tmp/suite-green/sigb-cast-family-FINAL.patch` + a `b3v2-…`-style M5.k delta diff (pure additions).

## 6. GATES (= cast family fully closed) → merge `wip/cast-family-cohort` into `feature/wfst-architecture` + commit
ALL 9 M4-regressions green (incl. `bool_prefix_tokens`) + `cross_cat_with_parens` + 16 M4-fixed + the 3 M3.1-sentinels (`cross_cat_dispatch_chaining`, `cross_cat_with_floats`, `test_nested_int_int`) STAY GREEN + `test_nested_float_float_int` + `test_triple_nested_float` GREEN; gauntlet 4220/0; C-bis 0-fail; op-suites ≥1331/532; soundness + `-3!` + parity 16/0 + cross_cat 2/0; Welch chain panel N≥51 no LOSS + chain RSS +5% max; ambiguity-preservation; TERMINATION (deep + synthetic-deeper return, bounded retention-fires + splices, no hang); `B3_DISABLE=1` restores EXACTLY Blocker-2's 8/9. Zero regression.

## 7. Risks
- **R1 non-termination** → monotone `crosswrap_drained` take-once certificate (§3); no new cap; `max_steps` backstop; M5.3 asserts bounded fires/splices.
- **R2 mechanism falsified at M5.0** → the M5.0 CONFIRM gate is FIRST + INERT; if CONFIRM-3's candidate count is 0 at the drop boundary, STOP (do NOT wire, do NOT relax clause 4). The existence proof: §1.2's verified `wrap:(0,2)` Resolved at `pos_at_dispatch=8/hi=9` with paused-member siblings present.
- **R3 over-fire / parens regression** → clause 4 UNCHANGED in the forward drain; §2.1b is a SEPARATE EOI-only predicate requiring a paused-member-bearing DISTINCT-wrap sibling; `cross_cat_with_parens` + `_{ge,ne,lt,le}` + `_in_expression` sentinels; `B3_DISABLE=1` must restore 8/9.
- **R4 chain Welch/RSS** → retention reached only at `branch_cursors.is_empty()` + §2.1b predicate `false` on chains (O(1) empty entries); per-step path byte-identical → NEUTRAL/WIN.
- **R5 EquivKey leak** → `K_sib.equiv()` READ-only; cache + `crosswrap_drained` full DispatchKey; 67>23 re-asserted.
- **R6 retention re-enters Error infinitely** → each retention that injects ≥1 cursor strictly grows `crosswrap_drained`; a retention with no undrained eligible pair returns 0 and the Error proceeds (terminates that branch). M5.3 instruments the fire count.
- **R7 spliced outer revive still Drops (doesn't reach accepting)** → if the genuine outer member's `return_frame` + `snap.worker_inner_state` does NOT reconstruct a `)`-consuming BinderRule that reaches `is_accepting_config`, M5.1 will show the target unmet under FULL-B3 → STOP+report (the member's continuation is structurally insufficient and a different splice-shape is needed). M5.0's CONFIRM-3 candidate-count proves the member EXISTS; M5.1 proves the revive ACCEPTS. This is the residual risk the prior two designs never reached because they died earlier.

## 8. Critical sites (post-patch lines = `6507b9c` + COMPLETE.patch, on `wip/cast-family-cohort` @ `3d5d94e`)
- `prattail/src/wpda_walker.rs`:
  - `step_fanout` `:9182`; per-cursor loop `CursorOutcome::Drop` discard `:9467`; cohort drain block `:9752`; per-step cross-wrap drain call `:9876`; `self.branch_cursors = new_cursors` `:9896`; **drop site `branch_cursors.is_empty()` → `Error("all fork branches dropped")` `:9954` (INSERT §2.1a pre-Error retention HERE)**; NEW `revive_outer_cast_members` next to `step_fanout` (~`:9229`, mirrors `revive_orphaned_cohort_members_once:8953`).
  - `run_to_end_of_input` `:3950`; top-of-loop `is_terminal()` early-return `:3981` (the residual's actual exit — confirms §2.1a, not `!progress_made:4082`, is the right site).
  - `revive_cohort_member_with_snapshot` `:13865` (REUSE for §2.1b revives); `cursor_gss_pop_via_edge` resolve+insert `:14375` (`pending_cohort_drain_keys.insert` `:14455/:14459`, UNCHANGED); `apply_pop_body_to_cursor` `:13992` (cast fire `emit_fire_action` `:14064`, UNCHANGED); `is_accepting_config` `:5620`; `is_cursor_accepting_terminal` `:11762`; `allocate_fork_push_child` register `:13628` + `RegisterOutcome` match `:13631` (M5.0 trace site); §3d backstop call `:13660`; gates `b2_crosswrap_disabled` `:83` + NEW `b3_disabled` (~`:90`) + `sigb_crosswrap_walker_trace` (reuse from M4.0 delta).
- `prattail/src/dispatch_cohort.rs`:
  - `DispatchKey` `:73` / `equiv` `:131`; `EquivKey` `:150`; `DispatchCacheEntry` `:208`; `take_pending_for_drain_crosswrap` `:791` (clause 4 `:846`, UNCHANGED body); **NEW `take_outer_cast_revival` next to it (~`:1000`, EOI-only `sib_hi==R.hi_pos` + paused-member predicate, shares `crosswrap_drained`)**; `crosswrap_drained` `:458` / `crosswrap_splices_total` `:463` / clear `:506`; `crosswrap_backstop_for_pausing_member` `:1244` (UNCHANGED); `pause_cohort_member` `:1171` (REUSE); `drain_orphaned_inflight_members` `:1034` (orphan precedent, UNCHANGED); `MAX_PENDING_COHORT_PER_KEY` `:1181`; `CrossWrapSpliceJob` struct (REUSE for the new drain's job type).
- `languages/tests/calculator.rs`: `simulator_regression_bool_prefix_tokens` `:2176` (residual input `:2188`); `simulator_regression_cross_cat_with_parens` `:2159`; `simulator_regression_cross_cat_dispatch_chaining` `:2119`; `simulator_regression_cross_cat_with_floats` `:2272`; nested `test_nested_float_float_int` `:1054` / `test_triple_nested_float` `:1059` / `test_nested_int_int` `:1064`; NEW TERMINATION test.
- `prattail/src/cohort_lazy.rs` (`materialize_branch_cursor` / `CohortShell` / `CohortMemberState` REUSE); `prattail/src/gss.rs` (`EdgeKind::CrossCatProjection` `:418`, `edge_kind` `:672` — the wrap carrier, UNCHANGED).
