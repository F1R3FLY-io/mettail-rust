# FV Coverage Matrix — mandate/invariant → Rocq → Rust → test → status

**Phase 1 of the Lazy-WPDA-Pipeline + FV-Cleanup plan.** A *living* matrix: every phase
updates it. It exists so we never (a) ship a Rust change without a non-vacuous proof, or
(b) believe a property is proven when its proof is vacuous. Pair with the baseline at
[`baseline-cf03e571.md`](baseline-cf03e571.md).

**Status legend:**
`proven` = non-vacuous Rocq, green under `make -C formal check-capped`, models the real code ·
`modeled-CE` = proven over a faithful model + a code-level counterexample/flip test ·
`regression-only` = guarded by a Rust test, no Rocq yet ·
`active-parser-risk` = retained audit item for the active WPDA parser/recognizer
path, not evidence for the Dovetail/Rho CESK runtime-backend replacement ·
`ascent-reference-note` = retained note about the Ascent reference/oracle path,
not a CESK runtime-backend claim ·
`cesk-runtime-backend-risk` = retained audit item for the CESK runtime backend
being replaced by the Rho machine backend ·
`obsolete-proof` = a proof artifact that is intentionally not used as evidence
for Dovetail/Rho correctness.

---

## A. Plan invariants → evidence

| # | Invariant | Rocq theorem(s) | Rust site(s) | Test(s) | Status |
|---|-----------|-----------------|--------------|---------|--------|
| 1 | No dropped alternatives; dedup only by exact tagged key (never 64-bit hash) | `RuntimeModel.v::exact_key_pair_dedup_preserves_distinct_keys`; `hash_only_pair_dedup_can_drop_distinct_keys` (negative); `ExactReachabilityDedup.v::legacy_seed_expands_all_exact_keys`, `exact_successor_preserved`, `legacy_collision_keeps_both`, `id_only_dedup_can_drop_exact_candidate` | `language.rs::from_alternatives` (semantic_fingerprint dedup → `Ambiguous`); `ReachableNormalForms` keys traversal by `ReachKey::{Exact,Legacy}` and expands colliding legacy ids to all exact keys | `-3!` family; gen_*_op; `reachable_normal_forms_use_exact_keys_despite_term_id_collision`; `weighted_reachable_normal_forms_use_exact_keys_despite_term_id_collision` | **proven** for parser assembly and eval NF reachability; guarded by codegen and collision regressions |
| 2 | Ambiguity end-to-end until evidence rejection | `RuntimeModel.v::parser_preserves_ambiguous_alternatives`, `semantic_rejection_preserves_unrejected_siblings`; `EvidenceComplete.v` (closure meta-property) | `from_alternatives`, `substitute_env` (Ambiguous arms) | `-3!`, ambiguity_exposure_*; `from_alternatives_deduplicates_only_by_semantic_fingerprint` | **proven** (parse) + **proven** closure meta-property (`EvidenceComplete.v`, `31bb90bc`: weights order never prune; removal only by evidence) + **guarded** codegen fence |
| 3 | Zero `Admitted`/`Axiom`; every change ships a non-vacuous proof | `formal/scripts/check_rocq_zero_admission.py` for critical Dovetail/Rho Rocq suites, plus suite compilation | — | `make -C formal check-capped`; focused target `FORMAL_CAPPED_TARGET=rocq-critical-zero-admission` | **proven + guarded** for Dovetail/Rho replacement evidence: comments are stripped before scanning, and real `Admitted`, `admit.`, `Axiom`, `Conjecture`, `Parameter`, or `Parameters` commands fail the capped gate |
| 4 | Baseline-relative empirical gates | — | — | baseline-cf03e571.md | **proven** (baseline captured: 4350/0 lib + 217 known-red) |
| 5 | Commit at stable points; prove root (flip) before naming a fix | — | — | git history | process invariant |

## B. Mandate areas → evidence

| Area | Rocq | Rust | Status |
|------|------|------|--------|
| State-space bounding (e-graph budget) | `EGraphBudgetDedup.v` (no-overshoot, explicit overflow, dedup membership+NoDup) — **proven from scratch** | `egraph.rs::{try_add_with_budget,rebuild_exact_indices,apply_matches_bounded,SaturationResult::node_limit_reached}` | **proven** (compile-time only; NOT the eval-fixpoint bound — Phase 3 adds that) |
| Recognizer realization laziness | `RuntimeModel.v` (demand-bounded realize; arena-order) | `facade.rs::__mettail_wpda_collect_prefix`; `wpda_walker.rs` realizers | **active-parser-risk**: this remains part of the active WPDA parser/recognizer audit. It is not CESK runtime-backend evidence; Dovetail/Rho extraction and scheduling evidence lives in `dovetail/formal/rocq/` and `formal/rocq/rho_bridge/`. |
| Evaluator demand-laziness | — | Ascent `prog.run()` eager fixpoint | **ascent-reference-note**: this is an Ascent reference/oracle path limitation, not a CESK runtime-backend claim. Dovetail/Rho CESK runtime-backend evidence is tracked by the Dovetail extraction/saturation proofs and Rho scheduling bridge proofs below. |
| Eval NF dedup soundness | `ExactReachabilityDedup.v` (zero-admission; cite invariant 1) | `runtime/src/language.rs` exact `RewriteSeed`/`WeightedRewriteSeed`, `TermInfo.exact_key`, `Rewrite.{from_key,to_key}`, `ReachableNormalForms` visited frontier over exact keys when available | **proven + guarded**: legacy id callers expand to every matching exact key before traversal; exact successors preserve distinct candidates; id-only dedup has an explicit negative witness |
| Cost / weight axis | reuse f1r3node-rust `CACostMonadInstances.v` ("R-C obstruction"), `JoinConservation.v`, `FuelGateSafety.v`, `UseCaseAdequacy.v` (55 thms) | (Dovetail adapter) | **proven upstream**; engine adds Δ1 min-cost-matching join (new) |
| Lexer laziness | — | `automata/codegen.rs::lex` (eager `Vec`), `runtime_types.rs::lex_dag_core` | **active-parser-risk**: retained for active lexer/parser audits; it is not a Dovetail/Rho runtime-flip gate. |

## C. Active Parser, Ascent Reference, and CESK Runtime Notes Excluded From Dovetail/Rho Evidence

These rows are retained so they are not accidentally cited as proof evidence.
They are not active Dovetail/Rho CESK runtime-backend replacement tasks unless
explicitly tagged as CESK runtime-backend work.

| Item | Why excluded from Dovetail/Rho evidence | Classification |
|------|-----|------------------|
| `BCG05_GuardKey.v` uses `normalize := identity` | obsolete guard-key model; Dovetail exact-key and requirement coverage proofs are the CESK runtime-backend replacement evidence | **obsolete-proof** |
| `WpdsSimulation.v` is parser-PDA-only | valid only as parser evidence; it does not model rewrite evaluation | **active-parser-risk** |
| `RuntimeModel.v:1419-1493` static count-lemmas ("lazy ≡ eager") | structurally true but not evidence for the realizer's ordering behavior | **obsolete-proof** |
| Eval NF dedup on 64-bit `DefaultHasher::finish()` | **fixed and guarded**: id-only dedup can drop a distinct exact candidate, so runtime traversal now prefers exact keys and expands legacy ids to all exact-key representatives | Phase 3a closed by `ExactReachabilityDedup.v` + collision regressions |
| Lexer item 206 `claimed_done` but `verified_at: null` | eager materialization in the active lexer path | **active-parser-risk** |
| `from_alternatives` weight-drop "violation" (plan premise) | **fixed and guarded** (Phase F.13 Stage 2.3.1: semantic_fingerprint dedup → Ambiguous; Phase EC source fence) | `EvidenceComplete.v` proves weights order rather than prune; `from_alternatives_deduplicates_only_by_semantic_fingerprint` rejects weight/groundness/declaration/display selectors in the generated-body site |

## D. Per-phase NEW Rocq obligations (created as each phase lands)

| Phase | New `.v` | Key theorems | Status |
|-------|----------|--------------|--------|
| EC | `EvidenceComplete.v` (prattail_wpda_runtime, zero-admission, `31bb90bc`) | **proven and guarded**: `no_valid_alternative_dropped`, `evidence_only_removal` (removal = observational-equiv merge), `weight_is_order_only` (kept key-set invariant under reweighting), `assemble_keys_nodup` (residual ambiguity surfaced), `weight_drop_can_lose_valid_alternative` (negative fence). `from_alternatives` (language.rs:722) remains evidence-complete: semantic-fingerprint-only dedup, no weight/groundness/declaration/display selector. The source-level guard `from_alternatives_deduplicates_only_by_semantic_fingerprint` keeps that Rust/codegen site aligned with the Rocq model. |
| 4A | `CD07_NfaFallbackNonLoss.v` | fanout_complete; fanout_sound; lexmin_orders_not_prunes | **proven** (zero-admission, committed `81ad0aa8`) + shipped_drops_boundary, selection_is_member, shipped_spillover_loss (negative fence), nfa_fallback_nonlossy, fixed_empty_boundary_not_present. Both latent-loss sites fixed; dispatch_strategy consumers re-scoped (dead-rule lint + NFA-spillover refinement only — the plan's "Walker Fork" premise was stale) |
| 5A | (`RuntimeModel.v` parser/recognizer extension) | cast_edge_source_roundtrip; eoi_ordering_is_permutation | **active-parser-risk**: tracked for the active parser path, not a Dovetail/Rho CESK runtime-backend obligation |
| 5C | (`RuntimeModel.v` parser/recognizer extension) | budget_overflow_is_surfaced; under_budget_preserves_all; lazy_member_counted_once | **active-parser-risk**: tracked for the active parser path, not a Dovetail/Rho CESK runtime-backend obligation |
| 5B | `BCG07_CongruenceSeedKey.v` | congruence_seed_key_disjoint; congruence_propagation_complete | **obsolete-proof**: BCG guard-key proof work is Ascent codegen optimization evidence, not a CESK runtime-backend obligation; Dovetail exact-key coverage is the CESK runtime-backend replacement evidence |
| 2 | `LazyRecognizer.v` | realized_prefix_is_eager_firstn; realization_prefix_stable; budget_overflow_reported_not_pruned | **active-parser-risk**: tracked for the active parser path, not a Dovetail/Rho CESK runtime-backend obligation |
| 3a | `ExactReachabilityDedup.v` | `legacy_seed_expands_all_exact_keys`; `exact_successor_preserved`; `legacy_collision_keeps_both`; `id_only_dedup_can_drop_exact_candidate` (negative fence) | **proven + guarded**: eval normal-form reachability is re-keyed by exact semantic keys when available, legacy seed ids fan out to all exact candidates, and runtime tests cover unweighted and weighted collision cases |
| 3 | `LazyEvalEnum.v` + `DemandTransform.v` | demand_eval_equals_eager_in_the_limit; demand_only_computes_reachable; enum_take_k_is_eager_quotient_prefix; fixpoint_bound_reported_not_pruned | **ascent-reference-note**: this belongs to the Ascent fixpoint-demand plan, not the CESK runtime backend. Dovetail/Rho uses the Dovetail extraction/saturation and Rho bridge proofs instead |
| 4B | `CD06_SuffixFactor.v` | factor_eq_matching_rule; factor_sound/complete; factor_language_eq | **proven** (zero-admission, all 8 theorems `Closed under the global context`): central `factor_eq_matching_rule` is exact match-LIST equality (labels + grammar order + multiplicity, NO disjointness precondition) ⇒ `factor_sound`/`factor_complete`/`factor_language_eq` + `factor_preserves_ambiguity_degree` are corollaries; T5 `degraded_path_preserved`/`residual_subset` (ineligible alternatives untouched); `factored_aux_counts_eligible` ties the model to the M1.0 measurement. **VERDICT: CD06 STOPPED at diagnostic-only** — measured depth2 ratios (calculator 0.19, rhocalc 0.42, Ambient 0.57, GuardedRho 0) EXCEED the 0.10 screen, but every depth-2 bucket is already leading-literal-disjoint under CD02 top-down dispatch ⇒ a shared tail is parsed once either way ⇒ factoring saves code size only, ZERO parse work ⇒ transform NOT wired (no suffix_trie.rs, no `Optimization::SuffixFactoring`); the I17 `cd06-shared-suffix-measure` diagnostic is retained |

## E. Engine (Dovetail) obligations — `dovetail/formal/rocq/`

**dovetail RUST core SHIPPED + capped-test verified** — Increments 1 (rigail
extraction), 2 (key), 3 (e-graph), 4 (WTA view), 5 (exact no-miss extractor), 6 (Newton-SCC
cyclic closure), 7 (rules-as-data saturation), 8 (tuplespace seam). **Engine FV now COMPLETE
(zero-admission): the extractor MISSES NOTHING across the candidate SET, the ORDER, the
RECURSION, and the cyclic WEIGHT, with cycle-cut boundedness explicit in the Rust API.**
Dovetail also has a formal MeTTaIL rewrite-requirements coverage taxonomy: every current
`LanguageDef` rewrite/equation/fold/guard/pattern class is classified as covered by a
Dovetail core capability or by an explicit external/native/Rho handler contract. Native/Rho
contracts are not core-proven Dovetail behavior; their operational-correspondence obligations
remain in M-RHO.*, below.

| Obligation | Source | Status |
|------------|--------|--------|
| Exact runtime e-graph dedup no-loss (Inc 3) | `ExactKeys/ExactKeyDedup.v` (rocq-dovetail, zero-admission) + invariant 1 | **proven**: exact-key dedup preserves every key, distinct keys are not conflated, add-with-budget never overshoots, overflow preserves state and reports refusal |
| Rules-as-data saturation (Inc 7) | `Saturation/DovetailSaturation.v` (rocq-dovetail, zero-admission) | **proven**: saturation steps are monotone; iterated saturation is monotone; generated-fact soundness preserves state soundness; bounded execution reports `Converged`, `NodeLimit`, or `IterationLimit` explicitly |
| WTA no-miss extraction — SELECTION layer (Inc 5) | `Extraction/NBestExtraction.v` (rocq-dovetail, zero-admission, committed `98a2d0d`) | **proven**: only-`0̄`-removal, no-miss (`select_complete`), equal-weight-both-survive, resumability (`select_prefix_monotone`), exhaustive-on-demand |
| WTA extraction — best-first ORDERING (Inc 5) | `Extraction/NBestExtraction.v` (`select_ordered_sorted`/`_perm`/`_complete`, zero-admission, committed `2ff227b9`) | **proven**: output sorted best-first; a permutation of the kept candidates (ordering reorders, never drops); every non-`0̄` alternative present |
| WTA extraction — lazy frontier ORDERING (Inc 5) | `Extraction/LazyFrontierOrder.v` (rocq-dovetail, zero-admission) | **proven**: heap-popped traces are sorted under the monotone-best-order successor condition; emitted elements plus final frontier are a permutation of the initial frontier plus generated successors; bumping one child rank cannot produce a strictly better candidate |
| WTA extraction — hypergraph-recursion COMPLETENESS (Inc 5) | `Extraction/EnumerationCompleteness.v` (rocq-dovetail, zero-admission, committed `4d482514`) | **proven**: `class_enum_complete` (EVERY hyperedge + EVERY valid rank-vector enumerated — NO derivation dropped), `enum_vectors_complete`/`_sound`, `class_enum_sound`; the per-edge cartesian product unioned over edges = Huang-Chiang Alg.3 candidate set. 9 Rust tests T1-T9 are the operational cross-check |
| WTA extraction — ordered child-key framing (Inc 5) | `Extraction/OrderPreservingFraming.v` (rocq-dovetail, zero-admission) | **proven**: ordered framing is prefix-free, injective, and preserves lexicographic child-key order; distinct derivation keys cannot be conflated by the child-key encoding |
| Cyclic inside-weight closure (Inc 6) | `InsideWeights/InsideWeightSccClosure.v` (rocq-dovetail, zero-admission) | **proven**: `lowering_factor_faithful`/`lowered_eq_recurrence`/`lowering_preserves_fixpoints` (the SCC->PackingFactored re-indexing is faithful + fixpoint-preserving), `star_closure_is_lfp` (scalar/self-loop closure = exact LEAST fixpoint = aggregate over all cycle-unfolded derivations), `trivial_scc_constant` (trivial-SCC `continue` sound), `bool_cka` (non-vacuity). n-D multi-call Newton CONVERGENCE cited (Esparza-Kiefer-Luttenberger 2007, implemented in rigail). Commutativity precondition is enforced by sealed `CommutativeStarSemiring`; currently only `TropicalWeight` implements the cyclic closed path |
| Cyclic e-class k>=2 enumeration closure (Inc 6) | `Extraction/CycleCutBoundary.v`, `Extraction/CyclicEnumerationImpossibility.v`, `Extraction/ExtractionOutcome.v`, extractor `on_stack` cycle guard, checked `Derivations::next_checked()`, `had_cycle_cut()` | **proven bounded-by-design** (NOT a silent miss): exact cyclic INSIDE weight via Newton (1-best/admissible-heuristic exact, proven above); productive cyclic derivation spaces are not finitely exhaustible (`no_finite_list_contains_all_cycle_unrollings`, `productive_cycle_cannot_claim_finite_complete`), so finite k>=2 lazy enumeration over cyclic SCCs is CUT and SURFACED by `BoundedByCycleCut`/`had_cycle_cut()` rather than reported complete |
| MeTTaIL rewrite-requirement coverage | `Requirements/MeTTaILRewriteCoverage.v` + `ast/tests/dovetail_rewrite_coverage.rs` | **proven + audited taxonomy**: equations, directional rewrites, congruence, folds/native handlers, freshness/env/relation/forall/behavioral/synthetic guards, collection/map/zip/binder/substitution patterns, exact keys, budget reports, demand/ambiguity, cyclic weights/boundaries, and Rho contracts are classified. Dovetail-core capabilities are covered here; external/native/Rho handler contracts remain separate implementation obligations |
| Runtime/Rho report boundary | `Refinement/RuntimeReportBridge.v`, `Refinement/RhoReportHandoff.v` + `dovetail::report` | **proven + Rust-tested**: checked extraction converts to a substrate-neutral report that preserves root order, exact `ContentKey` term identity, deduplicated term records, derivation-tree child order, and terminal completeness. `BoundedByCycleCut` cannot be asserted complete, and the formal Rho handoff rejects bounded reports without observations; complete handoff observations are exactly the Dovetail report roots in extractor order and every observed key has a report term record from the extraction forest |
| Rust-facing refinement bridge | `Refinement/RustModelBridge.v` | **proven**: Rust-shaped candidates, budget results, rank vectors, ordered framing, lazy frontier ordering, checked extraction outcomes, and cycle reports preserve the existing Rocq extraction/budget/cycle capstones |
| Actual `LanguageDef` inventory | `Requirements/LanguageDefInventory.v` + `ast/tests/dovetail_language_inventory.rs` | **proven + audited**: current in-repo `language!` macro bodies are parsed and classified; inventoried requirements are covered with no silent delegation |
| Pattern/premise lowering coverage | `Lowering/PatternLoweringSoundness.v` | **proven**: recursive pattern and premise lowering requirements map to covered Dovetail requirement constructors |
| Newton-SCC solver boundary | `Rigail/NewtonSccAdequacy.v` | **proven boundary**: scalar/self-loop closure is mechanized; n-D Newton adequacy is represented as an explicit proof-carrying solver contract, never as a global axiom |
| Pure arithmetic SMT + Rust Creusot budget contract | `dovetail/formal/why3/key_budget_contract.mlw` via `why3-dovetail-budget`; `dovetail/formal/creusot` via `creusot-dovetail-budget` | **proven Rust-level budget contract**: Why3 proves budget arithmetic directly; Creusot verifies `try_add_with_budget` plus the e-graph-shaped `admit_enode_with_budget` contract covering existing-node success, fresh-node add, fresh-node refusal at the limit, no overshoot, and sticky node-limit flag preservation/update |
| spec→Rholang-VM operational correspondence (up-to-weak-bisim) | `formal/rocq/rho_bridge/theories/LinearCommCorrespondence.v` + `CommReductionCorrespondence.v` + `RhoCommScheduleFamily.v`; `formal/process/rho_comm_slice.json`; `formal/mcrl2/rho_machine/`; `formal/maude/rho_machine/`; `formal/tla/rho_machine/`; `mettail-rho-runtime/tests/rho_comm_oracle.rs` | **proven bridge models + generated finite process/scheduler projection + runtime oracle**: linear M-RHO.1 COMM consumes matched send/receive and has sound/complete lowering correspondence; monotone rules-as-data correspondence remains proven for the saturation layer; `RhoCommScheduleFamily.v` proves for every finite independent-redex schedule that Rho reserve/fire traces erase to the same visible observations as direct Dovetail fire traces, full permutation schedules enable completion, missing-redex prefixes reject completion, and permutation schedules preserve the fired-redex set; mCRL2/Maude/TLA+ files are generated from one bounded four-redex COMM-slice specification and drift-checked before verification; mCRL2 model-checks the RhoNet/Dovetail COMM projection where Rho has hidden reserve actions and proves no deadlock, all 24 visible fire schedules, completion enablement after each full schedule, and branching bisimilarity modulo those hidden reserve actions; Maude rewrite-checks the same internal-reserve fragment for unique matching terminal normal forms, one-step independent redex witnesses, all 24 visible fire/complete traces on both projections, and unreachability of every premature visible completion trace with fewer than all four fires; TLA+/Apalache/TLC checks the bounded four-redex scheduler invariants and weak-fair eventual completion; runtime oracle covers the transport-pure corpus plus deterministic one-bind race enumeration; generated backend artifacts execute through validated normalized `Par`, raw `Par` execution is oracle/debug only, and source text remains hand-authored regression coverage |
| Bridge one-way dependency (M-RHO.0.0) | `BridgeInertness.v` (rocq-rho-bridge, zero-admission, `9c9300ec`) | **proven**: `f1r3node_never_depends_on_mettail`, `f1r3node_does_not_reach_mettail`, `bridge_acyclic` — the invariant the host guard `mettail_rust_is_not_a_cargo_dependency` enforces |
| OslfResourceLogic conformance (4 laws) — M-RHO.0.2 | `MettaOslfLawsConformance.v` + `MettaGsltPresentation.v` (rocq-rho-bridge, zero-admission, `03082acc`) | **proven**: 4 OSLF laws over the modelled `is_funded` (= `delta_sigma`'s `Δ+margin≤Σ`) + lane-decomposition sound/complete; 2nd instance reusing `GSLTOSLFCapstone.v`/`LinearLogicResources.v`. Rust `MettaResourceLogic` delegates to verified `delta_sigma`; 3/3 adapter tests green |
| spec→Rholang lowering total-or-reject (M-RHO.0.3) | `RhoLoweringTotalOrRejects.v`, `RhoRejectedCoverage.v`, `RhoParWellFormedness.v`, `RhoArtifactBoundary.v` (rocq-rho-bridge, zero-admission, `9478e791`) | **proven**: `lowering_total`/`_sound`/`_disjoint`/`_count` + lowered/rejected characterized by `supported` — every `LanguageDef` rule is lowered OR explicitly rejected, never silently dropped. `RhoRejectedCoverage.v` proves rejected-rule delegation is exact at the rule-identity/set level: `AllRulesLowered` accepts only an empty rejected set, delegated evidence is valid exactly when delegated rules equal rejected rules, and omitted or stale delegated rules block the default-backend gate. The generated artifact is normalized `rhoapi::Par`, not Rholang text; `RhoParWellFormedness.v` proves the scalar-contract shape, positive bind counts, bind-count agreement, return-channel convention, metadata cleanliness, validation soundness/completeness, invalid-artifact rejection, and lowered-scalar validation matched by the Rust validator. `RhoArtifactBoundary.v` proves generated-backend source-text artifacts are rejected, every accepted current artifact is a validated AST, and the current lowered scalar artifact is AST and not source text. `mettail-rho-codegen` incl. normalized-AST contract shape, opaque validated-artifact typestate, operand-first/return-channel-last ABI, de Bruijn binding order, `locally_free`/`connective_used` artifact validation, bounded bind-count validation before count-derived allocation, deadlock report gate, and full flip-decision/default-backend planning gate |
| Differential oracle rho ≡ Ascent (M-RHO.0.4/.0.5) | `OracleQuotientEquivalence.v` (rocq-rho-bridge, zero-admission, `168859e3`) | **proven + run on BOTH backends**: the oracle (weight-erase ∘ eqrel-quotient) is a sound exact equivalence; the lowered calculator runs on a real in-memory `RhoRuntime` (`bfe56c4b`) and its result equals `CalculatorLanguage::run_ascent` normal forms (`7629c828`, 5/5 Int ops: 2+3/10−4/3×7/20÷4/17%5) |
| Generic call-by-need observation and budget (M-RHO.2) | `RhoCallByNeedObservation.v`, `RhoCallByNeedBudget.v` + `mettail-rho-codegen::need` + `mettail-rho-runtime/tests/rho_call_by_need.rs` | **proven model + Rust admission gate + runtime oracle**: forcing a sound memoized thunk observes `source_eval`, a cold force memoizes, repeated forcing is observationally idempotent, and memo soundness is preserved. The bounded admission contract proves zero lookahead blocks before observation, memo hits consume only lookahead, memo misses consume lookahead and exactly one heap cell, cold forces without heap budget block without consuming budget, successful bounded forces require admission, and successful observations still match source evaluation. Rust `admit_call_by_need_force` mirrors the budget contract with explicit `LookaheadExceeded` and `HeapBudgetExceeded` blockers; runtime oracle tests exercise cold and hot thunk forcing on a real RhoRuntime source-oracle path |
| Ambiguity-set preservation | `AmbiguityWitnessEnumeration.v`, `AmbiguitySetPreservation.v`, `rho_comm_oracle.rs` order-sensitive race | **proven bridge model + runtime oracle**: enabled witnesses are enumerated independently of schedule order, candidate-set observations are order-insensitive, and the one-bind contention oracle covers both eager race outcomes |
| Δ1 N-ary candidate minima and min-cost matching join | `DeltaOneMinCostJoin.v`, `DeltaOneMinCostMatching.v` via `rocq-rho-bridge-delta1` + `mettail-rho-adapter::delta1` | **proven model + Rust adapter gate**: candidate selection proves selected preformed candidates are present, non-refuted, and enabled-cost-minimal; present enabled minimal candidates are selected by the relational contract; refuted candidates are absent; strictly more expensive enabled candidates cannot be selected while a cheaper enabled candidate exists. Bipartite matching proves selected finite-frontier matchings are actual input edges, endpoint-valid, non-refuted, duplicate-free on left and right endpoints, covering for required left obligations, allowed to leave extra right witnesses unused, and globally cost-minimal among enabled left-perfect matchings; equal-cost optimal alternatives remain selected. Rust tests preserve equal-cost ambiguity, prove refutation precedes ordering, reject impossible left-perfect matchings, ignore out-of-frontier edges, cover the empty-left frontier case, include an unused-right-witness regression, include a greedy-trap global-optimality regression, and expose exact `u128` total costs for the exhaustive reference selector |
| Per-purse determinism for funded Rho candidates | `RhoPurseDeterminism.v` + `formal/tla/rho_settlement/` + `mettail-rho-adapter::LocatedEscrowLedger` | **proven model + TLC model check + Rust adapter gate**: duplicate purse states reject, missing purse targets preserve the ledger, local settlement blockers preserve the ledger, located actions are deterministic, a successful located action preserves the target purse, and actions located at two distinct purses commute on the final ledger; TLC explores both two-purse reserve schedules plus duplicate/missing-purse reject stutters; Rust regressions cover duplicate rejection, missing purse preservation, matching-purse update, local blocker preservation, distinct-purse commutation, repeated-sequence determinism, and equivalence of the owned no-clone apply path with borrowed apply |
| Escrow/refund settlement for funded Rho candidates | `RhoEscrowSettlement.v` + `mettail-rho-adapter::settlement` | **proven model + bounded-machine adapter gate**: reserve moves available funds into escrow and returns a matching ticket; commit moves exactly the ticket amount from escrow into charged funds; refund restores a successful reserve; successful reserve/commit/refund conserve total funds; purse mismatches, insufficient available funds, insufficient escrow, and bounded `u64` arithmetic overflow fail closed and preserve the input state; Rust regressions cover reserve, commit, refund, mismatch, over-escrow, and overflow blockers |
| Predicated-types guarded-COMM soundness | `GuardedCommSoundness.v` + `mettail-rho-runtime/tests/rho_guard_oracle.rs` + `MsoAutomataEquivalence.v` (recognition) | **proven bridge model + runtime oracle**: false guards do not commit, missing-premise attempts do not commit, guarded attempts fabricate no facts, and true enabled guards add the output; host Rholang `where` tests show failed single-bind and cross-bind guards leave data resting and later satisfying data can still commit; recognition automata equivalence remains covered by the existing advanced-automata target |
| Per-language Rho flip gate, planned execution, and observation boundary | `RhoBackendFlipGate.v`, `RhoRejectedCoverage.v`, `RhoPlannedExecutionBoundary.v`, `RhoArtifactBoundary.v`, `RhoObservationReportBoundary.v`, `RhoRuntimeBackendReportBridge.v`, `RhoLanguageBackendWrapper.v` + `mettail-rho-codegen::{backend,flip,deadlock,validate}` + `mettail-rho-runtime::{PlannedRhoBackend,RhoObservationReport,RhoRuntimeBackedLanguage}` | **proven gate + Rust decision/planning/execution/observation API**: the flip gate is proof ∧ oracle ∧ coverage ∧ artifact-validation ∧ scheduler-fairness ∧ no-new-deadlocks; no blockers iff the flip Boolean is true; any blocker blocks the flip; malformed generated `Par` blocks through `RhoFlipBlocker::ArtifactValidation`; unmet scheduler fairness blocks through `RhoFlipBlocker::SchedulerFairness`; any non-empty analyzer diagnostic blocks the deadlock gate. The default-backend planner also requires exact coverage of scalar-lowering rejections: uncovered rejected rules and stale delegation claims both block the plan, with exact rule-set equality proved in `RhoRejectedCoverage.v`. The planned execution boundary proves and enforces that generated backend execution consumes a `RhoDefaultBackendPlan`, not merely a raw validated artifact; raw `ValidatedRhoProgram` execution remains oracle/debug-only. The artifact boundary proves planned current execution never uses source-text artifacts and has a validated AST artifact. The planned observation boundary proves reports preserve planned execution, channel, read-order values, exact order-insensitive set fingerprints, and exact order-insensitive counted fingerprints without using `AscentResults` as the result shape. `RhoRuntimeBackendReportBridge.v` proves that the typed Rho observation-to-generic runtime report conversion preserves Rho backend identity, normalized-AST artifact identity, channel, values, observed count, and evidence references while remaining observation-shaped, not Ascent-shaped. `RhoLanguageBackendWrapper.v` proves that a generated language can be wrapped as Rho-default without installing Rho in the generated crate: the wrapper defaults to Rho, delegates non-Rho backend support to the inner language, requires planned backend evidence plus a total typed invocation, returns observation-shaped reports, and rejects Ascent-shaped seeded facts on the Rho path. The codegen analyzer reports static waits without producers and closed wait cycles without external or seed entry; scalar lowering emits a clean report because lowered contracts are exported service entries; Rust `decide_rho_flip` reports all blockers together and `plan_rho_default_backend` returns either a concrete validated `RhoProgram` normalized-AST plan or complete blocker diagnostics |
| Runtime backend dispatch | `RuntimeBackendDispatch.v`, `RhoLanguageBackendWrapper.v` + `mettail_runtime::Language::{default_runtime_backend,run_with_backend,run_default_backend,run_backend_report,run_default_backend_report}` + `mettail_rho_runtime::RhoRuntimeBackedLanguage` | **proven fail-closed selector + Rust dispatch API**: generated languages inherit Ascent as the explicit default until a verified Dovetail/Rho gate installs another backend; requested or default Dovetail/Rho execution cannot silently fall back to Ascent when absent. A Rho-default instance is installed by wrapping the generated language in `RhoRuntimeBackedLanguage`, preserving the generated crate's substrate-neutral dependency direction while routing default report execution to a planned normalized-AST Rho backend. The REPL `exec` path now calls `run_default_backend_report`, preserves Ascent-shaped graph navigation when present, and stores observation-shaped runtime reports without fabricating `AscentResults`. Simulation `run_to_normal_form` now also calls `run_default_backend_report`: Ascent-shaped reports keep the existing rewrite-graph BFS and rule-coverage path, observation-shaped reports become terminal `TraceOutcome::RuntimeObservations` JSONL outcomes, and unsupported future report shapes fail closed. Generated-language simulation integration tests and the supply-chain example moved to `mettail-languages`, leaving `mettail-simulation` substrate-neutral and testable without compiling generated languages |
| Generated operational runtime tests | `macros/src/gen/test_gen/operational_tests/*` + `mettail_testkit::runtime_report` | **Rust-checked generator boundary**: generated expected-output, smoke, precedence, associativity, type-preservation, and algebraic-property templates evaluate parsed terms through `Language::run_default_backend_report` via public testkit helpers rather than direct `run_ascent`; comparisons accept both Ascent normal forms and observation-shaped runtime outputs. Type-preservation uses semantic backend outputs rather than channel-summary diagnostics, and algebraic concrete/proptest checks compare backend-output overlaps. Identity-law generation records whether the identity element is the left or right argument and emits property-based checks for that side instead of a smoke-only placeholder |

---

*Update protocol: when a phase lands, flip its row(s) to `proven`/`modeled-CE`, link the
commit, and (if it removed baseline failures) update `baseline-cf03e571.md`.*
